
## 1. Назначение циклов

**Цикл** — это конструкция языка, позволяющая **многократно выполнять один и тот же фрагмент кода**, пока выполняется заданное условие.

Используются для:

- перебора массивов;
- обработки строк;
- реализации алгоритмов;
- повторяющихся вычислений.
    
---

## 2. Основные виды циклов в C

В языке C есть **три оператора цикла**:

1. `while` — цикл с предусловием
2. `do ... while` — цикл с постусловием
3. `for` — цикл со счётчиком
    
---

## 3. Цикл `while`

Используется, когда количество итераций неизвестно заранее, но зависит от некоторого условия

`while (условие) {     тело цикла }`

 Условие проверяется до выполнения тела цикла. Если оно ложно с самого начала, тело цикла не выполнится ни разу


---

## 4. Цикл `do ... while`

Похож на while , но условие проверяется после выполнения тела цикла. 

`do {     тело цикла } while (условие);`

1. Тело цикла выполняется **минимум один раз**.
2. После выполнения проверяется условие.
3. Если условие истинно — повтор.
    
📌 **Тело выполняется хотя бы один раз всегда.**

---
## 5. Цикл `for`
Используется, когда количество итераций известно заранее

`for (инициализация; условие; шаг) {     тело цикла }`

- Инициализация: выполняется перед началом цикла, задаёт начальное состояние. 
- Условие: проверяется перед каждой итерацией; если условие ложно, цикл завершает работу. 
- Обновление: выполняется после каждого прохода тела цикла

1. Выполняется **инициализация** (один раз).
2. Проверяется **условие**.
3. Если истинно — выполняется тело цикла.
4. Выполняется **шаг**.
5. Возврат к пункту 2.
    
####  Особенности `for`

- все три части **необязательны**;
- можно писать пустые выражения.
    
`for (;;) { }   // бесконечный цикл`

|Характеристика|while|do while|for|
|---|---|---|---|
|Проверка условия|до|после|до|
|Минимум выполнений|0|1|0|
|Типичное применение|неизвестное число повторений|ввод данных|счётчик|

---

### Управление выполнением цикла

Для изменения стандартного поведения циклов используются специальные операторы.

#### Оператор `break`

Прерывает выполнение текущего цикла и выходит из него.
``` c
while (1) { // Бесконечный цикл
    if (условие_остановки) {
        break; // Выход из цикла
    }
} // Выполнение продолжается здесь
```
#### Оператор `continue`

Пропускает оставшуюся часть текущей итерации и переходит к следующей.

```c
for (int i = 0; i < N; i++) {
    if (данные_невалидны) {
        continue; // Переходим сразу к i++ и следующей проверке
    }
    // Здесь происходит основная обработка
}
```
####  goto

Позволяет перейти к произвольной метке. Рекомендуется избегать из-за снижения читаемости кода

**Принцип работы:** `goto метка;` переводит выполнение программы к строке, помеченной `метка:`.
``` c
for (i = 0; i < N; i++) {
    for (j = 0; j < M; j++) {
        if (ошибка) {
            goto cleanup; // Выход из обоих циклов
        }
    }
}

// ... Обычный код

cleanup:
    // Централизованный код очистки ресурсов, закрытия файлов и т...
    printf("Произошла ошибка, очистка завершена.\n");
```
####  Оператор `return` в циклах

Прерывает выполнение функции, включая все циклы, и возвращает управление вызывающему коду.

**Принцип работы:** Как только `return` выполняется, **все локальные переменные функции уничтожаются**, и выполнение кода в данной функции прекращается. Если функция не является `void`, `return` должен сопровождаться возвращаемым значением.

`return`:

- **выходит из функции**, а не только из цикла;
- завершает выполнение всей функции.
``` c
int find_element(int arr[], int size, int target) {
    for (int i = 0; i < size; i++) {
        if (arr[i] == target) {
            // Элемент найден. Прекращаем выполнение всей функции 
            // и возвращаем индекс элемента.
            return i; 
        }
    }
    // Если цикл завершился (элемент не найден), возвращаем специальное значение.
    return -1; 
}
```

**Ключевое отличие от `break`:**

- `break` выходит **только из цикла**.
- `return` выходит **из всей функции**, содержащей цикл.
####  Вложенные циклы

Циклы могут быть вложены друг в друга.

`for (int i = 0; i < n; i++) {     for (int j = 0; j < m; j++) {         ...     } }`

Вложенные циклы увеличивают алгоритмическую сложность:

- 2 цикла → `O(n²)`
- 3 цикла → `O(n³)`
    
