Отлично, давай разберу все приоритетные очереди (кучи) из твоих вопросов. Это шпаргалка для быстрого ответа, без кода, но со всеми ключевыми деталями.

---

## **1. БИНАРНАЯ КУЧА (Binary Heap)**

**Реализация:** Динамический массив

**Свойство кучи:** Для min-heap: родитель ≤ детей. Для max-heap: родитель ≥ детей.

**Структура в массиве:**

- Индексация с 0: левый ребенок = 2i+1, правый = 2i+2, родитель = (i-1)/2
- Индексация с 1: левый = 2i, правый = 2i+1, родитель = i/2

**Операции:**

- **Вставка (insert):** O(log n)
    
    - Добавляем в конец массива
    - Просеиваем вверх (sift-up/bubble-up): сравниваем с родителем, меняем местами если нарушено свойство кучи
- **Поиск минимума/максимума (find-min/max):** O(1)
    
    - Просто возвращаем array[0] (корень)
- **Удаление минимума/максимума (extract-min/max):** O(log n)
    
    - Сохраняем корень
    - Заменяем корень последним элементом
    - Уменьшаем размер
    - Просеиваем вниз (sift-down/heapify): сравниваем с детьми, меняем местами с меньшим/большим ребенком, пока свойство не восстановится
- **Слияние (merge) двух куч:**
    
    - **С разрушением:** O(n + m)
        - Копируем все элементы второй кучи в первую
        - Вызываем heapify для всего массива снизу вверх (строим кучу за линейное время)
    - **Без разрушения:** O(n + m)
        - То же самое, но создаем новую кучу

**Построение кучи из массива (heapify):** O(n) — идем с конца, просеиваем вниз все не-листья

---

## **2. ЛЕВОСТОРОННЯЯ КУЧА (Leftist Heap)**

**Реализация:** Бинарное дерево (каждый узел - структура с указателями на левого и правого ребенка)

**Свойства:**

- Свойство кучи (родитель ≤ детей для min-heap)
- **Левостороннее свойство:** dist(left) ≥ dist(right)
    - dist(узел) = минимальное расстояние до внешнего узла (узла с < 2 детей)
    - dist(NULL) = -1, dist(листа) = 0

**Структура узла:**

```
struct Node {
    int key;
    int dist;  // ранг/расстояние
    Node *left, *right;
}
```

**Операции:**

- **Слияние (merge):** O(log n) — **КЛЮЧЕВАЯ ОПЕРАЦИЯ!**
    
    1. Если одна куча пустая → возвращаем другую
    2. Сравниваем корни, меньший становится новым корнем
    3. Рекурсивно мержим правое поддерево нового корня с другой кучей
    4. После слияния проверяем левостороннее свойство: если dist(left) < dist(right) → swap(left, right)
    5. Обновляем dist = dist(right) + 1
- **Вставка (insert):** O(log n)
    
    - Создаем кучу из одного элемента
    - Мержим с основной кучей
- **Поиск минимума (find-min):** O(1)
    
    - Возвращаем корень
- **Удаление минимума (extract-min):** O(log n)
    
    - Сохраняем корень
    - Мержим левое и правое поддеревья
    - Возвращаем сохраненное значение
- **Слияние с/без разрушения:**
    
    - С разрушением: используем исходные деревья
    - Без разрушения: предварительно копируем одно/оба дерева

**Почему левосторонняя?** Гарантирует, что правый путь короткий (≤ log n), поэтому merge работает быстро

---

## **3. КОСАЯ КУЧА (Skew Heap)**

**Реализация:** Бинарное дерево (как левосторонняя, но без хранения dist)

**Свойства:**

- Свойство кучи
- **НЕТ строгого левостороннего свойства**
- Самобалансирующаяся через swap при каждом merge

**Структура узла:**

```
struct Node {
    int key;
    Node *left, *right;
    // НЕТ поля dist!
}
```

**Операции:**

- **Слияние (merge):** O(log n) амортизированно
    
    1. Если одна куча пустая → возвращаем другую
    2. Сравниваем корни, меньший становится новым корнем
    3. Рекурсивно мержим правое поддерево нового корня с другой кучей
    4. **ВСЕГДА меняем местами left и right детей** (это ключевое отличие!)
    5. Возвращаем новый корень
- **Вставка (insert):** O(log n) амортизированно
    
    - Создаем узел
    - Мержим с кучей
- **Поиск минимума (find-min):** O(1)
    
    - Корень
- **Удаление минимума (extract-min):** O(log n) амортизированно
    
    - Сохраняем корень
    - Мержим left и right поддеревья
- **Слияние с/без разрушения:** аналогично левосторонней
    

**Отличие от левосторонней:**

- Проще (нет dist)
- Безусловный swap при каждом merge
- O(log n) амортизированно, а не в худшем случае

---

## **4. БИНОМИАЛЬНАЯ КУЧА (Binomial Heap)**

**Реализация:** Кольцевой список биномиальных деревьев

**Биномиальное дерево B_k:**

- **Ранг k** — количество детей у корня
- **Число узлов:** 2^k
- **Высота:** k
- **Рекурсивное определение:** B_0 = один узел; B_k = два B_{k-1}, где одно подвешено как ребенок корня другого
- **Число узлов на глубине d:** C(k, d) — биномиальный коэффициент (отсюда название)

**Структура:**

- Список биномиальных деревьев различных рангов (не более одного дерева каждого ранга)
- Каждое дерево удовлетворяет свойству min-heap
- Представление: кольцевой список корней + у каждого узла список детей

**Узел:**

```
struct Node {
    int key;
    int degree;  // ранг
    Node *parent, *child, *sibling;  // указатели
}
```

**Операции:**

- **Слияние (merge):** O(log n)
    
    - Объединяем списки деревьев
    - Процесс похож на сложение двоичных чисел с переносом
    - Если два дерева одного ранга: связываем их (дерево с большим корнем подвешиваем к меньшему), получаем дерево ранга k+1
    - Продолжаем пока не останется максимум одно дерево каждого ранга
- **Вставка (insert):** O(log n) в худшем, O(1) амортизированно
    
    - Создаем кучу из одного элемента (дерево B_0)
    - Мержим с основной кучей
- **Поиск минимума (find-min):** O(log n)
    
    - Проходим по корням всех деревьев (их максимум log n)
    - Возвращаем минимальный
- **Удаление минимума (extract-min):** O(log n)
    
    - Находим дерево с минимальным корнем
    - Удаляем этот корень
    - Его дети становятся отдельными биномиальными деревьями рангов 0, 1, ..., k-1
    - Мержим эти деревья с оставшейся кучей
- **Слияние с/без разрушения:**
    
    - С разрушением: O(log n)
    - Без разрушения: O(n) — нужно копировать все узлы

**Представление числа элементов:** Если в куче n элементов, и n в двоичной записи = b_k...b_1b_0, то биномиальная куча содержит дерево B_i тогда и только тогда, когда b_i = 1

---

## **5. ФИБОНАЧЧИЕВА КУЧА (Fibonacci Heap)**

**Реализация:** Кольцевой список деревьев общего вида (не обязательно биномиальных)

**Свойства:**

- Свойство min-heap
- **Ленивая структура:** откладываем работу до extract-min
- Деревья не обязательно биномиальные (могут быть произвольные)

**Структура:**

- Кольцевой список корней деревьев
- Указатель на минимум
- У каждого узла: степень (degree), флаг marked, указатели на parent, child, left/right sibling

**Узел:**

```
struct Node {
    int key;
    int degree;
    bool marked;  // для cascading cut
    Node *parent, *child, *left, *right;
}
```

**Операции:**

- **Вставка (insert):** O(1) амортизированно
    
    - Создаем узел
    - Добавляем в корневой список
    - Обновляем min, если нужно
- **Поиск минимума (find-min):** O(1)
    
    - Храним указатель на минимум
- **Слияние (merge):** O(1)
    
    - Конкатенируем корневые списки
    - Обновляем указатель на минимум
- **Удаление минимума (extract-min):** O(log n) амортизированно
    
    - Удаляем минимальный узел
    - Детей добавляем в корневой список
    - **Консолидация (consolidation):**
        - Проходим по корням
        - Объединяем деревья одинаковой степени (как в биномиальной куче)
        - Строим массив указателей по степеням
        - В результате не более одного дерева каждой степени
    - Находим новый минимум
- **Уменьшение ключа (decrease-key):** O(1) амортизированно
    
    - Уменьшаем значение ключа
    - Если нарушено свойство кучи:
        - **Срезаем (cut):** отцепляем узел от родителя, добавляем в корневой список
        - **Каскадный срез (cascading cut):** если родитель был marked, срезаем и его рекурсивно
        - Unmarked узел при первом срезе ребенка становится marked
- **Удаление узла (delete):** O(log n) амортизированно
    
    - decrease-key до -∞
    - extract-min

**Почему Фибоначчи?**

- Максимальная степень узла ≤ log_φ(n), где φ = (1+√5)/2 — золотое сечение
- Связано с числами Фибоначчи в анализе амортизированной сложности

**Сравнение с биномиальной:**

- Фибоначчиева быстрее для insert и decrease-key (O(1) vs O(log n))
- Но сложнее в реализации
- Используется в алгоритмах Дейкстры и Прима для графов

---

## **КЛЮЧЕВЫЕ ОТЛИЧИЯ:**

|Операция|Бинарная|Левосторонняя|Косая|Биномиальная|Фибоначчиева|
|---|---|---|---|---|---|
|**insert**|O(log n)|O(log n)|O(log n) ам.|O(log n), O(1) ам.|**O(1) ам.**|
|**find-min**|**O(1)**|**O(1)**|**O(1)**|O(log n)|**O(1)**|
|**extract-min**|O(log n)|O(log n)|O(log n) ам.|O(log n)|O(log n) ам.|
|**merge**|O(n+m)|**O(log n)**|**O(log n) ам.**|**O(log n)**|**O(1)**|
|**decrease-key**|O(log n)|O(log n)|O(log n) ам.|O(log n)|**O(1) ам.**|

---

## **ПОДКОВЫРИСТЫЕ ВОПРОСЫ ДЛЯ ПРЕПОДА:**

1. **"Почему бинарную кучу храним в массиве?"**
    
    - Компактность (нет указателей)
    - Кэш-локальность
    - Простая адресация через формулы
2. **"Почему в левосторонней dist(right)+1, а не dist(left)?"**
    
    - Мы всегда мержим в правое поддерево
    - dist показывает длину правого пути
    - Левое гарантированно не короче правого
3. **"В чем смысл swap в косой куче?"**
    
    - Амортизация: тяжелые правые пути становятся легкими левыми
    - Не нужно хранить дополнительную информацию (dist)
4. **"Почему в биномиальной куче максимум log n деревьев?"**
    
    - n элементов = двоичная запись → максимум log₂(n) + 1 бит
    - Каждый бит = наличие/отсутствие дерева ранга i
5. **"Зачем marked флаг в фибоначчиевой куче?"**
    
    - Контролирует cascading cut
    - Гарантирует, что степень узла не слишком отличается от числа детей
    - Обеспечивает логарифмическую максимальную степень
6. **"Когда использовать какую кучу?"**
    
    - Бинарная: простота, быстрый find-min, если не нужен merge
    - Левосторонняя/косая: когда нужен эффективный merge
    - Биномиальная: теоретически интересна, баланс операций
    - Фибоначчиева: графовые алгоритмы с decrease-key (Dijkstra, Prim)

---

Готово! Это компактная шпаргалка со всеми ключевыми деталями. Если препод спросит что-то конкретное — у тебя есть база для развернутого ответа. Удачи на зачете! 🔥