## Введение

Компьютеры хранят информацию на различных **носителях вторичной памяти** (secondary storage). Эти устройства, как правило, **энергонезависимы (nonvolatile)**, то есть их содержимое сохраняется даже после перезагрузки системы.

Примеры таких носителей:

- **Устройства NVM (Non-Volatile Memory)**: Например, современные SSD.
    
- **HDD (Hard Disk Drives)**: Традиционные жёсткие диски.
    
- **Магнитные ленты (magnetic tapes)**.
    
- **Оптические диски (optical disks)**: Например, CD, DVD.

Для того чтобы сделать использование компьютерной системы **удобным**, операционная система предоставляет **единый логический взгляд** на всю хранящуюся информацию.
ОС **абстрагируется (abstracts from)** от физических свойств конкретных устройств хранения и определяет **логическую единицу хранения** — **файл (file)**.

В общем смысле, файл — это **последовательность битов, байтов, строк или записей**, **значение** которой определяется **создателем и пользователем** этого файла. Файлы обычно представляют программы (исходный код или исполняемый код) **и данные**.

**Файловая система (ФС)** — это мост между пользователем (или программой) и данными, которые физически хранятся на диске. Её главные задачи:

1. **Скрыть сложность.** Пользователь работает с файлами (открывает, сохраняет), а ФС за кулисами преобразует эти операции в чтение и запись конкретных блоков на диске.
    
2. **Обеспечить единообразие.** Одна и та же программа может работать с файлами на флешке, SSD-диске или в сети, потому что ФС предоставляет стандартный интерфейс.
    
3. **Организовать данные.** ФС ведет учет того, какие блоки диска заняты, какие свободны, где хранятся метаданные файлов (имя, размер, права доступа и т.д.).

## Три причины «многослойности»

1. **Независимость (Interoperability):** Вы можете обновить Windows, не меняя диск. Или сменить диск, не переустанавливая программы. Каждый слой защищает другой от изменений.
    
2. **Масштабируемость:** Если завтра появятся квантовые диски размером в петабайты, мы просто добавим новый «слой» или расширим LBA, но ваш текстовый редактор Word будет работать так же, как и 20 лет назад.
    
3. **Безопасность и Права:** Именно на уровне «Файла» и «Блока» ОС может сказать: «Сюда этому пользователю нельзя!», хотя на уровне «Сектора» диску вообще всё равно, кто его читает.
    

> **💡 Фишка Специалиста:** Эта «матрешка» позволяет делать крутые штуки. Например, **виртуальные диски**. Вы можете создать огромный **файл** на диске `C:`, и ОС «обманет» саму себя, заставив думать, что этот файл — это целый **отдельный физический диск** со своими секторами и LBA. Это база для всех облачных серверов и виртуальных машин.

## Назначение ФС

![[Pasted image 20251206133656.png]]

- Это создание **стандартизированного набора правил и функций** (API) для работы с данными, независимо от того, как эти данные физически хранятся. **Для всех файловых систем в системе используются одинаковые команды:**  `open`, `read`, `write`, `close`, `mkdir`, `unlink` — и не важно, NTFS это, ext4, флешка или сетевой диск

- Это создание **прослойки** (VFS - Виртуальная Файловая Система) между прикладной программой и драйвером конкретной файловой системы (например, NTFS или ext4).Между программой и реальной файловой системой есть **прокладка-переводчик**, которая скрывает все сложности конкретной FS.

- Это применение единых правил и методов организации данных к **разнообразному** физическому оборудованию.

### Блок
**Блочное устройство:** устройство, которое позволяет читать/писать фиксированные порции данных — _блоки_ (обычно 512B, 4KiB, реже другие размеры). Все операции ввода-вывода (I/O) на уровне физического устройства происходят **блоками**.
ФС настраиваются на разный размер блока (4КБ, 8КБ, 16КБ и т.д.).

- **Маленький блок (4КБ):** Меньше потерь места (меньше внутренней фрагментации), но больше метаданных и более медленная работа с очень большими файлами (нужно считывать много маленьких блоков).
    
- **Большой блок (64КБ):** Быстрая работа с большими файлами (меньше I/O операций), но много потерянного места, если вы храните много маленьких файлов. Выбор размера блока — это всегда **компромисс** между скоростью и эффективностью использования пространства.
## 🌐 Роль Виртуальной Файловой Системы (VFS)

VFS, как вы верно заметили, находится **между** прикладными программами и конкретными реализациями файловых систем (таких как NTFS, ext4 и т.д.).

Ее главная роль — **обеспечение единообразия** и **модульности**.

### 1. Единый Интерфейс (Сверху)

Для **Приложений** (верхний уровень):

- Приложения не должны знать, какую именно ФС они используют. Они просто вызывают **стандартизированные системные вызовы** (те, о которых мы говорили: `open`, `read`, `write`).
    
- **VFS** предоставляет этот **единый, стабильный API**. Все запросы от приложений (например, "прочитать файл `/home/user/doc.txt`") сначала попадают в VFS.
    

### 2. Абстракция и Диспетчеризация (В Центре)

Это ядро работы VFS.

- **Маршрутизация:** VFS анализирует путь к файлу (`/home/user/doc.txt`), определяет, на какой **смонтированный** раздел и, соответственно, какой **драйвер конкретной ФС** (например, `ext4_module`) нужно передать запрос.
    
- **Стандартизация Структур:** VFS оперирует общими, абстрактными объектами, которые каждая конкретная ФС обязана реализовать. Ключевые структуры VFS:
    
    - **Суперблок (Superblock):** Общие метаданные о ФС (тип, размер).
        
    - **dentry (Directory Entry):** Используется VFS для управления деревом каталогов и связывания имен файлов с Inode.
        
    - **Inode (Индексный дескриптор):** Абстрактная структура, представляющая метаданные файла.
        

VFS не заботится о том, как драйвер ext4 хранит свой `inode` на диске; ей важно, чтобы драйвер ext4 смог предоставить ей информацию из этого `inode` по ее запросу.

### 3. Связь с Конкретной ФС (Снизу)

VFS выступает как **мост** к конкретному драйверу.

- Когда VFS получает запрос `read`, она вызывает соответствующую **специализированную функцию** внутри драйвера, например, `ext4_read()` или `ntfs_read()`.
    
- Драйвер (например, `ext4_module`) берет запрос VFS: "Прочитай 100 байт из файла с таким-то Inode".
    
- Драйвер сам смотрит в свой **Inode Table**, находит **номера Блоков ФС**, где хранятся эти данные.
    
- Драйвер преобразует эти **Номера Блоков** в соответствующие **LBA** (адреса Секторов).
    
- Драйвер передает запрос: "Контроллер, прочитай LBA №X, Y, Z" на уровень блочного устройства.
    

---

## 💡 Лестница Адресации и Абстракции

|**Уровень**|**Кто работает?**|**Единица Адресации**|**Задачи**|
|---|---|---|---|
|**1. Приложение**|Пользовательский софт|Имя Файла / Путь|`fopen("file.txt", "r")`|
|**2. VFS**|Ядро ОС (Диспетчер)|dentry / Абстрактный Inode|Маршрутизация, Единый Интерфейс.|
|**3. Конкретная ФС**|Драйвер (NTFS, ext4)|**Номер Блока ФС**|Перевод dentry в Inode, Inode в Блоки, **Блоки в LBA**.|
|**4. Драйвер Диска**|Драйвер блочного устройства|**LBA (Номер Сектора)**|Передача LBA контроллеру диска.|
|**5. Диск**|Контроллер/Железо|**Сектор**|Физическое чтение/запись данных.|

**Самый главный вывод:**

VFS **не знает** о Блоках ФС и тем более об LBA/Секторах. Она оперирует только абстрактным понятием **Файл**.

**Конкретная ФС** — это та часть, которая **связывает** логический мир VFS с физическим миром LBA и Секторов. Именно она отвечает за **распределение места** и **перевод адресов**.

Теперь вы понимаете, что VFS обеспечивает совместимость на уровне ОС, а драйвер ФС обеспечивает совместимость на уровне хранения данных
### (Virtual File System)

```
Приложение (open/read/write/close)
          ↓ (системный вызов)
        Ядро (syscall dispatcher)
          ↓
         VFS  ←——— единственная входная точка файловых операций
          ↓
  Конкретный драйвер FS (ext4, ntfs, xfs, nfs, fuse...)
          ↓
    Блочное устройство / контроллер диска (HDD/SSD) / сеть

```
---
## Чем различаются драйверы файловых систем?

**Драйвер ФС** — это код в ядре, который знает:

> 📌 **как интерпретировать байты на диске как файлы и каталоги**

То есть:

- где лежат метаданные
- как выглядит inode / запись файла
- как найти данные файла
- как выделять и освобождать место
- как обеспечивать целостность
    

👉 **Диск — это просто массив блоков**  
👉 **ФС-драйвер говорит: “вот здесь каталог, вот здесь файл, вот здесь его данные”**

Представьте, что **драйвер** — это переводчик. Все программы говорят на языке «Дай мне файл», а диски — на языке «Дай мне блок №500». Но каждый драйвер делает этот перевод по-разному, потому что у каждой файловой системы своя «грамматика» (структура данных).

**Основные отличия драйверов:**

- **Метод поиска данных:** Один драйвер ищет файл через простую таблицу (как FAT), другой — через сложные деревья (как NTFS или Btrfs), что гораздо быстрее на огромных дисках.
    
- **Отказоустойчивость (Журналирование):** Продвинутые драйверы (ext4, NTFS) ведут «дневник» (журнал) всех действий. Если выдернуть вилку из розетки, драйвер по журналу поймет, что он не успел дописать, и не даст данным превратиться в «кашу». Дешевые драйверы (FAT32) так не умеют.
    
- **Лимиты:** Драйвер определяет, какого максимального размера может быть файл. Например, драйвер FAT32 «не понимает» файлы больше 4 ГБ.
    
- **Безопасность:** Драйвер NTFS умеет проверять права доступа (можно ли вам открывать этот файл), а драйвер FAT — нет

| Задача          | ext4     | FAT32       | NTFS        |
| --------------- | -------- | ----------- | ----------- |
| Где метаданные  | inode    | таблица FAT | MFT         |
| Как искать файл | по inode | по цепочке  | по индексам |
| Защита от сбоев | журнал   | ❌ нет       | журнал      |
| Права доступа   | POSIX    | ❌ нет       | ACL         |
| Большие файлы   | отлично  | плохо       | отлично     |
## Сравнение драйверов (Файловых систем)

| **Файловая система** | **Где живет** | **Плюсы**                                           | **Минусы**                                                | **Для чего лучше**             |
| -------------------- | ------------- | --------------------------------------------------- | --------------------------------------------------------- | ------------------------------ |
| **NTFS**             | Windows       | Журналирование (надежность), права доступа, сжатие. | Сложная структура, медленнее на флешках.                  | Системный диск Windows.        |
| **ext4**             | Linux         | Очень быстрая, надежная, проверенная годами.        | Не читается Windows «из коробки».                         | Почти любой Linux.             |
| **FAT32**            | Везде         | Понимают все: от магнитолы до Mac.                  | Лимит на файл 4 ГБ, нет защиты от сбоев.                  | Маленькие флешки.              |
| **exFAT**            | Везде         | Работает везде, нет лимита 4 ГБ.                    | Нет журналирования (легко потерять данные при вытыкании). | Большие внешние диски, флешки. |
| **APFS**             | macOS / iOS   | «Мгновенное» копирование файлов, шифрование.        | Только для техники Apple и SSD.                           | Современные Mac и iPhone.      |

Разные драйверы — это разные способы организовать эти 5 компонентов. Один делает упор на скорость, другой — на надежность, третий — на простоту для флешек. Но все они работают внутри **разделов**, чтобы не превратить весь диск в одну неразделимую кучу данных.
## Структура диска

## Как происходит деление на разделы (Технически)

На самом деле, диск — это просто длинная цепочка секторов от 0 до бесконечности. Чтобы поделить его на разделы, в самый-самый первый сектор диска (или в первые несколько) записывается **Таблица Разделов**.

Существует два стандарта этой «карты»:

1. **MBR (Master Boot Record):** Старый стандарт. Понимает диски только до 2 ТБ и максимум 4 «основных» раздела.
    
2. **GPT (GUID Partition Table):** Современный стандарт. Понимает огромные диски и позволяет делать сколько угодно разделов.
    

### Как это выглядит в разных ОС:

- **В Windows:** Вы заходите в «Управление дисками». Там вы видите физический «Диск 0» и синие полоски на нем — это разделы. Вы можете «сжать» одну полоску и из пустого места сделать новую. Windows назначит им буквы `C:`, `D:`, `E:`.
    
- **В Linux:** Букв нет. Там всё дерево файлов начинается с корня `/`. Но вы можете «примонтировать» один раздел в папку `/home` (для фоток), а другой в папку `/var` (для баз данных). Для пользователя это выглядит как одна система папок, но физически это разные разделы.

## Как это устроено на реальном компе (Жизненный цикл)

1. **Покупка диска:** Он абсолютно пуст. Секторы от 0 до 1 000 000.
    
2. **Разметка (Partitioning):** Вы запускаете установку ОС. Она спрашивает: «Как делим?». Вы говорите: «Первые 200 000 секторов под систему, остальное под файлы». В этот момент создается **GPT/MBR таблица**.
    
3. **Форматирование:** Теперь на каждом разделе создается **Файловая Система**. В начало раздела записывается **Суперблок**, создаются пустые **Bitmaps** и **Таблицы Inode**.
    
4. **Работа:** Когда вы сохраняете фильм, драйвер ФС смотрит в свою таблицу: «Ага, на моем разделе свободны блоки с 500 по 1000». Он пишет туда, не зная, что за границей его раздела есть другой мир (другая ФС).
    

**Фишка спеца:** Если вы когда-нибудь удалите раздел случайно — не паникуйте! Сами данные (фильмы и фото) остаются на диске. Удаляется только запись в **Таблице Разделов** (карта). Специальные программы могут просканировать диск, найти «потерянные» Суперблоки и восстановить карту. Данные целы, пока вы не записали поверх них что-то новое!

# Почему нельзя просто «на всём диске»

Исторически — **можно**.  
Но это плохо.

###  Проблемы без разделов:

###  1. Нет изоляции

Если ФС повредилась → всё пропало.

###  2. Нет нескольких ФС

Нельзя:

- Linux + Windows
- ext4 + FAT
- /home отдельно

###  3. Нет информации для загрузчика

Загрузчик **не знает**, где искать ОС.

###  4. Нет гибкости

Хочешь:

- переустановить ОС
- поменять ФС
- увеличить /home

**Зачем это нужно:**

1. **Разделение ответственности:** Вы можете отдать один раздел под Windows (ФС NTFS), а другой под Linux (ФС ext4). Они не будут мешать друг другу.
    
2. **Изоляция сбоев:** Если на разделе с фильмами произойдет программная ошибка и ФС «сломается», раздел с системными файлами ОС останется в безопасности.
    
3. **Геометрия диска:** В начале диска всегда лежит **Таблица разделов** (MBR или GPT). Она говорит: «С сектора 2048 по сектор 1000000 — это Раздел 1». Когда вы создаете ФС, драйвер берет этот кусок и начинает рисовать на нем свою структуру (те самые группы блоков из вашего слайда).


![[Pasted image 20251206133702.png]]
**GPT (GUID Partition Table)** — это современная схема разметки диска.

```
Диск → GPT → Раздел → ext4 → файлы
```

1. **Protective MBR (LBA 0):**
    
    - Это "защитный" элемент для совместимости со старыми системами, которые не понимают GPT. Он сообщает такой системе: "Весь диск занят одним разделом, не трогай его". Это предотвращает повреждение GPT-структур.
        
2. **GPT Header (LBA 1):**
    
    - Это "оглавление" для таблицы разделов. В нем хранится служебная информация: где начинается и заканчивается таблица разделов, её контрольная сумма (для проверки целостности) и уникальный идентификатор диска.
        
3. **Partition Table (LBA 2–33)(128 записей по 128 байт каждая):**
    
    - Это сама **таблица разделов**. Она представляет собой массив записей, где каждый элемент описывает один раздел на диске (например, диск C: или D: в Windows). В каждой записи указано:
        
        - Тип раздела (ESP, Linux FS, Swap, Microsoft Basic Data и т.д.)
        - Уникальный GUID раздела
        - Начальный сектор раздела
        - Конечный сектор раздела
        - Флаги
        - Имя раздела (например: `"EFI System"`, `"Linux root"`)
            
    - Поскольку эта таблица находится в начале диска, её называют **Primary Partition Table**.
4. **Partition 1, 2, 3, 4...:**
    
    - Это сами **разделы диска**, то есть области, отведенные под данные. Именно на этих разделах и создаются файловые системы (например, NTFS, ext4, FAT32).
5. **Backup Partition Entries Array (Резервная таблица разделов)**    

6. Backup GPT Header (Резервный заголовок GPT)
     Разметка дублируется и защищена от повреждений.
     
7. **GPT Disk Layout (вся схема):**
    
    - ключевое преимущество GPT: если основные структуры в начале диска будут повреждены, их можно восстановить из копии в конце.

## Базовые единицы

![[Pasted image 20251206133707.png]]


**LBA — это просто номер сектора на диске.**  Диск — это как длинная линейка, нарезанная на одинаковые кусочки — **сектора**.

Сектор — это минимальный кусок данных, который умеет читать/писать диск
Если файл занимает 1 байт — диск всё равно прочтёт/запишет целый сектор.

Блок — это логическая единица файловой системы, состоящая из нескольких секторов.

---
### 1. Сектор (Sector)

- **Определение:** Сектор — это **минимально адресуемая физическая единица** на диске. Это неделимая порция данных, которую контроллер диска может прочитать или записать за одну операцию.
    
- **Традиционный размер (512 байт):** Исторически, большинство дисков (особенно старые HDD) использовали секторы размером 512 байт.
    
- **Современный размер (4096 байт или 4 КБ):** Современные жесткие диски и SSD, использующие технологию **Advanced Format (AF)**, применяют размер сектора **4096 байт**.
    
    - **Преимущество:** Секторы 4КБ более эффективны с точки зрения коррекции ошибок (ECC), так как им требуется меньше служебных данных (промежутков и ECC-кодов) на единицу данных, что увеличивает полезный объем диска.
        

> **☝️ Фишка Профи: Эмуляция 512e.** Многие диски с физическим сектором 4096 байт работают в режиме эмуляции **512e** (512-byte emulation). Это сделано для совместимости со старыми ОС и программами, которые ожидают 512-байтовые секторы. Контроллер диска берет запрос на 512 байт, но на самом деле читает или пишет весь 4КБ физический сектор. Это может вызвать **снижение производительности**, если логический 512-байтовый запрос не выровнен по границе физического 4КБ сектора.

### 2. Logical Block Address (LBA)

- **Определение:** **LBA** — это просто **порядковый номер** сектора на диске. Это линейная, одномерная схема адресации.
    
- **Роль:** Это стандартный способ, которым **Операционная Система (ОС)** и **контроллер диска** общаются.
    
- **Как это работает:**
    
    - Раньше использовалась схема **CHS** (Cylinder-Head-Sector), которая требовала от ОС знания геометрии диска.
        
    - LBA полностью абстрагирует геометрию. ОС говорит: "Контроллер, прочитай данные, начиная с LBA №1000". Контроллер диска сам переводит этот LBA в физический адрес (цилиндр, головка, сектор) на поверхности диска.
        
    - В случае с SSD LBA просто указывает на определенный адрес в флеш-памяти.
        

> **🔥 Фишка Специалиста: TAA.** LBA — это основа для **абстракции** (помните, мы говорили о ней?) на самом низком уровне. Диск "притворяется" одним непрерывным массивом секторов, хотя физически это могут быть вращающиеся пластины или сложные массивы флеш-ячеек.
> 
> Раньше, чтобы прочитать данные с жесткого диска, системе нужно было знать: _«На каком блине (диске) лежат данные? На какой дорожке? В каком секторе на этой дорожке?»_. Это называлось **CHS** (Cylinder-Head-Sector). Представьте, если бы почтальону вместо адреса «ул. Пушкина, 5» давали инструкцию: «Пройди 500 шагов на север, поверни на 30 градусов, поднимись по лестнице на 3 метра». Неудобно!

**Как работает LBA:** Это стандарт, который говорит: «Забудьте про блины и головки. Весь диск — это просто длинная-длинная очередь из пронумерованных ячеек от 0 до миллиарда».

- **Для ОС:** Диск — это плоская ровная лента секторов.
    
- **Для Железа:** Контроллер внутри диска сам знает, что LBA №500 на самом деле лежит на 3-м блине SSD во флеш-ячейке №999.

### 3. Блок (Block)

- **Определение:** **Блок** — это **логическая единица адресации** на уровне **Файловой Системы (ФС)**. Это не то же самое, что сектор!
    
- **Состав:** Блок ФС **всегда** является последовательной группой из одного или нескольких **секторов**.
    
    - Например, если ваш сектор 4КБ, а блок ФС 8КБ, то один блок состоит из двух секторов.
        
- **Роль:** ФС управляет пространством, используя блоки. Адресация в структурах ФС (например, в **inode**) ведется по **номеру блока**, а не по LBA.
    
- **Размеры:** Как вы верно указали, размеры блоков ФС обычно кратны размеру сектора: 1024, 2048, 4096, 8192, 16384 байта и т.д.
    

> **Важно! Согласование размеров:**
> 
> - Если размер вашего блока ФС (например, 4096 байт) **совпадает** с размером физического сектора диска (4096 байт), вы получаете **оптимальную производительность**. Каждая логическая операция ФС соответствует одной физической операции диска.
>     
> - Если размер блока ФС **меньше** размера физического сектора (например, блок 512 байт на 4КБ-секторе), контроллеру диска придется читать весь 4КБ-сектор, даже если ФС запросила только 512 байт, что приводит к неэффективному чтению-модификации-записи. Это называется **проблемой выравнивания** (Alignment Problem).
>     

---

## 📝 Сводная Таблица: Сектор vs. Блок

| **Характеристика**  | **Сектор (Sector)**                               | **Блок (Block)**                                               |
| ------------------- | ------------------------------------------------- | -------------------------------------------------------------- |
| **Уровень**         | Физическое устройство/Контроллер                  | Файловая Система (ФС)                                          |
| **Адресация**       | **LBA** (Logical Block Address)                   | **Номер Блока** (Block Number)                                 |
| **Роль**            | Минимальная **физическая** единица чтения/записи. | Минимальная **логическая** единица выделения места для файлов. |
| **Типичный размер** | 512 байт или 4096 байт                            | 4КБ, 8КБ, 16КБ (обычно кратно размеру сектора)                 |
$$\text{Приложение} \rightarrow \text{ФС (Логический Блок)} \rightarrow \text{ОС (LBA)} \rightarrow \text{Контроллер Диска (Сектор)}$$

---
## Интересные факты, чтоб было


## Мост между Абстракцией и Физикой

### 1. Связь: Абстракция $\leftrightarrow$ Блок

**Ранее (Слайд 1):** Мы говорили, что Файловая Система (ФС) обеспечивает **уровень абстракции**, представляя файл как **поток байтов** и оперируя **метаданными**.

**Сейчас (Слайд 2):** Мы узнали, что ФС использует **Блоки** как минимальную единицу выделения места.

- **Как связано?** Блок ФС — это **реализация** этой абстракции.
    
    - ФС использует метаданные (**inode** или **MFT**) для хранения **номеров Блоков**, которые составляют файл.
        
    - Когда вы говорите: "Прочитай 1000 байтов из середины файла", ФС:
        
        1. Вычисляет, в **каком Блоке ФС** находятся эти байты.
            
        2. Оперирует только этими **Блоками**, игнорируя остальную часть диска.
            
- **Вывод:** ФС использует **Блок** как свой внутренний инструмент для организации данных, чтобы обеспечить логическую абстракцию "потока байтов" для приложений.
    

### 2. Связь: ФС (Блок) $\leftrightarrow$ ОС (LBA/Сектор)

Это ключевое звено — **перевод адресов**.

**Ранее (Слайд 1):** Мы знаем, что ФС должна преобразовывать логические запросы в физические операции на блочном устройстве.

**Сейчас (Слайд 2):** Мы знаем, что блочное устройство (диск) понимает только адреса **LBA** (номера Секторов).

- **Как связано?** ФС должна иметь внутренний механизм для перевода **Номера Блока ФС** в один или несколько **LBA**.
    
    - **Пример:**
        
        - ФС работает с блоками по **4 КБ**.
            
        - Диск использует секторы по **512 байт**.
            
        - Один **Блок ФС** (4 КБ) соответствует **8 Секторам** (8 x 512 байт).
            
    - Когда ФС решает, что файл нуждается в **Блоке №100**, она должна найти 8 свободных, последовательных секторов (например, с LBA №800 до LBA №807) и записать эту информацию в свои структуры.
        
- **Вывод:** **Блок** — это логический мост, который ФС строит из физических **Секторов**, используя их **LBA** для обращения к диску. ОС, используя LBA, прячет от ФС сложность геометрии диска.
    

### 3. Связь: Стандартизация $\leftrightarrow$ Размер Единиц

**Ранее (Слайд 1):** Мы говорили о **стандартизации** и **эффективности**.

**Сейчас (Слайд 2):** Мы обсуждали **размеры** Секторов и Блоков и проблему **выравнивания**.

- **Как связано?** Стандартизация размера единиц критически важна для эффективности и предотвращения проблем.
    
    - **Неэффективность:** Если ФС использует блок размером 512 байт, а диск — сектор 4096 байт, то для чтения одного блока ФС диск вынужден прочитать весь 4КБ сектор (8x больше, чем нужно) и вернуть его ФС. Это **замедляет** I/O операции.
        
    - **Оптимизация:** Современные ОС и ФС (такие как NTFS и ext4) при форматировании диска стараются установить размер **Блока ФС** равным или кратным размеру физического **Сектора** (обычно 4 КБ), чтобы обеспечить **согласованность** и **оптимальную производительность**.


```
[ЖЕЛЕЗО]      Сектора (LBA)
[Диск /dev]   Блочное устройство
[ФАЙЛОВАЯ СИСТЕМА]  Блоки (block)
[VFS]         Общие операции: open/read/write/stat
[ПРОГРАММЫ]   fopen(), cat, cp, браузер
```

### Уровни работы с диском:

## Уровни работы с диском (The Storage Stack)

Когда вы сохраняете файл, запрос проходит через «башню» уровней. Каждый уровень делает свою работу и передает результат вниз.

| **Уровень**                 | **Кто за это отвечает**    | **Что он видит/делает**                                       |
| --------------------------- | -------------------------- | ------------------------------------------------------------- |
| **1. Пользовательский**     | Программа (браузер, плеер) | Видит **Имя файла** и данные.                                 |
| **2. Виртуальная ФС (VFS)** | Ядро ОС (Диспетчер)        | Решает, какому драйверу отдать запрос.                        |
| **3. Конкретная ФС**        | Драйвер (NTFS, ext4)       | Превращает файл в **Блоки** и следит за правами.              |
| **4. Блочный уровень**      | Ядро ОС (Очереди I/O)      | Оптимизирует порядок записи, чтобы диск не «тормозил».        |
| **5. Драйвер устройства**   | Драйвер (NVMe, SATA)       | Переводит логические номера блоков в команды для контроллера. |
| **6. Железо**               | Контроллер диска           | Выполняет физическую запись в ячейки памяти или на блины.     |
## Из чего состоят уровни работы с диском?

Если мы заглянем внутрь каждого слоя, мы увидим там конкретные структуры данных:

1. **Пользовательский уровень (Application):** Здесь живут **Дескрипторы файлов (File Descriptors)**. Это просто числа (0, 1, 2...), через которые программа говорит: «Пиши в файл №3».
    
2. **Виртуальная ФС (VFS):** Это «мозг» в оперативной памяти. Состоит из объектов:
    
    - `inode` (информация о файле);
        
    - `dentry` (связка «имя файла — номер inode»);
        
    - `file` (состояние открытого файла, например, на какой строчке мы сейчас читаем).
        
3. **Конкретная ФС (ext4, NTFS):** Здесь те самые структуры с вашего слайда: **Суперблок**, **Таблицы Inodes**, **Битовые карты** и **Журнал** (список последних действий).
    
4. **Блочный уровень (Block Layer):** Здесь живут **Очереди запросов (I/O Queues)**. Если 10 программ одновременно хотят писать на диск, этот уровень выстраивает их в очередь и сортирует так, чтобы головка диска (на HDD) меньше дергалась.
    
5. **Драйвер устройства:** Это программный код, который знает команды конкретного «железа» (например, протоколы **NVMe** или **SATA**).
    
6. **Физика:** В SSD — это **ячейки памяти (NAND)** и контроллер, в HDD — **магнитные дорожки** и секторы


### ЧТО ТАКОЕ ФИЗИЧЕСКИЙ СЕКТОР?

Это **реальный, аппаратный минимальный блок**, который контроллер диска может **записать за один раз**.
Сегодня почти все HDD/SSD используют: `ФИЗИЧЕСКИЙ СЕКТОР = 4096 байт = 4 KiB`

###  ЧТО ТАКОЕ ЛОГИЧЕСКИЙ СЕКТОР?

Это **то, что диск ПРЕДСТАВЛЯЕТ системе** (Windows/Linux) как минимальный блок, который можно читать/писать.

Большинство дисков говорят ОС: `ЛОГИЧЕСКИЕ СЕКТОРА = 512 байт`
Хотя физически там 4096.

Представь:

- Физический сектор диска — это **коробка на 4096 ячеек**
    
    `[1][2][3]...[4096]  ← физический сектор`
    
- ОС хочет записать только **512 байт** в середину этого сектора.
    
`[ ... 256 ячеек ... | 512 ячеек новые данные | ... 3328 ячеек ... ]`

#### Что делает диск (read-modify-write):

Чтобы сохранить уже существующие данные и записать новые 512 байт:

1. **Прочитать весь сектор 4096 байт** (read)
    - Достает старые данные, чтобы не потерять остальные 3584 байта
        
2. **Внести изменения** в нужные 512 байт (modify)
    - Вставляет новые данные в середину сектора
        
3. **Записать весь сектор обратно 4096 байт** (write)
    - Сохраняет сектор целиком

### Проблема:

- **Медленно** — нужно три операции вместо одной
- **Изнашивает SSD** — пишется больше, чем нужно
- **Может снизить производительность** на многопоточном доступе
- **Фрагментация** — данные могут оказаться разбросаны

### Решение:

- **Выравнивание блоков ФС и разделов**
    
    - GPT начинает раздел с **LBA 2048 → 1 MiB**
    - Файловая система использует **блоки 4K**, идеально кратные физическому сектору
        
- **Тогда каждая запись ФС = целый физический сектор**
- **RMW не происходит**, всё быстро и эффективно

### А. Слой эмуляции (512e)

Чтобы старые ОС (например, Windows XP) вообще увидели новые диски, контроллер диска «притворяется», что он состоит из 512-байтовых секторов. Это та самая надпись в твоем логе: `Sector size (logical/physical): 512 bytes / 4096 bytes`.

### Б. Выравнивание разделов (Partition Alignment) — Самое важное!

Чтобы избежать Read-Modify-Write, нужно, чтобы первый сектор раздела начинался ровно в начале физического сектора 4 КБ.

- **Раньше:** Разделы начинались с 63-го сектора (это была традиция). Но 63 не делится на 8 (4096 / 512 = 8). Поэтому всё было «криво».
    
- **Сейчас:** Все современные ОС (Windows 10/11, Linux) начинают первый раздел с **2048-го сектора** (ровно 1 МБ от начала диска). 2048 идеально делится на 8. Это гарантирует, что все блоки ФС будут «попадать» в физические сектора диска.

Вот наши три героя:

1. **Физический сектор (4 КБ):** Реальный «кирпич» на диске. Диск не умеет брать меньше одного кирпича.
    
2. **Логический сектор (512 Б):** Это «язык общения». Диск говорит системе: «Я состою из кубиков по 512 байт», чтобы старая Windows не испугалась.
    
3. **Блок ФС (например, 4 КБ):** Это «коробка», в которой файловая система возит данные. Она состоит из нескольких логических секторов.
    

---

### 1. Как диск работает на 4 КБ, а кажется 512 Б? (Магия 512e)

Внутри диска стоит специальный микрочип — **контроллер**. Он работает как переводчик.

- **ОС просит:** «Дай мне логический сектор №3» (это всего 512 байт).
    
- **Контроллер диска понимает:** «Ага, логический сектор №3 находится внутри моего огромного физического кирпича №0».
    
- **Что делает диск:** Он вытаскивает **целый кирпич (4 КБ)**, отрезает от него нужный кусочек (512 Б) и отдает системе.
    

**Проблема записи:** Если ОС хочет изменить только эти 512 байт, диску приходится: прочитать весь кирпич 4 КБ $\rightarrow$ заменить в памяти кусочек $\rightarrow$ записать весь кирпич обратно. Это и есть тормоза.

---

### 2. Зачем нам настраивать Блок ФС, если логический сектор всё равно 512 Б?

Вот здесь кроется главная разгадка!

Файловая система (ФС) не обязана работать кусками по 512 байт. Она может сказать: «Я буду объединять эти мелкие логические кубики в большие паллеты (Блоки)».

Смотри, как решается проблема:

Если ты задаешь Блок ФС = 4 КБ, то для ФС один её блок — это ровно 8 логических секторов по 512 байт ($8 \times 512 = 4096$).

- **Если всё выровнено:** Когда ФС хочет записать один свой Блок (4 КБ), она отправляет запрос на запись 8 логических секторов. Контроллер диска видит эти 8 секторов и понимает: «О, они идеально заполняют ровно **один мой физический кирпич**!».
    
- **Результат:** Диск просто берет эти 4 КБ и записывает их за один раз. Никаких «прочитай-замени-запиши»!
    

**Почему размер Блока важен:** Если ты сделаешь Блок ФС = 512 байт, то ФС будет постоянно просить записывать данные «чайными ложками». Диск на каждую такую ложку будет вынужден ворочать огромный 4 КБ кирпич. Это убьет производительность.

---

### 3. Почему нельзя сделать всё одинаковым (везде 4 КБ)?

На самом деле — **можно и нужно**, и мир к этому идет! Это называется **4K Native (4Kn)**.

**Но есть две преграды:**

1. **Старое железо и софт:** Если ты возьмешь диск, у которого и логика 4 КБ, и физика 4 КБ, и вставишь его в старый компьютер, тот просто не загрузится. Он «ждет» 512 байт на входе, а получает 4 КБ — для него это как если бы в розетку 220В подали 1500В.
    
2. **Зачем делать 512 везде?** Мы уже обсуждали: физически делать сектора по 512 байт на современных дисках — значит терять 10% полезного места на «заборы» между секторами. Это невыгодно.
    

### Итоговая схема в твоей голове:

- **Диск (Физика):** «Я большой и мощный, я работаю только по 4 КБ».
    
- **Контроллер (512e):** «Я вру системе, что я маленький (512 Б), чтобы она не сломалась».
    
- **Ты (Специалист):** «Я знаю, что контроллер врет, поэтому я настрою **Блок ФС на 4 КБ** и **выровняю разделы**, чтобы мои данные идеально ложились в физические кирпичи диска, минуя тормоза эмуляции».
    

---

### Как это решается на практике?

Когда ты создаешь раздел в современном Linux или Windows, система автоматически начинает его с 2048-го логического сектора.

$2048 \times 512 = 1\,048\,576$ байт (ровно 1 МБ).

Этот 1 МБ идеально делится на 4 КБ физических секторов. В итоге все твои Блоки ФС по 4 КБ «попадают» точно в физические сектора.

## Выравнивание Блока ФС по Физическому Сектору (I/O Alignment)

Эта фишка касается того, как Файловая Система (ФС) должна "уважать" физические ограничения диска, чтобы избежать неэффективных операций ввода-вывода.

### 1. Исходная Проблема: 512e Эмуляция

Как мы видели в выводе `fdisk -l`, современные диски часто используют:

- **Физический Сектор:** 4096 байт (4 КБ). (Это реальная, минимальная единица записи).
    
- **Логический Сектор (LBA Unit):** 512 байт. (Это то, что диск сообщает старым ОС для совместимости).
    

Если ФС создается без учета физического размера, возникает проблема **невыровненности**.

### 2. Что такое Невыровненность (Misalignment)?

Представьте, что Файловая Система была отформатирована так, что ее **Блок ФС** (например, 4 КБ) начинается не с начала физического 4 КБ сектора, а где-то в середине.

- **Сценарий 1: Плохое Выравнивание**
    
    - **ФС:** Хочет записать свой 4 КБ **Блок 1**.
        
    - **Блок 1 ФС** начинается с 512-го байта физического **Сектора A** и заканчивается 512-м байтом физического **Сектора B**. (То есть, он перекрывает два физических сектора).
        
    - **Диск (Физика):** Чтобы записать 4 КБ ФС-блока, контроллер диска вынужден выполнить операцию **Чтение-Модификация-Запись (Read-Modify-Write)**:
        
        1. **Read A:** Прочитать весь Физический Сектор A (4 КБ).
            
        2. **Modify A:** Изменить только часть данных Сектора A в буфере.
            
        3. **Write A:** Записать весь Сектор A обратно.
            
        4. **Read B:** Прочитать весь Физический Сектор B (4 КБ).
            
        5. **Modify B:** Изменить только часть данных Сектора B в буфере.
            
        6. **Write B:** Записать весь Сектор B обратно.
            

### 3. Решение: Идеальное Выравнивание (Optimal I/O Size)

- **Цель:** Сделать так, чтобы каждый **Блок ФС** полностью находился внутри **одного** или нескольких **целых Физических Секторов (4096 B)**.
    
- **Как это работает:**
    
    - **ФС:** Хочет записать свой 4 КБ **Блок 1**.
        
    - **Блок 1 ФС** начинается ровно с начала Физического **Сектора A** и заканчивается ровно в конце Сектора A.
        
    - **Диск (Физика):** Контроллеру нужно выполнить только одну операцию:
        
        1. **Write A:** Записать 4 КБ данных ФС-блока в один 4 КБ Физический Сектор A.
            

**Сравнение:**

|**Операция**|**Невыровненное I/O**|**Выровненное I/O**|
|---|---|---|
|**Количество физических операций**|6 (2 Read + 2 Modify + 2 Write)|1 (1 Write)|
|**Производительность**|Очень низкая (тратится время на чтение данных, которые не нужно менять)|Максимально высокая|

### 4. Практическое Применение (`mkfs`)

Когда вы запускаете команду `mkfs.ext4` (или любую другую утилиту форматирования), вы можете указать размер блока.

> **Связь с `fdisk -l`:** Ваш вывод показал, что **Optimal I/O size** для дисков `/dev/sdc` и `/dev/sdd` составляет **4096 bytes**.

- **Что делать:** При форматировании нужно выбрать размер **Блока ФС** (например, 4 КБ, 8 КБ или 16 КБ).
    
    - **4 КБ (4096 B):** Идеально, каждый ФС-блок попадает ровно в один физический сектор.
        
    - **8 КБ (8192 B):** Тоже хорошо, каждый ФС-блок попадает ровно в два последовательных физических сектора (2 x 4 КБ).
        
    - **Если выбрать 512 B:** Будет **катастрофически медленно**, так как для записи 8 логических блоков ФС потребуется 8 операций Read-Modify-Write, вместо одной прямой записи.



Команда:

`sudo fdisk -l`

делает **просмотр всех блочных устройств**, которые видит Linux, и выводит:

```
kriasatri@kriasa2006:~$ sudo fdisk -l
[sudo] password for kriasatri:
Disk /dev/ram0: 4 MiB, 4194304 bytes, 8192 sectors
Units: sectors of 1 * 512 = 512 bytes
Sector size (logical/physical): 512 bytes / 4096 bytes
I/O size (minimum/optimal): 4096 bytes / 4096 bytes

Disk /dev/sdb: 186.04 MiB, 195080192 bytes, 381016 sectors
Disk model: Virtual Disk
Units: sectors of 1 * 512 = 512 bytes
Sector size (logical/physical): 512 bytes / 512 bytes
I/O size (minimum/optimal): 512 bytes / 512 bytes


Disk /dev/sdc: 2 GiB, 2147487744 bytes, 4194312 sectors
Disk model: Virtual Disk
Units: sectors of 1 * 512 = 512 bytes
Sector size (logical/physical): 512 bytes / 4096 bytes
I/O size (minimum/optimal): 4096 bytes / 4096 bytes

```
### 1. Блочные устройства RAM-дисков (`/dev/ram0` - `/dev/ram15`)

Эти диски созданы в оперативной памяти (RAM) для временного использования.

|**Параметр**|**Значение**|**Интерпретация в контексте ФС**|
|---|---|---|
|**Disk**|`/dev/ram0: 4 MiB, 4194304 bytes, 8192 sectors`|Общий размер 4 МБ. Общее количество секторов: 8192.|
|**Units: sectors of 1 * 512 = 512 bytes**||ОС по умолчанию использует 512-байтные секторы как **единицы измерения LBA**.|
|**Sector size (logical/physical): 512 bytes / 4096 bytes**|**Логический: 512 B** / **Физический: 4096 B**|**Критически важная информация!** Это тот самый случай **512e эмуляции** (Advanced Format), о котором мы говорили.|
|||➡️ **Логический размер (512 B):** Размер, который ОС **ожидает** или **использует** для адресации (LBA).|
|||➡️ **Физический размер (4096 B):** Реальный, **физический размер сектора** на устройстве.|
|**I/O size (minimum/optimal): 4096 bytes / 4096 bytes**||Это **идеальный размер блока** для Файловой Системы!|
|||➡️ **Оптимальный (4096 B):** Ядро ОС советует ФС использовать блоки размером **4096 байт (4 КБ)**, чтобы они совпадали с физическим сектором (4096 B). Если ФС использует блок 512 B, она будет неэффективна, так как диск всё равно будет читать/писать 4096 B.|

### 2. Устройства с Совпадающими Размерами (`/dev/sda`, `/dev/sdb`)

Эти виртуальные диски показывают идеальный сценарий или старый формат:

|**Параметр**|**/dev/sda и /dev/sdb**|**Интерпретация**|
|---|---|---|
|**Sector size (logical/physical): 512 bytes / 512 bytes**||Здесь **логический** и **физический** размер сектора **совпадают** (512 байт).|
|**I/O size (minimum/optimal): 512 bytes / 512 bytes**||ОС советует использовать блок ФС размером 512 байт для оптимальной работы.|
|**Вывод:**|Это либо очень старый формат диска, либо эмуляция, где нет необходимости в выравнивании по 4КБ.||

### 3. Современное Устройство (Проблема Выравнивания) (`/dev/sdc`, `/dev/sdd`)

Эти диски (особенно `/dev/sdc` и `/dev/sdd`, которые, скорее всего, представляют реальные SSD/HDD) демонстрируют ситуацию, характерную для современных накопителей:

| **Параметр**                                               | **/dev/sdc и /dev/sdd**                        | **Интерпретация**                             |
| ---------------------------------------------------------- | ---------------------------------------------- | --------------------------------------------- |
| **Sector size (logical/physical): 512 bytes / 4096 bytes** | **Логический: 512 B** / **Физический: 4096 B** | Снова 512e эмуляция!                          |
| **I/O size (minimum/optimal): 4096 bytes / 4096 bytes**    |                                                | Оптимальный I/O (ввод/вывод) — **4096 байт**. |
## Связь с Нашими Лекциями

Этот вывод — идеальная иллюстрация того, что мы обсуждали:

1. **Связь Сектор/LBA:** Вывод использует **Секторы** (512 B) как единицу для **LBA** (`Units: sectors of 1 * 512 = 512 bytes`). ОС будет обращаться к диску, используя LBA №1, LBA №2 и так далее, каждый LBA указывает на 512 байт.
    
2. **Проблема Абстракции и Выравнивания:**
    
    - ОС **логически** думает, что она работает с 512-байтовыми кусками.
        
    - Но **физически** диск может читать/писать только 4096-байтовые куски.
        
    - **Конфликт:** Если Файловая Система (которая работает с **Блоками**) не будет использовать размер блока, кратный **4096 байт** (оптимальный размер I/O), то при каждом обращении к диску произойдет неэффективная операция чтения-модификации-записи (Read-Modify-Write) всего 4КБ сектора, даже если ФС нужно изменить только 512 байт.
### RAM-диски- Фейковые диски в оперативной памяти (быстрые, маленькие).

Они существуют в Linux для:
- тестов
- временного хранения
- учебных целей
- ранних стадий загрузки системы

Ты видишь: `Disk /dev/ram0: 4 MiB, 4194304 bytes, 8192 sectors`

|Параметр|Значение|
|---|---|
|Размер|4 MiB|
|Секторов|8192|
|Размер сектора|512 байт логический, 4096 байт физический|
`8192 секторов * 512 логических байт = 4 194 304 = 4 MiB`

### Virtual Disk-Диски, созданные виртуальной машиной → это файлы-образы.

Устройств `/dev/sda`, `/dev/sdb`, `/dev/sdc`, `/dev/sdd`:
`Disk model: Virtual Disk`
Это значит, что ты используешь: виртуальную машину (VirtualBox, VMware, QEMU, Docker, WSL — что-то такое)То есть это **файлы-образы**, которые виртуальная машина показывает Linux как настоящие физические диски.

Sector size - 512 логический — ОС так проще, 4096 физический — железо так работает

### I/O size - Как оптимально писать, чтобы диск не страдал.

Пример `I/O size (minimum/optimal): 4096 bytes / 4096 bytes`
Это то, какими _минимальными блоками_ это устройство оптимально читает/пишет.

- minimum — минимально возможный блок запроса
- optimal — рекомендуемый, чтобы не было тормозов
 - если ФС пишет **меньше чем optimal**, диск делает read-modify-write → тормоза.
![[Pasted image 20260104223547.png]]

### 1. `Get-Disk`: Физический уровень

Здесь Windows видит твое «железо».

- **Friendly Name:** `Micron_240...` — это твой физический NVMe SSD диск.
    
- **Total Size:** 476.94 GB.
    
- **Partition Style: GPT** — современный стандарт таблицы разделов (вместо старого MBR). Он позволяет иметь много разделов и поддерживает диски больше 2 ТБ.
    

---

### 2. `Get-Partition`: Уровень «Заборов» (Разделов)

Вот здесь начинается магия и подтверждение наших теорий!

- **Посмотри на Partition 1 и её Offset:** `Offset: 1048576` Помнишь наше число **1048576**? Это ровно **1 МБ**! **Это прямое доказательство:** Твой первый раздел начинается не с нуля, а с отступа в 1 МБ (2048 секторов), чтобы всё было **идеально выровнено** по физическим секторам 4 КБ. Windows сама это сделала при установке.
    
- **Разнообразие разделов:** У тебя на одном диске целых 7 разделов!
    
    - **System (100 MB):** Тот самый EFI-раздел, где лежит маленький загрузчик, который запускает компьютер.
        
    - **Reserved (16 MB):** Служебное место для GPT.
        
    - **Basic (C и F):** Твои основные рабочие разделы (Windows и Данные).
        
    - **Recovery (767 MB):** Раздел для восстановления системы, если Windows сломается.
        

---

### 3. `Get-Volume`: Уровень Файловых Систем

Здесь мы видим, как эти разделы «обустроены» внутри.

- **FileSystemType:** Везде стоит **NTFS** (кроме EFI, там **FAT32**).
    
- **DriveLetter:** Здесь разделы получают свои буквы (`C`, `F`, `E`).
    
- **Интересный факт:** Обрати внимание, что у «системных» разделов (EFI и Recovery) **нет буквы**. Они есть на диске, у них есть файловая система, но Windows прячет их от тебя в «Проводнике», чтобы ты случайно не удалила там что-нибудь важное.
---
## Интересные факты

## 🧊 Виртуальные диски: Почему это файл, а не раздел?

Вы спросили: _«Почему при скачивании виртуальной машины (ВМ) ей не выделяется раздел, а она выглядит как файл?»_.

Это гениальное решение проблем гибкости. Виртуальный диск (например, форматы `.vmdk`, `.vhd`, `.iso`) — это **эмуляция блочного устройства внутри файла**.

### Как это работает:

1. **Host-система (ваша Windows):** Видит обычный файл на диске `C:`, скажем, `linux_disk.vhd`. Для нее это просто набор байтов.
    
2. **Guest-система (Виртуальная машина):** Программа-эмулятор (VirtualBox, VMware) «подсовывает» этот файл виртуальной машине и говорит: «Смотри, это твой жесткий диск».
    
3. **Результат:** Внутри этого файла виртуальная машина создает **свою таблицу разделов** и **свою файловую систему**.
    

### Почему это круто?

- **Мобильность:** Вы можете просто скопировать файл виртуальной машины на флешку и запустить на другом ПК. Если бы это был раздел, вам пришлось бы «резать» диск заново.
    
- **Снапшоты (Snapshots):** Вы можете мгновенно «заморозить» состояние файла.
    
- **🔥 Фишка Профи: Динамические (Sparse) файлы.** Если вы создали вирт. диск на 100 ГБ, он не займет 100 ГБ сразу. Файл будет расти только тогда, когда вы реально записываете туда данные. В таблице метаданных вашей основной ФС будет написано: «Место забронировано», но физические блоки будут выделяться по мере надобности.
    

---

## 🐧 Что такое WSL в этой схеме?

**WSL (Windows Subsystem for Linux)** — это технология, которая позволяет запускать Linux прямо внутри Windows.

### WSL 1 (Старый подход)

Это был «переводчик». Он не создавал виртуальных дисков. Когда Linux-программа просила создать файл, WSL «на лету» переводил это в запрос к Windows и создавал файл в обычной папке NTFS. Это было медленно и не всегда совместимо.

### WSL 2 (Современный подход)

Это уже **настоящая легкая виртуальная машина**.

1. Для WSL 2 создается специальный виртуальный диск — файл формата **`.vhdx`** (обычно он лежит глубоко в папках `AppData`).
    
2. Внутри этого файла создается полноценная файловая система **ext4**.
    
3. **Почему это важно:** Windows «не понимает» ext4 напрямую. Поэтому она создает этот виртуальный «контейнер» (файл-диск), внутри которого Linux чувствует себя как дома и работает с максимальной скоростью.
    

> **☝️ Фишка Специалиста:** Именно поэтому, если вы зайдете в папку WSL из Windows, вы увидите путь вроде `\\wsl$\Ubuntu\home\user`. Это не просто папка, это Windows обращается к сетевому протоколу, чтобы «заглянуть» внутрь виртуального диска, пока Linux-машина запущена.

---

## Резюме лектора

Всё в компьютере — это уровни абстракции.

- **Файл** может притворяться **Диском** (виртуальный диск).
    
- **Диск** может быть нарезан на **Разделы**.
    
- **Разделы** управляются **Файловыми системами**.
    

Мы используем файлы для виртуальных машин, потому что ими **легче управлять** как обычными данными, сохраняя при этом возможность структурировать их внутри как настоящие железные диски.

## Про виртуальную машину: «Откуда дровишки?»

Когда вы создаете ВМ и она просит 100 ГБ, есть два варианта того, как это место «отрезается» от основной ОС. Это зависит от типа диска, который вы выбрали в настройках (VirtualBox или VMware):

### Вариант А: Фиксированный размер (Pre-allocated)

- **Что происходит:** Основная ОС (Windows) сразу создает на диске файл размером ровно 100 ГБ.
    
- **Занимает ли место?** Да, **сразу все 100 ГБ**. Для вашей основной ОС это место становится занятым «черным ящиком». Вы не можете положить туда свои фото, пока не удалите виртуальную машину.
    
- **Плюс:** Это работает быстрее, так как место уже выделено и оно не «размазано» по всему физическому диску.
    

### Вариант Б: Динамический размер (Thin Provisioning / Sparse) — _Самый частый_

- **Что происходит:** Вы говорите ВМ: «Твой лимит 100 ГБ». Но на основной ОС создается крошечный файл (пару мегабайт).
    
- **Откуда берется место?** По мере того как вы устанавливаете программы _внутри_ виртуальной машины, файл на основной ОС начинает «раздуваться».
    
- **Можно ли занимать место на основной ОС?** Да! Если вы записали в ВМ только 10 ГБ данных, остальные 90 ГБ всё еще доступны вашей Windows.
    
- **Минус:** Если ваша основная ОС забьет диск под завязку, виртуальная машина внезапно «умрет», потому что ей некуда будет расширяться, хотя она «думала», что у неё в запасе еще 90 ГБ. Это называется **Overprovisioning** (перепродажа ресурсов).


![[Pasted image 20251206133718.png]]

## Структура Раздела на Примере ext2/ext4

Вся Файловая Система ext2/ext4 построена на принципе **Групп Блоков (Block Groups)**.

### 1. Уровень Раздела (Partition Level)

Раздел диска (Partition) — это область, которую ОС выделяет для одной файловой системы.

- **Boot Sector (Загрузочный Сектор):** Это самый первый сектор раздела. Это самые первые **1024 байта** раздела. Он содержит код, который запускает процесс загрузки ОС, если этот раздел является загрузочным. Для ФС он имеет служебное значение, но не содержит данных о самой структуре файлов.
    
- **Block Group 0, Block Group N...:** Это ключевая идея! Файловая система разбивается на несколько одинаковых по структуре, но разных по содержимому **Групп Блоков**.
    
    - **Принцип Групп (Фишка Специалиста):** Деление на группы используется для **борьбы с фрагментацией** и **ускорения доступа**. Идея в том, чтобы хранить метаданные (Inode Table) и связанные с ними данные (Data Blocks) **физически близко** друг к другу внутри одной группы. Это минимизирует перемещение головки диска (на HDD) или улучшает локальность доступа (на SSD).

---
    **Зачем это нужно? (Главная причина — СКОРОСТЬ):** Вспомни HDD: чтобы прочитать файл, головка должна физически перелететь к таблице (где написано, где лежит файл), а потом — к самим данным. Если таблица в начале диска, а данные в конце — головка будет летать туда-сюда (**Seek Time**).

- **Решение:** Мы делим диск на группы. В каждой группе есть своя мини-таблица (Inodes) и свои блоки данных. ФС старается записывать файл и его «паспорт» (Inode) **в одну и ту же группу**. Головке диска почти не нужно двигаться. Для SSD это тоже полезно (локальность данных улучшает кэширование)
---

- **Unused Sectors (Неиспользуемые Секторы):** Оставшееся пространство, если раздел не кратен размеру группы блоков.
    

### 2. Уровень Группы Блоков (Block Group Structure)

Каждая **Группа Блоков** (например, Block Group 0) имеет строго определенную внутреннюю структуру, которую вы видите внизу.

|**Элемент**|**Название в ext2**|**Роль и Назначение**|
|---|---|---|
|**Super Block (Суперблок)**|`Super Block`|**Главный центр управления ФС.** Содержит общую информацию о всей файловой системе: ее тип, размер (сколько всего блоков/групп), размер блока, количество свободных i-узлов и блоков, дата последнего монтирования, а также флаги состояния (например, "чистая" или "грязная" после сбоя).|
|**Group Descriptors (Дескрипторы Групп)**|`Group Descriptors`|Это, по сути, **таблица групп**. Хранит информацию о том, **где** внутри данной группы расположены остальные структуры (Inode Bitmap, Block Bitmap, Inode Table).|
|**Block Bitmap (Битовая Карта Блоков)**|`Block Bitmap`|**Карта свободного/занятого пространства данных.** Это битовый массив, где каждый бит соответствует одному **Блоку Данных** в этой группе. Если бит = 1, блок занят; если 0, блок свободен. ФС использует эту карту, чтобы быстро найти свободное место для записи нового файла.|
|**Inode Bitmap (Битовая Карта I-узлов)**|`Inode Bitmap`|**Карта свободных/занятых метаданных.** Аналогично Block Bitmap, но отслеживает, какой **i-узел** (Inode) в **Inode Table** свободен, а какой используется.|
|**Inode Table (Таблица I-узлов)**|`Inode Table`|**Метаданные файлов.** Содержит сами **i-узлы**. Каждый i-узел — это запись, хранящая все метаданные одного файла (размер, владелец, права доступа, время создания) и, самое главное, **указатели на его Data Blocks** (номера блоков, где физически лежат данные файла).|
|**Data Blocks (Блоки Данных)**|`Data Blocks`|**Место хранения содержимого файлов.** Это самая большая часть группы. Именно здесь хранятся сами данные файлов.|

### 3. Как это работает (Краткий Сценарий Записи)

Когда вы создаете новый файл, ФС выполняет следующие шаги, используя эти структуры:

1. **Проверка:** ФС читает **Суперблок**, чтобы убедиться, что система в порядке.
    
2. **Поиск i-узла:** ФС находит свободный бит в **Inode Bitmap**, берет соответствующий свободный **i-узел** из **Inode Table** и записывает в него метаданные нового файла (имя, дата, права).
    
3. **Поиск Блоков Данных:** ФС смотрит в **Block Bitmap**, находит несколько свободных бит (столько, сколько нужно для файла) и помечает их как занятые.
    
4. **Запись:** ФС записывает содержимое файла в соответствующие **Data Blocks** и записывает номера этих блоков в **i-узел** этого файла.
    

> **🔥 Фишка Специалиста: Резервирование Суперблока.** Суперблок критически важен. Если он повредится, вся ФС станет нечитаемой. Поэтому в ext2/ext4 **копии Суперблока** хранятся в начале **каждой** группы блоков (или, по крайней мере, в первых нескольких). Если основной Суперблок (в Block Group 0) поврежден, ОС может использовать одну из резервных копий для восстановления ФС.

![[Pasted image 20251206133724.png]]

### 1. Что именно хранит Суперблок? (Разбор параметров)

Представь, что ты — драйвер файловой системы. Тебе дали диск и сказали: «Работай!». Без суперблока ты не знаешь даже, с какого конца к нему подойти.

- **`s_inodes_count` / `s_blocks_count`:** Общее количество «паспортов» и «кирпичей». ОС должна знать пределы своей империи. Если ты захочешь создать миллион маленьких файлов, а `s_inodes_count` всего 100 тысяч — система выдаст ошибку «нет места», хотя на диске еще полно свободных гигабайт.
    
- **`s_free_blocks_count` / `s_free_inodes_count`:** Это счетчики для быстрой справки. Чтобы не пересчитывать миллиард бит в Битмапах каждый раз, когда ты спрашиваешь «сколько осталось места», ОС просто смотрит в эти переменные в Суперблоке.
    
- **`s_block_size`:** Самый важный параметр! Как мы обсуждали, это размер нашего «кирпича» (1 КБ, 2 КБ или 4 КБ). Все расчеты адресов в системе зависят от этого числа.
    
- **`s_mtime` / `s_wtime`:** Время последнего монтирования и записи. Помогает системе понять, когда диск использовали в последний раз.
    
- **`s_state` (Чистая или с ошибками):** Это «флаг здоровья». Когда ты выключаешь компьютер правильно, ФС ставит там отметку «Clean». Если ты выдернула вилку из розетки, флаг остается «Dirty». При следующем включении Linux увидит это и запустит проверку (`fsck`), чтобы убедиться, что данные не побились.
    

---

### 2. Почему копий много и зачем они в нечетных группах?

Это вопрос **выживания**. Суперблок настолько критичен, что если в секторе, где он лежит, случится физический сбой (битый сектор), ты потеряешь **весь диск**. Вообще всё.

- **Избыточность:** Файловая система создает копии (Backup Superblocks) по всему разделу.
    
- **Хитрый алгоритм:** Раньше (в ext2) копии были в **каждой** группе блоков. Но если групп тысячи, это пустая трата места. В современных ext4 копии обычно хранятся в группах **0, 1** и группах, которые являются степенями 3, 5 и 7 (например, 3, 5, 7, 9, 25...). Это и есть те самые «нечетные группы» или разреженные копии (**Sparse Superblock**).
    

> **Фишка Специалиста:** Если у тебя «упал» диск и пишет "Bad magic number in super-block", ты можешь сказать системе: «Эй, не используй главный суперблок, он сломан. Возьми копию из группы №8193». И диск оживет!

```
kriasatri@kriasa2006:~$ sudo tune2fs -l /dev/sdd | grep -E "Block size|count|state"
[sudo] password for kriasatri:
Filesystem state:         clean
Inode count:              16777216
Block count:              67108864
Reserved block count:     3355443
Block size:               4096
Mount count:              130
Maximum mount count:      -1
```
### Порядок монтирования (Как ОС читает слайд)

Когда ты вставляешь флешку или запускаешь WSL, происходит следующее:

1. Драйвер идет в начало раздела (пропуская Boot Sector).
2. Читает **Суперблок**.
3. Если магическое число (Magic Number) совпадает (для ext4 это `0xEF53`), драйвер понимает: «О, это мой формат!».
4. Он берет из Суперблока `s_block_size` и адреса Таблиц Inode.
5. Только после этого диск становится доступен тебе как папка.

## Что такое Магическое Число (Magic Number)?

Это «секретное рукопожатие» между файловой системой и ядром ОС.

Представь, что тебе подсовывают флешку. Как компьютер узнает, что там именно `ext4`, а не `NTFS` или просто случайный мусор?

1. В каждом Суперблоке по строго определенному адресу (смещение 56 байт от начала суперблока) записано двухбайтовое число.
    
2. Для `ext2/3/4` это всегда **0xEF53**.
    

Когда ты вводишь команду `mount`, ядро заглядывает в начало раздела, находит суперблок и ищет там `0xEF53`.

- Если нашло — "О, привет, ext4! Я знаю, как с тобой работать".
    
- Если там написано `0x58465342` — "А, это XFS".
    
- Если там случайные цифры — "Ошибка: неизвестная файловая система".
![[Pasted image 20251206133729.png]]

### 1. Зачем нужны дескрипторы?

Помнишь, мы говорили, что файловая система `ext4` разбита на группы, чтобы данные лежали рядом с метаданными? Так вот, ОС нужно знать, где **внутри конкретной группы** лежат её инструменты (карты и таблицы).

Представь, что драйвер ФС заходит в Группу №5. Он знает, что он внутри группы, но он не знает: «А на какой странице тут список свободных мест?». Он заглядывает в **Дескриптор** этой группы и читает:

- **`bg_block_bitmap`**: «Список свободных блоков этой группы лежит в блоке №501».
    
- **`bg_inode_bitmap`**: «Список свободных паспортов (инодов) лежит в блоке №502».
    
- **`bg_inode_table`**: «Сама картотека (таблица инодов) начинается с блока №503».
    

**Почему это важно?** Без этого драйверу пришлось бы каждый раз вычислять адреса по сложным формулам. А так у него есть «оглавление» для каждого района.

---

### 2. Параметры «занятости»

Дескриптор также хранит оперативную статистику района:

- **`bg_free_blocks_count`** и **`bg_free_inodes_count`**: Когда ты создаешь файл, ОС сначала смотрит в дескрипторы групп: «Так, в Группе №1 свободно 0 блоков, пролетаем. О, в Группе №2 свободно 500 блоков! Иду туда». Это избавляет от необходимости сканировать всю битовую карту блоков.
    
- **`bg_used_dirs_count`**: Это очень интересная штука. ФС старается распределять папки (директории) равномерно по разным группам блоков. Если в Группе №1 уже много папок, новую папку ОС создаст в Группе №2, чтобы файлы из разных папок не перемешивались в одной куче.
    

---

### 3. Избыточность (Копии дескрипторов)

На слайде написано: _"Их копии находятся в каждой нечётной группе блоков"_. Это работает так же, как и с Суперблоком. Если физически повредится начало диска, где лежит основная таблица дескрипторов, мы потеряем доступ к данным во всех группах.

Поэтому ФС хранит **таблицу всех дескрипторов** (которая описывает все группы сразу) в нескольких местах. Если основная таблица «умрет», ОС возьмет копию из Группы №1, №3, №5 и так далее.

```
kriasatri@kriasa2006:~$ sudo dumpe2fs /dev/sdd | head -n 50
[sudo] password for kriasatri:
dumpe2fs 1.47.0 (5-Feb-2023)
Filesystem volume name:   <none>
Last mounted on:          /distro
Filesystem UUID:          3255683f-53a2-4fdf-91cf-b4c1041e2a62
Filesystem magic number:  0xEF53
Filesystem revision #:    1 (dynamic)
Filesystem features:      has_journal ext_attr resize_inode dir_index filetype needs_recovery extent 64bit flex_bg sparse_super large_file huge_file dir_nlink extra_isize metadata_csum
Filesystem flags:         signed_directory_hash
Default mount options:    user_xattr acl
Filesystem state:         clean
Errors behavior:          Continue
Filesystem OS type:       Linux
Inode count:              16777216
Block count:              67108864
Reserved block count:     3355443
Overhead clusters:        1330671
Free blocks:              64172394
Free inodes:              16645340
First block:              0
Block size:               4096
Fragment size:            4096
Group descriptor size:    64
Reserved GDT blocks:      1024
Blocks per group:         32768
Fragments per group:      32768
Inodes per group:         8192
Inode blocks per group:   512
Flex block group size:    4096
Filesystem created:       Wed Apr 10 19:35:05 2019
Last mount time:          Sun Jan  4 23:54:37 2026
Last write time:          Sun Jan  4 23:54:37 2026
Mount count:              130
Maximum mount count:      -1
Last checked:             Wed Apr 10 19:35:05 2019
Check interval:           0 (<none>)
Lifetime writes:          35 GB
Reserved blocks uid:      0 (user root)
Reserved blocks gid:      0 (group root)
First inode:              11
Inode size:               256
Required extra isize:     32
Desired extra isize:      32
Journal inode:            8
Default directory hash:   half_md4
Directory Hash Seed:      832ad346-60be-4f80-92f8-835728a807fe
Journal backup:           inode blocks
Checksum type:            crc32c
Checksum:                 0x600bdad4
Journal features:         journal_incompat_revoke journal_64bit journal_checksum_v3
Total journal size:       1024M
Total journal blocks:     262144
```
![[Pasted image 20251206133735.png]]

- **i-узел** = метаданные + номера блоков
- **Имя файла** = запись в каталоге, указывающая на i-узел
- **Разделение имени и i-узла позволяет:**
    - делать жёсткие ссылки
    - быстро переименовывать файлы
    - эффективно управлять метаданными
    - минимизировать дублирование данных

### 1. Великая тайна: Где имя файла? 🕵️‍♂️

Это самый контринтуитивный момент в Linux: **в самом файле (его Inode) нет его имени.**

- **Как это работает:** Представь, что Inode — это **паспорт** человека. В паспорте есть всё: рост, вес, прописка, номер. Но «имя» (как нас зовут в жизни) — это запись в «телефонной книге» (которой является **Каталог**).
    
- **Каталог (Directory)** — это специальный файл, внутри которого просто список пар: `Имя файла <---> Номер Inode`.
    
- **Почему это круто?** Это позволяет реализовать **Hard Links** (жесткие ссылки). Ты можешь иметь два разных имени (например, `foto_v_otpúske.jpg` и `super_photo.jpg`), которые указывают на **один и тот же номер Inode**. Файл на диске один, а имен у него — сколько угодно.
    

---

### 2. Три времени файла (atime, mtime, ctime)

В Windows мы привыкли к «Дате создания», но в Linux всё точнее:

- **atime (access time):** Когда ты последний раз открывала файл, даже если ничего не меняла (просто посмотрела фильм).
    
- **mtime (modification time):** Когда ты реально меняла **содержимое** файла (дописала текст).
    
- **ctime (change time):** Когда ты меняла **метаданные** (например, сменила владельца или права доступа).
    

> **💡 Фишка Специалиста:** Постоянное обновление `atime` (времени доступа) замедляет диск, потому что при каждом чтении файла диску приходится делать запись в Inode. Поэтому в твоем выводе `dumpe2fs` наверняка есть флаг `relatime` или `noatime` — это способ сказать системе: «Не записывай время доступа слишком часто, пощади мой SSD».

---

### 3. Типы файлов (Всё есть файл!)

На слайде написано: «обычный файл, каталог, устройство...». В Linux даже твоя **видеокарта**, **клавиатура** и **терминал** имеют свои Inode!

- Если тип файла — «устройство», то вместо указателей на блоки данных в Inode записаны «Major» и «Minor» номера драйвера.
    
- Это позволяет программам общаться с железом так же просто, как если бы они писали текст в обычный файл.
    

---

### 4. Размер: 128 vs 256 байт

Ты видела в своем выводе `dumpe2fs`: `Inode size: 256`. Зачем `ext4` нужно в два раза больше места, чем `ext2`?

1. **Наносекунды:** Чтобы хранить время не просто «в секундах», а сверхточно.
    
2. **Extents:** Те самые «диапазоны блоков», о которых мы говорили. Они требуют больше места в структуре Inode, чем старые простые номера блоков.
    
3. **Безопасность:** Дополнительные контрольные суммы (checksums), чтобы система сразу узнала, если Inode повредится.

![[Pasted image 20251206133741.png]]

Вот подробная расшифровка каждого поля этого `struct`:

### 1. `__le16 i_mode;` (Тип файла + права доступа)

- **Тип данных:** `__le16` — это 16-битное целое число (2 байта) в формате _Little Endian_ (младший байт идет первым).
    
- **За что отвечает:** Это «маска». Она объединяет в себе:
    
    - **Тип:** файл это, каталог, символическая ссылка или устройство.
        
    - **Права:** те самые `rwxrwxrwx` (права владельца, группы и остальных).
        
- **Пример:** Если там записано число в восьмеричном виде `0100644`, это значит, что перед нами обычный файл (`0100`) с правами `rw-r--r--` (`644`).
    

### 2. `__le16 i_uid;` (ID владельца)

- **Тип данных:** `__le16` (2 байта).
    
- **За что отвечает:** Хранит числовой идентификатор пользователя (**User ID**), которому принадлежит файл. Например, если владелец `root`, здесь будет `0`. По этому числу система понимает, пускать тебя в файл или нет.
    

### 3. `__le32 i_size;` (Размер файла)

- **Тип данных:** `__le32` (4 байта / 32 бита).
    
- **За что отвечает:** Хранит точный размер файла в **байтах**.
    
- **Нюанс:** Так как поле 32-битное, в классическом `ext2` оно ограничивало размер файла до 4 ГБ (позже это обошли, используя другие поля для хранения старших битов).
    

### 4. `__le32 i_blocks;` (Счетчик блоков)

- **Тип данных:** `__le32` (4 байта).
    
- **За что отвечает:** Указывает, сколько **512-байтовых секторов** (в Linux традиционно используется этот размер для данного поля) реально занято файлом на диске.
    
- **Зачем это нужно:** Размер файла (`i_size`) может быть 100 байт, но на диске он всегда занимает минимум один блок (например, 4096 байт). Это поле помогает ОС вести учет реального потребления места.
    

### 5. `__le32 i_block[15];` (Карта адресов)

Это массив из **15 элементов**, каждый по 4 байта. Общий размер — 60 байт. Это «адресная книга» файла.

- **`i_block[0]` — `i_block[11]` (Прямые адреса):** В этих 12 ячейках записаны номера блоков (LBA) на диске, где лежат данные. Если файл маленький (до 12 блоков), все его адреса помещаются здесь.
    
- **`i_block[12]` (Косвенный адрес):** Номер блока, в котором лежит список номеров других блоков.
    
- **`i_block[13]` (Двойной косвенный):** Указатель на блок, который указывает на блоки, в которых лежат номера блоков.
    
- **`i_block[14]` (Тройной косвенный):** Самый глубокий уровень вложенности для очень больших файлов.
    

---

### Что пропущено в слайде (но подразумевается в `// ...`):

Чтобы структура была полной, в ней также обычно присутствуют:

- `i_gid`: ID группы владельца.
    
- `i_atime / i_mtime / i_ctime`: Временные метки (по 4 байта каждая).
    
- `i_links_count`: Счетчик жестких ссылок (сколько имен у этого inode).
    
- `i_flags`: Специальные флаги (например, запрет на удаление).

---
### Система 12 + 3 (Адресация блоков)

В распоряжении Inode есть всего 15 ячеек для хранения номеров блоков. Если размер блока 4 КБ, то 15 ячеек — это всего 60 КБ. Маловато для фильма в 4К, верно? Поэтому инженеры придумали «матрешку» из указателей.

#### 1. Прямые указатели (Direct Blocks): ячейки с 0 по 11

Первые 12 чисел в массиве — это прямые номера блоков на диске.

- Если файл маленький (до 48 КБ), его адреса помещаются прямо здесь.
    
- **Плюс:** Мгновенный доступ. ОС читает Inode и сразу знает, куда прыгать за данными.
    

#### 2. Одинарный косвенный указатель (Indirect): ячейка №12

Если файл не влез в 48 КБ, 12-я ячейка указывает на **блок-посредник**.

- В этом блоке-посреднике лежат не данные, а просто список номеров других блоков.
    
- Один блок (4 КБ) может хранить 1024 номера блоков (если каждый номер — 4 байта).
    
- **Итог:** Мы сразу расширяем размер файла до 4 МБ.
    

#### 3. Двойной косвенный (Double Indirect): ячейка №13

Здесь уровень вложенности растет. Это «указатель на указатель на блоки».

- Один такой указатель позволяет адресовать уже гигабайты данных.
    

#### 4. Тройной косвенный (Triple Indirect): ячейка №14

Это финальный уровень. Позволяет работать с файлами терабайтного размера.

---

### ❓ Почему это «старая школа» и в чем проблема?

Представь, что ты хочешь прочитать последний байт видеофайла на 10 ГБ.

1. ОС читает Inode.
2. Видит, что нужно идти через тройной косвенный указатель.
3. Читает блок-посредник 1.
4. Читает блок-посредник 2.
5. Читает блок-посредник 3.
6. И только потом находит номер нужного блока с данными. **Это 4 лишних чтения с диска!** На медленных HDD это было очень долго.
    

---

### ✅ Как это решено у тебя (в ext4)?

Помнишь, в твоем выводе `stat` было написано `regular file`, а в `dumpe2fs` — `extent`? В `ext4` вместо этого массива `i_block[15]` используется структура **Extent Tree**. Она не хранит номера каждого блока по отдельности. Она хранит: **«Начальный блок — Количество блоков»**.

Если твой файл `passwd` занимает 1 блок, в `ext4` это выглядит так:

- _«Блок 49242, длина 1»_.
    

А если бы файл был 1 ГБ и лежал одним куском:

- _«Блок 1000, длина 262144»_. Всего одна запись вместо гигантской «матрешки» из косвенных указателей!

![[Pasted image 20251206133751.png]]

### Что конкретно изображено на схеме:

1. **Inode (левая колонка):** Это «паспорт» файла. В нем есть метаданные (Information) и массив указателей (номера строк 1, 2, 3 ... 13, 14, 15).
    
2. **Direct Data Blocks (Прямые указатели):** Строки с 1 по 12 (на схеме показаны первые три) указывают **напрямую** на блоки, где лежат ваши данные (серые квадраты).
    
3. **Indirect Data Blocks (Косвенная адресация):** * **Строка 13** указывает на «Блок указателей» (**Blocks of Pointers**).
    
    - В этом промежуточном блоке лежат не данные, а список адресов других блоков.
        
4. **Double Indirect (Двойная косвенная):**
    
    - **Строка 14** указывает на блок, который указывает на блоки, которые в свою очередь указывают на данные. Это «матрешка» из двух уровней.
        
5. **Triple Indirect (Тройная косвенная):**
    
    - **Строка 15** (пунктир внизу) — это три уровня вложенности для самых больших файлов.


![[Pasted image 20251206133807.png]]

### 1. Таблица i-узлов (Inode Table)

Представь себе длинный стеллаж с папками в архиве.

- **Слайд говорит:** i-узлы хранятся в таблице, которая является массивом.
    
- **Что это значит:** В каждой группе блоков есть выделенное место (та самая **Inode Table**, которую ты видела в `dumpe2fs`). Это просто непрерывная область на диске, «нарезанная» на ячейки по 128 или 256 байт.
    
- **Индекс массива:** Когда мы говорим «Inode №49242», система просто берет начало таблицы и отсчитывает от него 49242-ю ячейку. Это очень быстро, так как не нужно ничего искать — достаточно простого математического расчета адреса.
    

### 2. Сколько файлов может быть в «районе»?

- **Слайд говорит:** Количество записей обычно **8192** для ext4 (может быть до 32768).
    
- **Почему это важно:** Это подтверждает твой вывод `dumpe2fs`, где было написано `Inodes per group: 8192`.
    
- Это лимит «вместимости» одной группы блоков. Если ты создашь в одной группе 8192 маленьких файла, место для новых паспортов в этой группе закончится, и системе придется создавать файлы в следующей группе блоков.
    

### 3. Битовая карта i-узлов (Inode Bitmap)

Это «чек-лист» для системы.

- Чтобы не заглядывать в каждую ячейку таблицы и не проверять, пустая она или нет, у системы есть **Битовая карта**.
    
- Это последовательность битов (0 и 1).
    
    - **0** — вешалка (inode) свободна.
        
    - **1** — вешалка занята файлом.
        
- Когда ты создаешь файл, Linux смотрит в эту карту, находит первый «0», меняет его на «1» и записывает данные в соответствующую ячейку таблицы.
    

---

### Разбор картинки на слайде (Справа)

Там показана визуальная связь:

1. **Inode Table**: массив ячеек (0, 1, 2, 3, 4...).
    
2. **Стрелочка от ячейки №0**: ведет к подробному описанию (Metadata).
    
3. **Metadata**: ты видишь те самые поля, которые мы разбирали в коде:
    
    - `owner` (владелец),
    - `timestamps` (три времени: доступ, изменение, смена статуса),
    - `permissions` (права доступа),
    - `file type` (тип файла).

![[Pasted image 20251206133816.png]]

### 1. Что такое Symlink на самом деле?

- **Слайд говорит:** Это специальный файл, который содержит путь к другому файлу.
    
- **Как это устроено внутри:** У симлинка есть свой собственный номер **Inode**. Но в его блоках данных (или прямо внутри Inode, если путь короткий) записан не контент, а просто текстовая строка — адрес (например, `/home/user/file.txt`).
    

### 2. Как работает ОС при открытии ссылки?

- **Слайд говорит:** Когда программа открывает ссылку, ОС открывает файл по пути, который в ней указан.
    
- Процесс: 1. Система видит, что тип файла в Inode — symbolic link.
    
    2. Она читает содержимое ссылки (текстовый путь).
    
    3. Она заново начинает поиск файла, но уже по этому новому адресу.
    
    4. Если по тому адресу лежит еще одна ссылка, процесс повторяется (но не бесконечно, есть лимит).
    

### 3. Проблема «Битых ссылок»

- **Слайд говорит:** Если переименуем или удалим файл, ссылка не будет никуда указывать.
    
- **Почему:** Симлинк — это как записка на двери: «Я ушел в магазин по адресу ул. Ленина, 5». Если магазин переехал или закрылся, записка на двери остается, но она ведет в пустоту. Такая ссылка называется **«битой» (dangling symlink)**.
    
- В этом главное отличие от жесткой ссылки: жесткая ссылка хранит данные файла до тех пор, пока существует хотя бы одно имя. Символическая ссылка — это просто «указатель на имя».
    

---

### Сравнение для наглядности:

| **Характеристика**        | **Hard Link (Жесткая ссылка)** | **Symlink (Символическая ссылка)** |
| ------------------------- | ------------------------------ | ---------------------------------- |
| **Inode**                 | Тот же самый, что у оригинала  | **Свой собственный**               |
| **Что внутри**            | Прямой доступ к блокам данных  | Текст с путем к файлу              |
| **Если удалить оригинал** | Данные доступны через ссылку   | **Ссылка перестает работать**      |
| **Через разные диски**    | Невозможна                     | **Возможна**                       |
- **Жесткие связи:** Имя -> Inode (один Inode — много имен).
    
- **Символические связи:** Имя1 -> Inode_ссылки -> Путь -> Имя2 -> Inode_оригинала.

## Зачем они нужны(символические)
### 1. Управление версиями программ и библиотек

Это самое частое применение. Представь, что у тебя в системе установлена библиотека `libdatabase.so.2.1`. Программы ищут её именно под этим именем.

- Ты обновляешь её до версии `libdatabase.so.2.2`.
    
- Чтобы не перенастраивать все программы, ты просто создаешь симлинк `libdatabase.so`, который указывает на актуальную версию.
    
- Программа обращается к «стабильному» имени-ссылке, а система незаметно перенаправляет её на самый свежий файл.
    

### 2. Создание коротких путей (Alias)

Иногда файлы лежат очень глубоко в структуре каталогов, например: `/var/lib/docker/volumes/my_very_long_name_storage/_data/config.yaml`

- Чтобы не вводить этот путь каждый раз, ты создаешь симлинк `config` прямо в домашней папке.
    
- Теперь ты можешь редактировать файл по короткому пути, а изменения будут происходить в реальном «далеком» файле.
    

### 3. Экономия места и работа между дисками

Жесткие ссылки (hard links) **не могут** указывать на файлы на другом физическом диске или разделе.

- Символические ссылки **могут**.
    
- **Пример:** У тебя закончилось место на SSD (раздел `/`), и ты перенесла тяжелую папку с играми на медленный HDD (раздел `/mnt/hdd`). Чтобы система «думала», что папка всё еще на месте, ты оставляешь на SSD симлинк, указывающий на новое место на HDD.
    

### 4. Стандартизация структуры системы

Разные дистрибутивы Linux могут хранить системные файлы в разных местах.

- Чтобы программы, написанные под один дистрибутив, работали в другом, разработчики создают «стандартные» ссылки.
    
- Например, часто `/bin` — это просто симлинк на `/usr/bin`. Это позволяет объединить все исполняемые файлы в одном месте, сохраняя совместимость со старыми скриптами.
    
## Проблемы

### 1. Проблема «битых» ссылок (Dangling Symlinks)

Это самая частая проблема, о которой говорилось на твоем слайде.

- **Суть:** Если ты удалишь, переименуешь или переместишь **оригинальный файл**, ссылка об этом не узнает. Она продолжит хранить старый путь.
    
- **Результат:** При попытке открыть такую ссылку ты получишь ошибку «Файл не найден» (`No such file or directory`), хотя сама ссылка (файл-ярлык) на месте.
    

### 2. Зацикливание (Symlink Loops)

Это «кошмар» для поисковых алгоритмов и антивирусов.

- **Суть:** Можно создать ссылку `A`, которая указывает на `B`, а внутри папки `B` создать ссылку, которая указывает обратно на `A`.
    
- **Результат:** Если программа (например, сканер диска) не умеет распознавать такие циклы, она будет бесконечно ходить по кругу, пока не съест всю оперативную память или не выдаст ошибку переполнения стека.
    

### 3. Снижение производительности

Каждый раз, когда ты обращаешься к симлинку, операционная система вынуждена делать дополнительную работу:

1. Прочитать **Inode ссылки**.
    
2. Прочитать **содержимое ссылки** (путь).
    
3. Начать **новый процесс поиска** (path resolution) уже для того файла, на который она указывает.
    

- Если у тебя цепочка ссылок (ссылка на ссылку на ссылку), каждое открытие файла превращается в целый квест для ядра ОС. Это медленнее, чем прямое обращение к файлу через Hard Link.
    

### 4. Проблемы безопасности (Symlink Race / Attacks)

Хакеры часто используют симлинки для обмана программ, работающих от имени администратора (`root`).

- **Пример:** Программа хочет записать лог в файл `/tmp/log.txt`. Хакер быстро удаляет этот файл и подсовывает на его место симлинк, указывающий на `/etc/shadow` (где лежат пароли).
    
- **Результат:** Программа, думая, что пишет лог, перезаписывает критически важный системный файл. Современные системы защищены от этого, но это всё еще классический вектор атак.
    

### 5. Относительные vs Абсолютные пути

- Если в ссылке прописан **абсолютный путь** (`/home/user/file.txt`), и ты перенесешь папку с этой ссылкой на другой компьютер, она может перестать работать, так как там структура папок другая.
    
- Если прописан **относительный путь** (`../data/file.txt`), и ты переместишь саму ссылку в другую папку, она «потеряет» свой файл, так как путь «на один уровень вверх» теперь ведет в другое место.
    

---

### Резюме: когда что использовать?

- Если тебе нужно несколько имен для файла **внутри одного диска** и ты хочешь максимальной надежности — используй **Hard Link**.
    
- Если тебе нужно ссылаться на **папки**, на файлы на **других дисках** или создавать удобные **псевдонимы** — используй **Symlink**, но следи, чтобы оригинал не «убежал»
## Где это нада

### 1. Перенос тяжелых папок на другой диск (Самый частый кейс)

Представь, что у тебя диск `C:` (SSD) почти заполнен, а игра или тяжелая программа (например, Docker или папка с библиотеками Python) по умолчанию лезет в твой профиль пользователя.

- **Что ты делаешь:** Переносишь огромную папку (например, `C:\Users\Kriasa\Videos`) на диск `D:`.
    
- **Проблема:** Windows или программы «привыкли» искать файлы по старому адресу.
    
- **Решение:** Ты создаешь **символическую ссылку** на диске `C:`, которая называется `Videos`, но физически «пробрасывает» систему на диск `D:`.
    
- **Результат:** Программы продолжают работать с `C:\Users\Kriasa\Videos`, не замечая подвоха, а место на SSD свободно.
    

### 2. Удобная работа с проектами в WSL

Если ты разрабатываешь что-то в WSL, тебе часто нужно быстро попадать в папки Windows.

- **Пример:** Твои учебные проекты лежат по адресу `/mnt/c/Users/Kriasa/Documents/Projects/Python/Study/`. Это очень долго вводить.
    
- **Решение:** В домашней папке Linux ты создаешь ссылку: `ln -s /mnt/c/Users/Kriasa/Documents/Projects/Python/Study/ ~/projects`
    
- **Результат:** Теперь ты просто пишешь `cd ~/projects`, и ты мгновенно там. Для системы это выглядит как обычная папка внутри Linux.
    

### 3. "Бесконечный" Dropbox или Google Диск

Облачные сервисы часто умеют синхронизировать только одну конкретную папку (например, `C:\Dropbox`).

- **Что ты делаешь:** Ты хочешь синхронизировать папку с сохранениями игры, которая лежит в `AppData/Roaming/...`.
    
- **Решение:** Ты создаешь внутри папки `Dropbox` символическую ссылку на папку с игрой.
    
- **Результат:** Dropbox «думает», что файлы лежат внутри него, и выкачивает их в облако, хотя на самом деле они остаются в папке с игрой.
    

---

### 4. Версионность (Магия обновлений)

Это то, что ты видела в выводе `dumpe2fs` или `ls -l /usr/bin`.

- В системе может стоять интерпретатор `python3.10` и `python3.12`.
    
- Программе часто всё равно, какая минорная версия стоит, она хочет просто `python3`.
    
- **Решение:** Файл `/usr/bin/python3` — это **символическая ссылка** на `/usr/bin/python3.12`.
    
- **Результат:** Когда выйдет `python3.13`, системный администратор просто перекинет ссылку на новый файл. Саму программу переписывать не придется.
![[Pasted image 20251206133824.png]]

Если символическая ссылка — это «ярлык-записка», то жесткая ссылка — это «второе официальное имя».

Разберем тезисы со слайда:

### 1. Несколько имен — один i-узел

- **Суть:** Вспомни нашу аналогию с паспортом (i-узел). В файловой системе каталог хранит список пар «Имя — Номер i-узла».
    
- **Как это работает:** Ты можешь создать для одного и того же номера i-узла (например, 49242) два разных имени в каталоге: `file1.txt` и `file2.txt`.
    
- **Результат:** С точки зрения системы это **один и тот же файл**. У них общие права доступа, общие блоки данных и общий размер, потому что все эти данные лежат в одном i-узле.
    

### 2. Синхронные изменения

- **Слайд говорит:** Любые изменения в одном файле приведут к изменениям в другом.
    
- **Почему:** Поскольку оба имени ведут к одному и тому же i-узлу, который указывает на одни и те же блоки данных на диске, редактирование `file1` мгновенно «меняет» `file2`. Ты просто правишь одно и то же физическое содержимое под разными именами.
    

### 3. Счетчик ссылок (Reference Counter)

Это ключевой механизм контроля «жизни» файла.

- **`i_links_count`**: В i-узле есть специальное поле (счетчик). Оно показывает, сколько имен в системе сейчас указывают на этот узел.
    
- **Удаление:** Когда ты вводишь команду `rm file1`, система не стирает данные сразу. Она просто удаляет имя из каталога и **уменьшает счетчик на 1**.
    
- **Окончательное удаление:** Файл (блоки данных и сам i-узел) будет помечен как свободный только тогда, когда счетчик станет равен **0**. То есть, пока живо хотя бы одно имя (жесткая ссылка), файл существует.
    

### 4. Открытые файлы и счетчик

- **Слайд говорит:** Если открыть файл, его счетчик увеличится.
    
- **Зачем это нужно:** Это защита от случайного удаления. Если ты удалишь файл (`rm`), пока его читает какая-то программа, счетчик не упадет до нуля (так как программа его «держит»). Файл исчезнет из списка папок, но физически останется на диске, пока программа его не закроет.
    

---

### 🛠️ Давай увидим это на твоем примере с `/etc/passwd`

Посмотри еще раз на свой вывод команды `stat /etc/passwd`:

> `Device: 8,48 Inode: 49242 Links: 1`

Видишь поле **Links: 1**? Это и есть тот самый `reference counter`. Он говорит, что у этого i-узла сейчас всего одно имя.

**Давай проведем эксперимент (виртуально):**

1. Если мы выполним: `sudo ln /etc/passwd /etc/passwd_copy` (создадим жесткую ссылку).
    
2. Выполним `stat /etc/passwd` еще раз.
    
3. Поле **Links** станет равным **2**. Но номер **Inode** останется **49242**.
    

---

### ⚠️ Ограничения жестких ссылок (почему они не заменяют всё):

1. **Только внутри одного раздела:** Ты не можешь сделать жесткую ссылку с диска `C:` на диск `D:`, потому что на разных дисках свои таблицы i-узлов, и номера могут совпасть.
    
2. **Нельзя для каталогов:** Чтобы не создать бесконечные петли в структуре папок, обычным пользователям запрещено создавать жесткие ссылки на папки.

## Примерчики

Вот где это реально применяется в твоем компьютере:

### 1. Экономия места при бэкапах (Time Machine в macOS / rsync в Linux)

Это самое гениальное применение. Представь, что ты делаешь бэкап системы каждый день.

- В понедельник ты скопировала 100 ГБ данных.
    
- Во вторник изменился только один файл на 1 МБ.
    
- **Как работают жесткие ссылки:** Программа бэкапа не копирует все 100 ГБ заново. Она создает в папке «Вторник» **жесткие ссылки** на те же самые i-узлы, что были в «Понедельнике».
    
- **Результат:** У тебя есть две папки, в каждой из которых лежат «все» файлы. Но физически на диске они занимают место один раз. Если ты удалишь папку «Понедельник», файлы во «Вторнике» останутся живы, так как счетчик ссылок (`Links count`) всё еще больше нуля.
    

### 2. Установка обновлений (Windows Side-by-Side — WinSxS)

В Windows есть огромная папка `C:\Windows\WinSxS`. Если ты посмотришь на файлы в `C:\Windows\System32`, то многие из них — это жесткие ссылки на файлы внутри `WinSxS`.

- **Зачем это нужно:** Система хранит «эталонную» версию файла в хранилище компонентов, а в рабочую папку System32 пробрасывает жесткую ссылку. Это позволяет разным программам использовать разные версии одной и той же библиотеки, не дублируя их физически на диске и защищая систему от поломки.
    

### 3. Защита от случайного удаления важного файла

Поскольку файл удаляется только тогда, когда счетчик ссылок становится равным нулю, жесткая ссылка работает как **страховка**.

- Ты можешь создать жесткую ссылку на важный конфиг в другом месте диска.
    
- Даже если ты (или какой-то скрипт) случайно удалишь оригинал, данные не пропадут, потому что вторая «ссылка-имя» всё еще держит i-узел и блоки данных.
    

### 4. Процесс «удаления» открытого файла

Ты когда-нибудь замечала, что в Linux можно удалить файл (например, лог-файл), который в этот момент пишет программа, и система не выдает ошибку «Файл занят»?

- **Что происходит:** Команда `rm` удаляет имя файла (счетчик ссылок уменьшается). Но так как программа его «держит» (открытый дескриптор), внутренний счетчик ссылок в памяти не равен нулю.
    
- **Результат:** Файл исчезает из папки, но программа продолжает в него писать. Место на диске освободится только тогда, когда программа закроется и счетчик ссылок окончательно станет равным 0.
    

---

### 🏁 Краткая шпаргалка: когда использовать?

|**Ситуация**|**Что использовать?**|
|---|---|
|Нужно сделать "зеркало" файла на том же диске для надежности|**Hard Link**|
|Нужно перенести папку с игрой на другой диск, чтобы игра её видела|**Symlink**|
|Нужно сделать ярлык программы на рабочий стол|**Ярлык (.lnk / .desktop)**|
|Нужно, чтобы при удалении оригинала файл "сломался"|**Symlink**|

![[Pasted image 20251206133836.png]]

### 1. Директория — это тоже файл

- **В чем секрет:** В Linux директория — это не какая-то особая «коробка», а обычный файл, который помечен специальным типом в i-узле.
    
- **Контент:** Вместо текста или видео, внутри этого «файла» лежит простая таблица соответствий: **Имя файла <-> Номер i-узла**.
    
- **Именно поэтому:** Имя файла не хранится в i-узле самого файла. Оно хранится в «файле-директории».
    
1. **Для тебя (как пользователя):** Директория — это «контейнер» или папка, в которую можно зайти, положить туда файлы или другие папки.
    
2. **Для системы (под капотом):** Директория — это **специальный файл**, в котором записан список имен файлов и их «адресов» (номеров i-узлов).
    

### Давай наглядно:

Когда ты в терминале пишешь `cd /home/user/Documents`, ты «переходишь в папку». Но технически происходит вот что:

- Система открывает «файл-директорию» с именем `home`.
    
- Читает его содержимое и находит там строчку `user`.
    
- Переходит к «файлу-директории» `user`.
    
- Читает его и находит строчку `Documents`.
    
- Теперь твоя «рабочая область» привязана к i-узлу этой папки.
    

### Почему важно помнить, что это «файл»?

Помнишь, на одном из твоих первых слайдов было написано: **«В Unix абсолютно всё — файлы»**? Это значит, что операционной системе проще работать со всем одинаково.

- У обычной папки (директории), как и у обычного файла, есть:
    
    - Свой **i-узел** (паспорт).
        
    - Свои **права доступа** (кто может «зайти» в папку, а кто нет).
        
    - Свой **владелец** (UID).
        
    - Свои **метки времени** (когда папку создали или когда в ней что-то меняли).
        

### Единственное отличие:

У обычного файла в блоках данных лежит текст, картинка или видео. А у директории в блоках данных лежит **список содержимого** этой папки.
### 2. Что такое dentry (Directory Entry)?

Когда операционная система работает с путями (например, `/home/user/test.txt`), ей приходится постоянно искать имена в таблицах директорий. Чтобы не делать это медленно, в памяти ядра создаются структуры `dentry`.

- **Dentry** — это логическая связь между именем файла и его i-узлом в оперативной памяти. Это «кэш», который помогает ядру быстро переходить от имени к данным.

Dentry (Directory Entry) — это **структура в оперативной памяти (RAM)**. Она не хранится на диске. Она создается «на лету» ядром Linux, когда ты начинаешь обращаться к файлам.

**Почему нельзя использовать только Dentry?** Потому что оперативная память — штука временная. Если ты выключишь компьютер, все `dentry` исчезнут. Директории на диске — это **хранилище**, а dentry — это **быстрый кэш** в памяти.

---

### 3. Разбор структуры `struct dentry`

Слайд показывает, как это выглядит в коде ядра Linux:

- **`struct inode *d_inode;`**: Самое важное поле. Это прямой указатель на i-узел (паспорт) файла.
    
- **`struct qstr d_name;`**: Здесь хранится имя файла (строка и её длина).
    
- **`struct dentry *d_parent;`**: Указатель на родительскую директорию. Благодаря этому система знает, что `/home/user` находится внутри `/home`.
    
- **`d_subdirs` и `d_child`**: Списки, которые связывают папки в древовидную структуру. Это позволяет быстро найти все файлы внутри конкретной папки.
    
- **`d_bucket`**: Используется для хэш-таблицы, чтобы поиск файла «по имени» занимал доли миллисекунды даже в папке, где лежат миллионы файлов.
    

---

### 🏘️ Пример из жизни (на твоем компьютере)

Представь папку `Documents`. На диске это файл-список:

1. `foto.jpg` -> Inode 101
    
2. `report.pdf` -> Inode 102
    
3. `work` (папка) -> Inode 103
    

Когда ты вводишь `cd work`:

1. Ядро берет `dentry` текущей папки.
    
2. Ищет в списке `d_subdirs` имя "work".
    
3. Находит указатель на `d_inode` (номер 103).
    
4. Переходит к этому i-узлу, который, в свою очередь, указывает на новый список файлов.


### Как происходит поиск файла (Пример: `/etc/passwd`)

Вот как эти двое работают в паре:

#### Шаг А: Работа с Директорией (Первый раз)

Когда ты первый раз после включения компьютера обращаешься к файлу, в памяти еще нет нужных `dentry`.

1. Ядро идет на диск и читает i-узел корня `/`.
    
2. Читает «файл-директорию» корня и ищет там строку `etc`. Находит номер i-узла для `etc`.
    
3. Читает «файл-директорию» `etc` и ищет там строку `passwd`. Находит номер 49242.
    
4. **Это долго**, потому что было много обращений к диску.
    

#### Шаг Б: Работа с Dentry (Второй и последующие разы)

Как только ядро нашло файл, оно создает в памяти «цепочку» из структур `dentry`:

- `dentry` для `/`
    
- `dentry` для `etc` (указывает на родителя `/`)
    
- `dentry` для `passwd` (указывает на родителя `etc`)
    

Теперь, когда ты снова захочешь открыть этот файл, ядро не пойдет на диск. Оно посмотрит в **Dentry Cache** (хэш-таблицу в памяти). Оно мгновенно увидит: «Ага, `passwd` лежит в `etc`, и я уже знаю, что его i-узел — 49242».

---

### Почему это важно до выключения?

Оперативная память очень быстрая, но «забывчивая». Как только компьютер выключается, весь **Dentry Cache** (кэш путей) очищается. Поэтому после первого включения компьютер может чуть дольше «задумываться» при открытии папок, а потом начинает «летать».

### Интересный факт про `dentry`:

Они сохраняют информацию даже об **отсутствующих** файлах! Если ты 100 раз подряд попробуешь открыть несуществующий файл `secret.txt`, Linux только первый раз сходит на диск, убедится, что его нет, и создаст так называемый **Negative Dentry**. В следующий раз система сразу скажет «Файла нет», вообще не тратя ресурсы диск

![[Pasted image 20251206133848.png]]

На прошлом слайде мы узнали, что **директория** — это список на диске, а **dentry** — это «маршрут» в оперативной памяти.

**Проблема:** В памяти могут быть миллионы таких «маршрутов» (dentry). Если ядро будет искать нужный маршрут простым перебором (как ты ищешь ключи в большой сумке), компьютер будет тормозить.

**Решение этого слайда:** Чтобы поиск был мгновенным, Linux использует **хэш-таблицу (dcache)**.

**Dcache** — это и есть такой «умный гардероб» в памяти Linux. Ключом для формулы служит **Имя файла + его Папка-родитель**.

### Почему именно хэширование?

Представь, что у тебя в одной папке 10 000 файлов. Если искать файл «по старинке», системе придется перебирать все 10 000 имен, пока она не найдет нужное. Это медленно.

- **Хэш-таблица (dcache)** превращает имя файла в число (индекс). Вместо перебора всех имен, система мгновенно «прыгает» в нужную ячейку таблицы.
    
- **Ключ для поиска:** используется не только имя файла, но и его **родитель**. Это важно, потому что в системе может быть много файлов с именем `config.yaml`, но только один из них лежит в папке `/etc/ssh/`.

### Жизненный цикл dentry (как ядро ими «дышит»)

Здесь описана стандартная работа системы, когда ты, например, открываешь файл:

1. **`lookup()` (Поиск):** Ты вводишь команду. Ядро сначала лезет в свой скоростной кэш (`dcache`). Если путь там есть — победа, диск не трогаем!
    
2. **`d_alloc()` (Выделение памяти):** Если в кэше пусто, ядро выделяет кусочек оперативной памяти под новую структуру `dentry`.
    
3. **`d_add()` (Привязка):** Ядро считывает данные с диска, узнает номер i-узла и «склеивает» имя файла с его паспортом в памяти. Теперь путь официально «запомнен».
    
4. **`dput()` (Уменьшение счетчика):** Когда ты закрываешь файл, ядро уменьшает счетчик использования этого `dentry`. Но оно **не удаляет** его сразу! Оно оставляет его в памяти на случай, если ты снова его откроешь через секунду.
    
5. **`d_invalidate()` (Аннулирование):** Если файл удалили или переименовали, старый маршрут в памяти больше не верен. Ядро помечает эту запись как «мусор».
    
6. **`d_prune()` (Обрезка):** Если оперативная память (RAM) заканчивается, а тебе нужно запустить тяжелую игру или браузер, Linux начинает «выбрасывать» самые старые и неиспользуемые `dentry` из памяти, чтобы освободить место.
---

### Жизненный цикл dentry (Как это происходит пошагово)

Давай проследим за судьбой одного файла, например, когда ты открываешь картинку `cat.jpg`:

1. **Поиск (`lookup`):** Ядро спрашивает у кэша: «У тебя есть маршрут до `cat.jpg` в папке `Photos`?». Оно считает хэш и прыгает в нужную ячейку памяти.
    
2. **Создание (`d_alloc`):** Если в памяти пусто (ты первый раз открываешь файл), ядро говорит: «Окей, я этого не помню. Выделяю в памяти пустое место под новый маршрут».
    
3. **Склейка (`d_add`):** Ядро лезет на медленный диск, находит там i-узел файла и «приклеивает» его к нашему маршруту в памяти. Теперь система всё знает.
    
4. **Ожидание (`dput`):** Ты закрыла картинку. Ядро уменьшает счетчик (никто файл не смотрит), но **оставляет маршрут в памяти**. Оно надеется: «А вдруг ты сейчас снова захочешь её открыть? Я не буду удалять это из памяти, пусть полежит».
    
5. **Очистка (`d_prune`):** Проходит час. Ты открыла тяжелую игру. Памяти не хватает. Ядро смотрит на старый маршрут к `cat.jpg` и говорит: «Хозяйка давно его не открывала, а память нужна игре». И **выбрасывает** этот dentry из памяти.
    

---

### Итоговая связь всех понятий:

1. **Диск:** Хранит файлы и таблицы имен (Директории).
    
2. **Inode:** Паспорт файла (лежит на диске, кэшируется в памяти).
    
3. **Dentry:** Тот самый «маршрут» в памяти, который связывает Имя и Inode.
    
4. **Dcache (этот слайд):** Способ организовать эти маршруты в памяти так, чтобы поиск по ним был мгновенным (через хэширование).

![[Pasted image 20251206133854.png]]

На картинке изображена структура данных, которая позволяет ядру Linux находить файлы мгновенно:

- **Хэш-таблица (слева):** Это входная точка. Когда ты вводишь имя файла, система вычисляет его хэш-код и сразу перепрыгивает в нужную строку таблицы, вместо того чтобы перебирать тысячи файлов по порядку.
    
- **Списки dentry (квадратики):** Каждая запись в кэше — это **dentry** (путь к файлу). Они связаны стрелочками в цепочки.
    
- **Фронт (LRU) и Тыл (MRU):** Это механизм управления памятью:
    
    - **MRU (Most Recently Used):** Сюда попадают файлы, к которым ты обратилась только что. Они «самые свежие» и важные.
        
    - **LRU (Least Recently Used):** Сюда сдвигаются файлы, про которые ты забыла. Если оперативной памяти станет мало (процесс `d_prune()` из твоего текста), система начнет удалять записи именно с этого конца — «стариков» выкидывают первыми.

![[Pasted image 20251206133900.png]]

### 1. Создание и удаление

- **Создать (create):** Система находит свободный бит в **битовой карте i-узлов** , выделяет новый **inode** и записывает имя файла в файл-директорию.
    
- **Удалить (unlink):** Обрати внимание на название `unlink`. Это именно то, о чем мы говорили на слайде про жесткие ссылки: система просто удаляет "связь" (имя) из директории и уменьшает счетчик ссылок в inode. Если счетчик стал 0 — файл стирается.
    
- **Переименовать (rename):** Это самая быстрая операция. Данные на диске не двигаются. Система просто меняет строчку в файле-директории и обновляет запись в **dcache**.
    

### 2. Открыть и закрыть

- **Открыть (open):** Именно здесь включается **dcache** и процесс **lookup()**. Система ищет путь в памяти, находит inode и создает в памяти "дескриптор" для твоей программы.
    
- **Закрыть (close):** Программа говорит, что файл ей больше не нужен. В этот момент срабатывает `dput()` — счетчик использования dentry уменьшается.
    

### 3. Чтение и запись

- **Прочитать (read) / Записать (write):** Вот тут система идет по цепочке указателей из i-узла . Она смотрит: "Так, мне нужен блок №1, потом №2... Ого, файл большой, лезу в блок №13 (косвенный), чтобы найти адреса остальных данных".
    

### 4. Найти (lseek)

- **lseek:** Это перемещение "курсора" внутри файла. Ты не читаешь данные, ты просто говоришь системе: "Перескочи сразу на 100-й мегабайт". Система по таблице указателей в inode вычисляет, в каком конкретно блоке данных находится эта позиция, чтобы не читать файл с самого начала.
    

### 5. Атрибуты (lstat, chmod, chown)

- Эти операции работают **только с i-узлом**, не трогая сами данные.
    
    - **lstat:** Просто читает "паспорт" файла (владелец, время, права).
        
    - **chmod / chown:** Меняет поля `permissions` или `owner` прямо в таблице i-узлов (твое 2-е фото).

### Итог по всем твоим материалам:

1. **Пользователь** вызывает `open()` или `read()`.
    
2. **Ядро** ищет путь в **dcache** (хэш-таблица на фото 3).
    
3. Если нашло — берет **inode** из таблицы (фото 2).
    
4. Через **указатели в inode** (фото 1) достает **блоки данных** с диска.

![[Pasted image 20251206133905.png]]
### 1. Операции с каталогом (структурные изменения)

Эти действия меняют саму «сетку» твоих папок.

- **Создать (`mkdir` / `opendir`):** * Когда ты создаешь папку, система выделяет для неё новый **i-узел** с типом «директория».
    
    - Внутри него автоматически создаются две записи: `.` (указывает на этот же i-узел) и `..` (указывает на i-узел родительской папки).
        
    - В родительской директории появляется новая строка в таблице `dirent` с именем твоей папки.
        
- **Удалить (`rmdir`):** * Система сначала проверяет, пуста ли папка (есть ли там что-то кроме `.` и `..`).
    
    - Если пуста, она удаляет запись из родительской директории и освобождает i-узел папки.
        
- **Переименовать (`rename`):** * Это «косметическая» операция. Система просто находит нужную структуру `dirent` в файле-директории и перезаписывает поле `d_name`.
    
    - Номер i-узла и данные на диске остаются нетронутыми.
        

---

### 2. Работа с «потоком» директории (открыть/закрыть)

Директорию нельзя просто открыть как текстовый файл в блокноте. Для этого есть специальные вызовы.

- **Открыть (`opendir`):** * Система проверяет твои права доступа (можно ли тебе «входить» в эту папку).
    
    - Если всё ок, она создает в памяти специальный «указатель» на начало списка файлов в этой папке.
        
- **Закрыть (`closedir`):** * Освобождает память, которую занимал этот указатель, и завершает сеанс работы с папкой.
    

---

### 3. Навигация по списку файлов

Представь, что папка — это длинный список имен.

- **Прочитать следующую запись (`readdir`):** * Это как листать книгу по одной странице. Каждый вызов `readdir` выдает тебе одну структуру `dirent` (имя, номер i-узла, тип) и сдвигает «курсор» к следующему файлу.
    
    - Именно так работает команда `ls`: она вызывает `readdir` в цикле, пока файлы не кончатся.
        
- **Сместить указатель (`seekdir`):** * Использует поле `d_off` из структуры. Если ты хочешь вернуться к 5-му файлу в списке или перескочить сразу к 10-му, ты используешь эту операцию.
    

---

### 4. Создание связей (Ссылки)

Это самое интересное — как мы соединяем имена с данными.

- **`link` (Hard link):** * Система берет существующий номер i-узла (например, файла `A`) и создает в директории **новую запись** с другим именем (например, `B`), но с тем же самым номером i-узла.
    
    - Счетчик ссылок в самом i-узле увеличивается на 1.
        
- **`unlink` (Удаление):** * Система удаляет запись `dirent` из папки.
    
    - Она **не удаляет файл сразу**. Она уменьшает счетчик ссылок в i-узле. Если счетчик стал 0 — только тогда данные стираются.
        
- **`symlink` (Символическая ссылка):** * Система создает **совершенно новый** i-узел специального типа.
    
    - В блоки данных этого i-узла она записывает просто текстовую строку — путь к оригинальному файлу.

---
### Разбор всех полей `struct dirent`

- **`ino_t d_ino`**: Номер i-узла. Тот самый уникальный ID («паспорт»), который мы обсуждали ранее. Именно через него имя файла связывается с его метаданными на диске.
    
- **`off_t d_off`**: Несмотря на название (offset — смещение), в современных системах это скорее «маркер» или «указатель» на следующую запись в каталоге. Когда программа читает длинный список файлов, это поле говорит ей: «Чтобы найти следующий файл, прыгай вот сюда». Оно нужно для работы функции `seekdir()`.
    
- **`unsigned short d_reclen`**: Длина всей этой записи в байтах. Поскольку имена файлов (`d_name`) бывают разной длины (одно — 3 буквы, другое — 20), записи в «файле-директории» имеют разный размер. Это поле сообщает системе, сколько байт нужно отступить, чтобы добраться до начала следующей структуры `dirent`.
    
- **`unsigned char d_type`**: Тип файла. Это очень крутая оптимизация. Здесь сразу указано, является ли объект папкой, обычным файлом или ссылкой. Благодаря этому команда `ls -F` или `ls --color` может мгновенно раскрасить файлы в разные цвета, не тратя время на открытие i-узла каждого файла.
    
- **`char d_name[256]`**: Само имя файла. Это массив символов, заканчивающийся нулевым байтом. В большинстве систем Linux лимит длины имени файла — 255 символов (плюс 1 байт на завершающий ноль).Заметь: **имя хранится в директории**, а не в самом файле!

### Как это связано в одну систему?

Теперь мы можем собрать «Полную цепочку жизни файла» из всех твоих слайдов:

1. **Поиск в памяти**: Ты вводишь имя файла. Система первым делом бежит в **dcache** (хэш-таблица на фото 3). Если маршрут (dentry) найден — мы сразу получаем Inode.
    
2. **Поиск на диске (если в памяти пусто)**:
    
    - ОС открывает **директорию** (папку).
        
    - Читает её содержимое как набор структур **`dirent`** (этот слайд).
        
    - Сравнивает `d_name` с тем, что ты ввела.
        
    - Находит нужный **`d_ino`**.
        
3. **Работа с данными**:
    
    - Зная номер i-узла, ОС находит его в **таблице i-узлов** (фото 2).
        
    - Из i-узла она берет **указатели на блоки** (фото 1: прямые, косвенные 128 строк и т.д.).
        
    - И наконец, считывает сами **данные** (серые квадраты на фото 1).

![[Pasted image 20251206133915.png]]

**VFS (Virtual File System)** — это «переводчик» или универсальный пульт, который стоит между твоей программой и реальным диском.

---

### 1. Почему без VFS было бы плохо?

Представь, что у тебя в компьютере стоит флешка (FAT32), системный диск (Ext4) и сетевой диск.

- **Без VFS:** Твоя программа (например, Python или браузер) должна была бы сама знать: «Так, этот файл на флешке, значит мне нужно использовать код для FAT32. А этот на диске — использую код для Ext4». Это был бы хаос.
    
- **С VFS:** Программа просто говорит: `open("file.txt")`. Ей всё равно, какая там файловая система. Она общается только с VFS.
    

### 2. Как это работает (Объекты и указатели)

Ядро Linux держит в памяти «чертежи» (объекты), которые мы уже изучили:

1. **Superblock** — информация о всей ФС.
    
2. **Inode** — информация о конкретном файле.
    
3. **Dentry** — информация о пути (маршруте).
    
4. **File** — информация об открытом файле (позиция чтения и т.д.).
    

**Магия указателей:** Внутри каждого такого объекта есть таблица «методов» (функций). Когда ты вызываешь `read()`, VFS смотрит: «Ага, этот файл лежит на Ext4. Значит, я вызываю функцию `ext4_read()`. А если файл на флешке — вызываю `fat32_read()`.»

---

### 3. Типы файловых систем, которые видит VFS

VFS позволяет работать не только с реальными железками, но и с «воображаемыми» файлами:

- **Физические (Дисковые):** Те, что реально записывают нули и единицы на блин диска или в ячейки SSD (**ext4, btrfs, xfs, zfs**).
    
- **Виртуальные (Спец-файлы):**
    
    - `procfs` (папка `/proc`): Там нет реальных файлов. Когда ты заходишь туда, VFS на лету генерирует текст о том, как работает твой процессор или сколько памяти занято.
        
    - `sysfs` (папка `/sys`): Файлы для настройки железа (яркость экрана, режим работы батареи).
        
    - `tmpfs`: Файлы, которые живут только в оперативной памяти (исчезают при выключении).
        
- **FUSE (Filesystem in Userspace):** Позволяет обычным программистам писать свои ФС (например, чтобы облако Google Drive выглядело как обычная папка на компе).
    

---

### Итоговая связь с прошлыми слайдами:

VFS — это то место, где живут структуры **dentry** (фото 3) и **inode** (фото 2).

1. Программа делает запрос.
    
2. **VFS** ищет путь через **dentry-кэш**.
    
3. **VFS** находит нужный **inode**.
    
4. **VFS** вызывает специфическую функцию конкретной ФС (например, Ext4), чтобы та достала данные из **блоков** (фото 1).

![[Pasted image 20251206133921.png]]

Этот слайд раскрывает техническую реализацию **VFS** через конкретные структуры данных в коде ядра Linux. Здесь мы видим, как именно ядро объединяет разные файловые системы под один стандарт.

---

### 1. Структура `super_block` (Объект ФС)

Если i-узел — это паспорт файла, то **суперблок** — это «паспорт» всей файловой системы (раздела диска).

- **`struct dentry *s_root;`**: Указывает на самый первый, корневой каталог (`/`) этого раздела. Это «входная дверь» в файловую систему.
    
- **`struct super_operations *s_op;`**: Это набор «инструкций» для ядра. Здесь лежат указатели на функции, которые умеют:
    
    - `read_inode`: прочитать паспорт файла с диска.
        
    - `sync_fs`: сохранить все изменения из памяти на диск.
        
- **`struct file_system_type *s_type;`**: Поле, которое говорит ядру: «Я — Ext4» или «Я — NFS».
    

---

### 2. Структура `inode` (Универсальный i-узел)

Этот i-узел уже не просто запись на диске, а объект в **оперативной памяти**, с которым работает VFS.

- **`umode_t i_mode;`**: Хранит две важные вещи: **тип** (файл, папка или ссылка) и **права доступа** (кто может читать/писать).
    
- **`uid_t i_uid;`**: ID владельца файла.
    
- **`struct inode_operations *i_op;`**: Самое интересное! Это методы для работы с **именами** и структурой. Если ты хочешь создать файл (`create`), удалить его (`unlink`) или сделать папку (`mkdir`), VFS вызовет функцию именно отсюда.
    
- **`struct file_operations *i_fop;`**: Это методы для работы с **содержимым** уже открытого файла. Когда ты вызываешь `read()`, `write()` или `lseek()`, ядро лезет в эту таблицу функций.
    
- **`void *i_private;`**: Секретный кармашек для конкретной ФС. Сюда Ext4 или NTFS прячут свои специфические данные, которые не нужны VFS, но важны для них самих.
    

---

### В чем главная идея этого слайда?

**Абстракция через указатели на функции.**

Заметила, что и в `super_block`, и в `inode` есть поля, заканчивающиеся на `_op` (operations)? Это и есть «пульт управления».

**Как это работает на практике:**

1. Ты вызываешь `mkdir("new_folder")`.
    
2. VFS берет `inode` текущей папки.
    
3. Она не знает, как создавать папки на твоем диске, но она видит поле `i_op`.
    
4. VFS просто вызывает `i_op->mkdir()`.
    
5. Если твой диск отформатирован в **Ext4**, сработает функция `ext4_mkdir`. Если это **сетевой диск**, сработает `nfs_mkdir`.
    

---

### Связь со всеми предыдущими слайдами:

Мы прошли путь от структуры на диске до структур в коде.

- **На диске:** Была просто таблица инодов (фото 2).
    
- **В ядре (VFS):** Эта таблица превратилась в живой объект `struct inode`, у которого есть функции-крючки (`i_op`, `i_fop`), позволяющие управлять данными.

---
Вот несколько «киллер-фич» и неочевидных фактов, которые помогут тебе блеснуть знаниями:

### 1. Объектно-ориентированное программирование на языке C

Хотя ядро Linux написано на языке **C** (который не является объектно-ориентированным), этот слайд показывает, как программисты «обманули» систему.

- Поля `i_op` и `i_fop` — это фактически **интерфейсы**.
    
- VFS говорит: «Мне всё равно, кто ты, но если ты хочешь называться файловой системой, у тебя должен быть метод `read`».
    
- Это позволяет добавлять поддержку новых файловых систем, не переписывая код всей ОС.
    

### 2. «Ленивое» удаление (Фишка поля `unlink`)

Благодаря структуре `inode`, в Linux есть крутая фишка: ты можешь удалить файл, который в данный момент смотришь в плеере.

- **Как это работает:** Вызов `unlink` удаляет имя из директории, но в структуре `inode` счетчик ссылок не падает до нуля, пока файл открыт программой.
    
- **Итог:** Файла в папке уже нет, но видео продолжает играть, потому что `inode` в памяти всё еще «жив». Данные исчезнут с диска только после закрытия плеера.
    

---

### 3. Скрытые возможности `i_private`

Поле `void *i_private` — это «черный ящик».

- В **Ext4** там могут храниться данные о специфических блоках.
    
- В **виртуальных ФС** (как `/proc`) там может лежать идентификатор процесса.
    
- Это поле позволяет VFS оставаться универсальной, не вникая в «грязные подробности» конкретных железок.
    

### 4. Файлы, которых не существует (Виртуальные ФС)

С помощью этих структур VFS создает магию папок `/proc` и `/sys`.

- Когда ты открываешь файл `/proc/meminfo`, VFS вызывает метод `read` из `i_fop`.
    
- Но вместо чтения диска, этот метод запускает функцию ядра, которая прямо сейчас опрашивает планки оперативной памяти.
    
- Для тебя это выглядит как текстовый файл, но на диске его **никогда не было**.
    

---

### 5. Почему `i_mode` так важен?

В поле `i_mode` зашифровано не только `rwxr-xr-x`.

- Там хранятся первые 4 бита, которые определяют **тип** файла (папка, обычный файл, сокет, ссылка).
    
- Это позволяет системе мгновенно понимать, можно ли применить к объекту операцию `mkdir` или `readdir`, просто взглянув на одно число в памяти.
    

### 💡 Лайфхак для запоминания:

Если `super_block` — это **Конституция** (главные правила всей страны/раздела), то `inode` — это **Гражданин** (конкретный человек со своими правами и обязанностями). А VFS — это **Государство**, которое следит, чтобы все соблюдали правила, независимо от того, в каком городе они живут.

![[Pasted image 20251206133925.png]]

Этот слайд завершает иерархию объектов VFS в оперативной памяти. Если предыдущий слайд описывал «статику» (ФС и файлы на диске), то этот описывает «динамику» — как мы перемещаемся по папкам и как работаем с уже открытым файлом.

---

### 1. Объект `struct dentry` (Путь)

Мы уже говорили, что **dentry** — это «маршрут». В контексте VFS это связующее звено между именем файла и его «паспортом» (inode).

- **`struct inode *d_inode;`**: Самое важное поле. Это указатель на структуру `inode`, которую мы разобрали на прошлом слайде. Именно здесь имя наконец встречается с данными.
    
- **`struct qstr d_name;`**: Быстрая строка с именем файла. Использование `qstr` (quick string) позволяет ядру очень быстро сравнивать имена при поиске.
    
- **`struct dentry *d_parent;`**: Ссылка на «родителя». Благодаря этому ядро может мгновенно подняться вверх по дереву каталогов (например, при выполнении команды `cd ..`).
    

**Интересная фишка:** `dentry` существуют только в памяти. На диске их нет. Они — как кэш навигаторов, который помнит, что «улица Ленина» ведет к конкретному «дому №5».

---

### 2. Объект `struct file` (Открытый файл)

Это объект, который создается **только тогда, когда программа вызывает `open()`**. Если `inode` один на весь компьютер, то объектов `file` может быть много для одного и того же файла (например, если три разные программы одновременно читают один лог).

- **`struct path f_path;`**: Содержит указатель на `dentry` и информацию о том, куда смонтирована файловая система (`mount`). Это позволяет системе понимать, на каком физическом диске находится файл.
    
- **`struct file_operations *f_op;`**: Это «пульт управления» конкретно для открытого файла. Здесь лежат функции `read`, `write` и `lseek`.
    
- **`loff_t f_pos;`**: **Самое важное поле для работы.** Это текущая позиция («курсор») чтения или записи.
    
    - _Фишка:_ Если две программы открыли один файл, у каждой будет свой объект `struct file` и свой `f_pos`. Одна может читать начало файла, а другая — конец, и они не будут мешать друг другу.
        

---

### Общая цепочка: Как всё работает вместе?

Давай соберем финальный пазл из всех твоих слайдов:

1. **Процесс** (твоя программа) держит **Файловый дескриптор** (просто число).
    
2. Это число указывает на **`struct file`** (где мы сейчас читаем — `f_pos`).
    
3. `struct file` указывает на **`struct dentry`** (как называется файл и где он лежит в дереве папок).
    
4. `struct dentry` указывает на **`struct inode`** (права доступа, владелец и где блоки данных).
    
5. `struct inode` указывает на **`super_block`** (общая информация о диске).
    

---

### Что в этом самого крутого?

Благодаря такой многослойности, Linux может делать невероятные вещи. Например:

- **Переименовать файл**, пока он открыт: изменится только `d_name` в `dentry`, а программа продолжит писать в тот же `inode` через свой `f_pos`, даже не заметив смены имени.
    
- **Монтировать одну ФС внутрь другой**: за это отвечает поле `mount` в `f_path`. Для программы это будет выглядеть как одна большая папка, хотя физически данные лежат на разных дисках.

---

### 1. Объект `file` — это «Личный кабинет» программы

Самая большая путаница происходит между `inode` и `file`.

- **Фишка:** `inode` — это сам файл (один на всю систему). А `struct file` — это **сессия** работы с ним.
    
- **Почему это важно:** Если ты открыла один и тот же лог-файл в двух разных терминалах, у тебя будет **два** объекта `struct file`. В одном ты можешь пролистать в конец (`f_pos` изменится), а в другом остаться в начале. У каждого процесса свой независимый «курсор» чтения.
    

### 2. Зачем нужно `f_path`? (Магия монтирования)

В структуре `file` есть поле `f_path`, которое хранит не только `dentry`, но и `vfsmount`.

- **Подробность:** Это позволяет Linux делать «матрёшку» из файловых систем. Ты можешь «накрыть» папку на жестком диске флешкой.
    
- **Как это работает:** Когда ты открываешь файл, `f_path` точно знает, через какую «дверь» (точку монтирования) ты зашла. Это позволяет системе различать два одинаковых пути, если один из них — это смонтированный сетевой диск.
    

### 3. Быстрые строки `qstr` в dentry

Вместо обычных строк в `dentry` используется `struct qstr`.

- **Фишка:** Она хранит не только имя, но и заранее вычисленный **хэш** этого имени.
    
- **Результат:** Когда ядро ищет файл в огромной папке, оно не сравнивает буквы «c-a-t...», оно сначала сравнивает числа (хэши). Это делает поиск по имени почти мгновенным.
    

---

### 4. Жизнь после смерти (Pinned Dentry)

- **Нюанс:** Даже если ты закрыла файл (объект `file` удалился), соответствующий ему объект `dentry` часто **остается в памяти**.
    
- **Зачем:** Это называется "Dentry Cache". Если ты через секунду снова нажмешь на этот файл, ядру не придется заново опрашивать диск и строить структуру пути — она уже готова и «пришпилена» (pinned) в памяти.
    

### 5. Поле `f_op` может меняться на лету

Это одна из самых мощных фишек VFS.

- **Подробность:** Когда файл открывается, ядро копирует указатели на функции из `i_fop` (из inode) в `f_op` (в объект открытого файла).
    
- **Зачем:** В некоторых случаях ядро может подменить эти функции прямо в процессе работы, чтобы, например, включить специальный режим прямого доступа к диску (Direct I/O), минуя кэш.
    

---

### 🏁 Итоговое резюме всей цепочки:

|**Объект**|**Роль**|**Где живет**|
|---|---|---|
|**`file`**|**Действие**: где сейчас читаем (`f_pos`)|Только в памяти, пока файл открыт|
|**`dentry`**|**Имя**: как называется и кто «родитель»|В памяти (кэш путей)|
|**`inode`**|**Суть**: кто владелец и где блоки данных|На диске + кэш в памяти|
|**`super_block`**|**Закон**: правила всей файловой системы|В начале раздела диска + в памяти|

![[Pasted image 20251206133932.png]]

### Пошаговый разбор открытия `/etc/passwd`

**1. Вызов syscall `open()`** Твоя программа (например, текстовый редактор) говорит: «Хочу файл `/etc/passwd`». Это переключает процессор в режим ядра.

**2. Разбор пути (`namei`)** Ядро не может просто «прыгнуть» к файлу. Функция `namei` (name-to-inode) берет строку и разбивает её на части. Она должна пройти путь: Корень `->` папка `etc` `->` файл `passwd`.

**3. Поиск в dcache (`lookup`)**

Ядро сначала смотрит в свою «быструю память» (тот самый кэш dentry из фото 3). Если ты или кто-то другой недавно открывали этот файл, путь уже там. Это экономит кучу времени.

**4. Связь с Inode** Как только ядро нашло нужный `dentry` для `passwd`, оно смотрит в его поле `d_inode`. Там лежит «ссылка» на паспорт файла — его **i-узел**.

**5. Получение данных об i-узле** Если i-узла еще нет в памяти, вызывается метод `lookup()`. Он лезет на диск, читает таблицу инодов и загружает «паспорт» файла в оперативную память. Теперь ядро знает права доступа и где лежат блоки данных.

**6. Создание сессии (`alloc_file`)** Ядро создает в памяти объект `struct file`. Это та самая «сессия», о которой мы говорили на прошлом слайде. В ней будет храниться твой личный курсор (`f_pos`).

**7. Наследование методов (`f_op = i_fop`)** Это момент «прошивки». Ядро говорит: «Так, этот файл лежит на Ext4, поэтому для чтения мы будем использовать функции из этого i-узла». Теперь объект `file` умеет читать и писать именно так, как требует эта файловая система.

**8. Финальный дескриптор**

Программе нельзя отдавать саму структуру `file` (это небезопасно). Поэтому ядро кладет её в список открытых файлов твоего процесса, а тебе возвращает просто **номер** (например, `3`). Это и есть **файловый дескриптор**.

---

### Главные фишки этого слайда (для ответа):

- **Почему это быстро?** Благодаря пункту 3 (dcache). Если файл популярен, ядро даже не шевельнет головкой диска, всё произойдет в RAM.
    
- **Почему это безопасно?** Пункт 5 проверяет права доступа. Если тебе нельзя читать `passwd`, ядро остановится прямо здесь и вернет ошибку «Permission Denied».
    
- **В чем хитрость дескриптора?** Программа работает с числом `3`, а ядро за этим числом прячет огромную сложную структуру со всеми правами и путями.
    

---

### 💡 Лайфхак для запоминания:

Представь, что ты пришла в библиотеку:

1. **`open("/etc/passwd")`** — ты просишь книгу по адресу.
    
2. **`namei`** — библиотекарь идет по рядам: зал `etc`, полка `passwd`.
    
3. **`dcache`** — библиотекарь вспоминает: «А, я эту книгу только что ставил на место, она еще на тележке!» (быстрый доступ).
    
4. **`struct file`** — тебе выписывают читательский билет на эту книгу.
    
5. **Дескриптор** — тебе выдают номерок. Когда ты скажешь «дай книгу №3», он сразу поймет, о чем речь.

---
## Для +вайбика

### 1. Что такое `/etc/passwd`?

Да, это **классика Linux**. Это не просто пример, это один из важнейших файлов в системе.

- **`/etc/`** — это стандартная директория для всех **конфигурационных файлов** системы. Если тебе нужно изменить настройки сети, пароли или правила загрузки — тебе в `/etc`. Название произошло от "etcetera" (и так далее), потому что раньше туда сваливали всё, что не подходило под другие категории.
    
- **`passwd`** — это текстовый файл, в котором хранится **список всех пользователей** системы.
    
    - _Раньше_ там хранились и сами пароли (в зашифрованном виде), поэтому он так и называется.
        
    - _Сейчас_ там лежат имена пользователей, их ID и путь к их домашним папкам.
        

---

### 2. В чем суть дескриптора? Почему с ним удобнее?

Представь, что `struct file` (про которую мы говорили) — это огромная анкета на 10 листов, где написано: кто открыл файл, когда, на какой позиции сейчас чтение, какие функции использовать для этого диска и т.д.

**Почему нельзя просто отдать эту анкету программе?**

1. **Безопасность:** Если дать программе прямой доступ к структурам ядра, она может случайно (или специально) изменить права доступа или «курсор» другого процесса. Это вызовет крах системы.
    
2. **Сложность:** Программисту пришлось бы каждый раз таскать за собой огромный объект данных.
    

**Решение — Дескриптор (число):** Ядро говорит программе: «Слушай, я всё записало у себя в блокноте под номером **3**. Тебе не нужно знать подробности. Просто когда захочешь почитать файл, скажи мне: "прочитай из номера 3"».

**Преимущества:**

- **Краткость:** Это просто `int` (целое число). Его легко передавать между функциями.
    
- **Абстракция:** Программе всё равно, что за номером 3: реальный файл на диске, видеокамера или сетевое соединение. Команда `read(3, ...)` будет работать одинаково.
    
- **Порядок:** У каждого процесса своя таблица дескрипторов. Твоя "тройка" — это твой файл, а "тройка" другого процесса — его файл. Они не пересекаются.

![[Pasted image 20251206133940.png]]
### Разбор схемы «Связь структур при работе с файлом»

Давай пройдем слева направо, как идет сигнал от твоей программы к железу:

**1. Таблицы дескрипторов (Слева)** Здесь показано три разных процесса (родительский, дочерний и посторонний).

- **Суть:** У каждого процесса свой список «номерков» (дескрипторов).
    
- **Фишка:** Заметь, что дескрипторы родителя и ребенка могут указывать на **одну и ту же** запись в «Таблице открытых файлов». Это происходит, например, при наследовании.
    

**2. Таблица открытых файлов (По центру — `struct file`)** Это системная таблица в памяти ядра.

- Она хранит **Позицию в файле** (`f_pos`).
    
- Она хранит режим (Чтение/Запись).
    
- **Важный момент:** Несколько дескрипторов могут указывать на одну запись здесь, а несколько записей здесь могут указывать на один и тот же **I-узел**.
    

**3. I-узел (Справа — `struct inode`)** Это паспорт файла. Здесь хранятся метаданные:

- **Счетчик связей:** сколько имен у этого файла.
    
- **Uid/Gid:** кто хозяин.
    
- **Размер и Времена:** когда меняли.
    
- **Адреса блоков:** самое интересное — как мы находим данные.
    

---

### Подробно про «Указатели на блоки» (Нижний правый угол)

Это классическая схема файловой системы **Ext2/3**. Чтобы файл мог быть и маленьким, и огромным, Linux использует систему «матрешки»:

1. **Прямые указатели (первые 12 блоков):** Если файл крошечный (до 48 Кб), адреса блоков лежат прямо в i-узле. Это максимально быстро.
    
2. **Однократный косвенный блок:** Если файл больше, i-узел указывает на блок, который сам забит «адресами» других блоков.
    
3. **Двукратный/Трехкратный:** Это уже для гигантских файлов. Один указатель ведет на блок, тот на второй, тот на третий — и только там лежат данные. Это позволяет адресовать терабайты данных.

В Linux дочерний процесс (ребенок) при создании является «копией» родителя. Поэтому они действительно **делят одну и ту же запись** в «Таблице открытых файлов» (System-wide Open File Table).

### Как это работает на самом деле:

1. **Общий «курсор» (позиция):** Если родитель прочитает 10 байт, то у ребенка позиция в файле (`f_pos`) тоже сдвинется на 10 байт. Они как будто пьют один коктейль через две соломинки: если один отхлебнул, в стакане стало меньше для обоих.
    
2. **Изменения (Запись):** Если один из них начнет вносить изменения (записывать данные), они **все равно будут использовать одну запись** в таблице. Запись одного не создаст «отдельную копию» таблицы. Они просто будут писать в один и тот же файл, используя общую позицию.
    
3. **Когда записи разделяются?** Только если один из процессов **закроет** этот файл и **откроет его заново** сам. Тогда ядро создаст для него новую, личную запись в «Таблице открытых файлов» со своим собственным курсором.

### Почему это сделано именно так? (Фишка)

Это сделано для того, чтобы родитель и ребенок могли **согласованно** писать в один лог-файл или терминал. Если бы у каждого сразу был свой «курсор», они бы просто затирали данные друг друга, печатая поверх текста соседа.


Связь родителя и ребенка в контексте файлов — это одна из самых изящных и в то же время опасных зон в Linux. Давай разберем это «на пальцах», чтобы ты понимала механику процесса `fork()` (создания дочернего процесса).

### Подробная связь: Родитель + Ребенок

Когда вызывается `fork()`, ядро не просто копирует переменные программы, оно **копирует таблицу дескрипторов**.

- **Копирование дескрипторов:** Если у родителя был открыт файл с дескриптором `3`, то у ребенка **тоже** появится дескриптор `3`.
    
- **Указатель на ту же запись:** Оба этих дескриптора (и у папы, и у сына) указывают на **один и тот же объект `struct file`** в системной таблице открытых файлов.
    
- **Разделяемый курсор (`f_pos`):** Поскольку объект `struct file` один на двоих, поле `f_pos` (текущая позиция) у них общее.
    

> **Пример:** Родитель прочитал 50 байт из файла. Курсор сдвинулся на 50. Если следом ребенок вызовет `read()`, он начнет читать **с 51-го байта**, а не с начала. Они буквально «доедают» файл друг за другом.
---

### 😈 Твои обещанные коварные вопросы

Эти вопросы помогут тебе «завалить» любого, кто скажет, что понимает Linux, но не смотрел на эту схему:

1. **«Могут ли два разных процесса читать один файл с разных мест одновременно?»**
    
    - **Ответ:** Да. У каждого процесса свой дескриптор, который ведет к **разным** записям в «Таблице открытых файлов». Значит, у каждого свой `f_pos` (позиция), хотя `inode` у них общий.
        
2. **«Что будет с данными на диске, если мы удалим файл, но счетчик связей в i-узле еще равен 1 (например, из-за жесткой ссылки в другой папке)?»**
    
    - **Ответ:** Данные останутся на месте. I-узел и блоки данных удаляются только тогда, когда **Счетчик связей** становится равен 0 и файл никем не открыт.
        
3. **«Почему в i-узле всего 12 прямых указателей, а не, скажем, 1000?»**
    
    - **Ответ:** Для экономии места. Большинство файлов в системе — очень маленькие. Делать i-узел огромным ради редких больших файлов — значит впустую тратить место на диске под таблицу инодов.
        
4. **«Если я изменю права доступа (chmod) к файлу через дескриптор родительского процесса, изменятся ли они для постороннего процесса?»**
    
    - **Ответ:** Да. Потому что права доступа лежат в **i-узле**, а он общий для всех, кто обращается к этому файлу.
---
### 😈 Коварные вопросы (проверь себя)

Эти вопросы часто задают, чтобы понять, понимает ли человек суть, или просто вызубрил слайды.

**Вопрос №1: «Что будет, если процесс откроет файл /etc/passwd, получит дескриптор №3, а потом другой процесс удалит этот файл с диска?»**

- **Ответ:** Процесс **спокойно продолжит читать файл**. Почему? Потому что `d_inode` и блоки данных в памяти всё еще «живы», пока на них ссылается открытый объект `file`. Файл исчезнет с диска физически только тогда, когда процесс закроет дескриптор №3 (последняя ссылка пропадет).
    

**Вопрос №2: «Могут ли два разных дескриптора (например, 3 и 4) указывать на один и тот же i-узел?»**

- **Ответ:** Да! Например, если ты открыла один и тот же файл дважды. У тебя будет два объекта `file` (с разными позициями чтения `f_pos`), но они оба будут указывать на один и тот же `dentry` и `inode`.
    

**Вопрос №3: «Где хранится имя файла: в i-узле или в директории?»**

- **Ответ (самый важный):** Только в **директории** (в структуре `dirent`). i-узел вообще не знает, как называется файл, он знает только его "паспортные данные" и где лежат кирпичи (данные). Именно поэтому у одного i-узла может быть много имен (жесткие ссылки).
    

**Вопрос №4: «Зачем нужен шаг lookup в dcache, если можно сразу лезть на диск?»**

- **Ответ:** Скорость. Обращение к оперативной памяти (кэшу) в **тысячи раз быстрее**, чем поиск по магнитному или даже SSD диску. Без dcache каждое открытие папки заставляло бы компьютер "заикаться"

**Вопрос №6: «Мы знаем, что `dentry` ускоряет поиск. А что произойдет, если мы переименуем файл (сделаем `rename`), пока родитель и ребенок его читают?»**

- **Ответ:** Ничего страшного не произойдет. Процессы продолжат читать данные. Почему? Потому что их объекты `file` уже указывают на конкретный `inode`. Имя файла (которое хранится в `dentry`) нужно было только в момент открытия (`open`). После открытия системе «плевать» на имя, она работает напрямую с «паспортом» (inode).
    

**Вопрос №7: «Может ли возникнуть ситуация, когда у родителя дескриптор №3 указывает на один файл, а у ребенка дескриптор №3 — на совсем другой?»**

- **Ответ:** Да, но **только если** ребенок после создания закроет старый дескриптор (`close(3)`) и откроет новый файл. Или если ребенок запустит другую программу через `exec()`. В момент самого `fork()` они всегда идентичны.
    

**Вопрос №8: «Посмотри на схему с блоками (прямые и косвенные указатели). Где физически на диске хранится информация о том, что файл — это "символическая ссылка" (symlink)?»**

- **Ответ:** Если путь ссылки короткий, он хранится прямо в i-узле (вместо списка блоков). Если путь длинный — выделяется отдельный блок данных. Это еще одна крутая оптимизация VFS.

### Магия `fork()` + `exec()` (Замена кода)

Это классическая ситуация в Linux (так работает командная строка/шелл). **Сценарий:** Родитель открыл файл, сделал `fork()`, а потом ребенок вызвал `exec()` (заменил свою программу на другую, например, на `ls` или `grep`).

- **Дескрипторы сохраняются:** По умолчанию при вызове `exec()` таблица дескрипторов **не очищается**. Новый код в дочернем процессе «наследует» тот же номер дескриптора (например, 3), который был у родителя.
    
- **Связь не рвется:** Новый код всё так же указывает на ту же самую `struct file` в ядре.
    
- **Общий курсор:** Даже после замены кода родитель и ребенок всё еще **делят одну и ту же позицию** в файле.
    

> **Исключение:** Есть специальный флаг `O_CLOEXEC` (close-on-exec). Если родитель открыл файл с этим флагом, то при вызове `exec()` дескриптор у ребенка автоматически закроется. Это нужно для безопасности, чтобы «чужая» программа случайно не получила доступ к секретным файлам родителя.

## Список литературы

https://gemini.google.com/share/90d74383728d