
![[Pasted image 20251208234845.png]]
Процессор исполняет **машинный код**, который определяется его **ISA**. Все последующие темы (микрооперации, конвейеры, кэши) — это механизмы, которые процессор использует для **быстрого и эффективного выполнения** этого самого машинного кода.

![[Pasted image 20251209000731.png]]
·  **Компиляция** — это процесс перевода всей программы заранее, до запуска, в машинный код (набор инструкций, который напрямую исполняет процессор). Результат — исполняемый файл (.exe, ELF и т.д.). После компиляции программа запускается быстро, потому что перевод уже выполнен.

·  **Интерпретация** — перевод и исполнение происходят по ходу: интерпретатор читает одну строку/инструкцию высокого уровня, переводит её «на ходу» и сразу выполняет. Это похоже на устный переводчик в реальном времени. Проще в разработке, но обычно медленнее.

### Как высокоуровневый код становится машинным: этапы 

1. **Исходный код** (`int c = a + b;` в C).
2. **Препроцессинг** (для C/C++ — макросы, include).
3. **Компиляция** — перевод из языка высокого уровня в **ассемблер**  или в промежуточное представление.
4. **Ассемблер (assembler)** — перевод ассемблера в **объектный код** (машинный код в виде секций, ещё без связки).
5. **Линковка (linker)** — связывание объектных файлов и библиотек в **исполняемый образ** (формирует адреса, таблицы символов, разрешает внешние вызовы).
6. **Загрузка (loader)** — загрузчик операционной системы помещает программу в память, настраивает адреса и начинает выполнение.
(Если интерпретатор — многие этапы происходят динамически.)

### Что такое ISA (Instruction Set Architecture)?
Примеры ISA: **x86**, **ARM**, **MIPS**, **RISC-V**.

ISA (Instruction Set Architecture) — это набор инструкций, который определяет, какие операции может выполнять процессор. 

Процессор, чтобы выполнить задачу, должен быть запрограммирован на выполнение определённых команд. Эти команды называются инструкциями, и каждая инструкция имеет свой уникальный битовый паттерн — последовательность единиц и нулей. Например, в архитектуре x86 инструкция для сложения двух чисел может быть закодирована определённым набором битов, а в архитектуре ARM — другим.

Архитектуры процессоров, как x86, ARM, MIPS и RISC-V, имеют свои уникальные ISA. Одна и та же программа, написанная на высокоуровневом языке, будет компилироваться в машинный код, но каждая архитектура будет иметь свой набор инструкций, на основе которого этот код будет выполнен.

**Простая программа на ассемблере**
```assambler
mov eax, 5    ; Положить число 5 в регистр EAX
add eax, 3    ; Прибавить 3 к EAX
```

**Как это выглядит в машинном коде разных ISA**

**x86-64 (Intel/AMD в ПК)**

```
Машинный код: B8 05 00 00 00   83 C0 03
(это 11 байт в памяти)
```
_Особенность x86:_ Команды переменной длины, есть много сложных инструкций.
**ARM (Apple M1/M2/M3, смартфоны)**

```
Машинный код: 40 00 80 D2   63 10 00 91
(это 8 байт в памяти)
```

Кроме того, даже если два процессора используют одинаковый ISA, их внутреннее устройство (микроархитектура) может сильно различаться. Например, один процессор может иметь более быстрые каналы связи между компонентами, другой — более мощные кэш-памяти. Эти различия могут влиять на производительность программы, даже если обе системы используют одну и ту же инструкцию.

**Микроархитектура** — это **конкретная аппаратная реализация** архитектуры набора команд (ISA), определяющая внутреннюю структуру и организацию процессора для выполнения команд этой ISA.

### Практическая демонстрация различий

###  Ситуация 1: Совместимость (зависит от ISA)

- У вас есть **файл game.exe**, скомпилированный для x86-64
- Он запустится на: Intel Core i5, AMD Ryzen, старом AMD Athlon 64 (2003 год)
- Он **НЕ запустится** на: Apple Macbook M2, Raspberry Pi, Android-смартфоне
- **Почему?** Потому что у них разная ISA (ARM vs x86)
### Ситуация 2: Производительность (зависит от микроархитектуры)

- Возьмем **один и тот же тест** Cinebench R23
- Запустим на:
    1. **Intel Core i5-12600K** (микроархитектура Alder Lake, 2021)
    2. **Intel Core i5-10600K** (микроархитектура Comet Lake, 2020)
    3. **Intel Core i5-8600K** (микроархитектура Coffee Lake, 2017)
- **Результат:** Все три процессора поймут команды теста (одинаковая ISA x86-64), но покажут **разную производительность** из-за разных микроархитектур!
### Микрооперации (µOPs)

Микрооперации — это более мелкие операции, на которые разбиваются сложные инструкции. Процессор может разбить, например, одну команду сложения двух чисел в несколько операций, таких как извлечение чисел из памяти, их сложение и запись результата обратно в память.

Разбиение инструкций на микрооперации помогает процессору **оптимизировать выполнение** команд. Разные микрооперации могут быть выполнены параллельно в разных частях процессора. Например, одна микрооперация может быть выполнена в одном исполнительном блоке, а другая — в другом, что позволяет ускорить выполнение программы. Микрооперации также позволяют процессору эффективнее использовать свои ресурсы. Например, если одна команда требует выполнения нескольких шагов, процессор может начать работать над следующими командами, не дожидаясь завершения всех операций первой. Простые, унифицированные микрооперации легче и быстрее обрабатывать на специализированных блоках (например, ALU или AGU).

![[Pasted image 20251208235329.png]]

Инструкция ADD (M32, I32) — это пример сложной операции, которая велит процессору **сложить 32-битное значение из памяти (****M32****) с 32-битным непосредственным значением (****I32****) и сохранить результат обратно в память (****M32****)**. 

Разбивая инструкцию на μOPs, процессор может: Отправлять задачи на специализированные блоки (AGU для адресов, ALU для арифметики).

![[Pasted image 20251209010125.png]]

## Конвейер 

**Конвейер** — это способ организации работы процессора, который позволяет ускорить обработку инструкций. В отличие от последовательного выполнения, где каждая инструкция выполняется полностью, одна за другой, конвейер разделяет выполнение инструкций на несколько этапов и позволяет процессору одновременно обрабатывать несколько инструкций. (на ранних этапах работал только с инструкциями как в примерах, сейчас работает с микрооперациями)
![[Pasted image 20251209010318.png]]
![[Pasted image 20251209010938.png]]
#### Преимущества конвейера

- **Параллелизм**: Разделение работы на этапы позволяет процессору выполнять несколько инструкций одновременно. Например, пока процессор обрабатывает инструкцию на стадии выполнения, он может загружать следующую инструкцию на этапе загрузки.
    
- **Увеличение производительности**: Каждый этап конвейера может работать на разных инструкциях одновременно, что снижает общее время выполнения программы. Процессор не стоит в ожидании завершения одной инструкции, а сразу же начинает работать с другой.

Когда процессор выполняет инструкции в конвейере, возникает ситуация, когда одна инструкция зависит от результата предыдущей. Эти зависимости могут замедлить процесс, и процессор должен каким-то образом их обрабатывать. Это называется **data hazard** или "опасность данных".
### Типы **data hazard**

1. **RAW (Read After Write)** — Чтение после записи.  
    Это самая распространённая зависимость, когда одна инструкция пытается использовать данные, которые ещё не записаны в регистр или память предыдущей инструкцией.
    
    **Пример**:  
    Допустим, у нас есть две инструкции:
    
     ``` cpp
     ADD R1, R2, R3  // R1 = R2 + R3
     MUL R4, R1, R5  // R4 = R1 * R5
       ```
    
    Здесь вторая инструкция `MUL` зависит от результата первой инструкции `ADD`, потому что она использует значение, которое будет записано в регистр R1. Однако вторая инструкция не может начать выполнение, пока первая не завершит запись в R1. 
    
2. **WAR (Write After Read)** — Запись после чтения.  
    Это ситуация, когда одна инструкция пытается записать в регистр, который ещё используется другой инструкцией для чтения данных.
    
    **Пример**:
    ```cpp
    ADD R1, R2, R3  // R1 = R2 + R3
    SUB R2, R1, R4  // R2 = R1 - R4
    ```
    
    В этом случае, если вторая инструкция пытается записать в регистр R2, пока первая инструкция ещё читает его, это создаёт опасность. Во время выполнения инструкции ADD значение в R2 может измениться, что сделает второй шаг некорректным.
    
3. **WAW (Write After Write)** — Запись после записи.  
    Это ситуация, когда две инструкции пытаются записать данные в один и тот же регистр, и порядок записи имеет значение.
    
    **Пример**:
    ```cpp
     ADD R1, R2, R3  // R1 = R2 + R3
     MUL R1, R4, R5  // R1 = R4 * R5
    ```
    
    Здесь обе инструкции пытаются записать результат в регистр R1. Проблема возникает, если процессор не гарантирует, что первая инструкция завершит запись до того, как вторая инструкция начнёт свою запись в R1. Если это не контролировать, то результат может быть неправильным.

### Как процессор решает эти проблемы?

Процессоры используют различные техники для минимизации или устранения этих проблем. Рассмотрим несколько распространённых методов.

#### 1. **Стратегия "стопор" (Stall cycles)**

Чтобы избежать проблемы **RAW**-опасности, процессор может **поставить паузу** (stall) в конвейере, чтобы дождаться завершения выполнения инструкции, которая записывает данные. Это означает, что другие инструкции, которые зависят от данных, будут временно "остановлены", пока не будут готовы нужные данные.

**Пример**:  
Возьмём те же две инструкции:

`1. ADD R1, R2, R3 2. MUL R4, R1, R5`

Процессор увидит зависимость между `ADD` и `MUL`, и вместо того, чтобы сразу начать выполнять `MUL`, он поставит её на паузу. Инструкция `MUL` будет ждать, пока не будет завершена запись в регистр R1. Это приводит к дополнительным задержкам, но предотвращает ошибки.

#### 2. **Forwarding (перенаправление данных)**

Вместо того чтобы ставить паузу, процессор может использовать технику **forwarding** (или **bypassing**). В этом случае процессор перенаправляет данные сразу после выполнения инструкции, минуя запись в регистр, если данные нужны другой инструкции.

**Пример**:  
Возьмём те же две инструкции:

`1. ADD R1, R2, R3 2. MUL R4, R1, R5`

Если вторая инструкция (`MUL`) зависит от данных, которые только что были вычислены в первой инструкции (`ADD`), процессор может передать результат сложения прямо в исполнение умножения, минуя стадию записи в регистр R1. Это позволяет ускорить выполнение и избежать пауз в конвейере.

#### 3. **Предсказание зависимостей**

Процессор может использовать **предсказание зависимостей** для того, чтобы заранее понять, когда могут возникнуть проблемы с зависимостями и пытаться решить их ещё до того, как они возникнут. Например, если процессор может заранее оценить, что в будущем будет зависимость, он может подготовить механизмы для её обработки.

### Пример с паузами и перенаправлением

Допустим, у нас есть следующая последовательность инструкций:

`1. ADD R1, R2, R3   // R1 = R2 + R3 2. MUL R4, R1, R5   // R4 = R1 * R5`

1. На этапе **Fetch** обе инструкции загружаются в конвейер.
    
2. На этапе **Decode** процессор анализирует, что `MUL` зависит от результата `ADD` (он использует R1, который будет записан только после выполнения `ADD`).
    
3. На этапе **Execute** для `ADD` происходит сложение, а для `MUL` пока не происходит умножения, потому что данные для R1 ещё не готовы. Процессор ставит **паузы** (stall) в конвейере, пока не получит данные из `ADD`.
    
4. Альтернативно, процессор может **перенаправить** результат выполнения `ADD` сразу в этап выполнения `MUL`, минуя стадию записи в регистр R1.
    

Таким образом, эти техники позволяют минимизировать потери времени в конвейере и ускорить выполнение программы.
![[Pasted image 20251209012336.png]]

Четыре инструкции (обозначенные разными цветами: **красный, синий, фиолетовый и зелёный**) проходят через **четыре этапа** конвейера. **Без конвейера:** Выполнение 4 инструкций заняло бы 4×4=16 тактов (так как каждая инструкция проходит 4 этапа).

### **Структура диаграммы**

- **Ось X (Горизонтальная):** Отмечены **Clock cycle (такты процессора)**, от 0 до 8. Это единицы времени, в которых процессор выполняет работу.
    
- **Центральная часть ("Pipeline"):** Представляет **четыре стадии (этапа)** конвейера:
    
    - Stage 1: **Fetch (Загрузка)**.
    - Stage 2: **Decode (Декодирование)**
    - Stage 3: **Execute (Выполнение)**.
    - Stage 4: **Write-back (Запись результата)**.
        
- **Верхняя часть ("Waiting instructions"):** Инструкции, которые **ждут** своей очереди для входа в конвейер.
- **Нижняя часть ("Completed instructions"):** Инструкции, которые **завершили** все этапы конвейера.
![[Pasted image 20251209012853.png]]
###  Принцип Специализации и Портов

·        **Специализированные Блоки:** Процессор не выполняет всю работу одним блоком. Вместо этого, внутри ядра существует множество **специализированных исполнительных блоков** (functional units), каждый из которых "заточен" под свой тип работы.

·        **Порты:** Эти блоки **подключены к портам**. **Порт** — это логический канал, через который микрооперация (μOP) отправляется на конкретный исполнительный блок. Процессор выступает в роли **диспетчера**, который распределяет (μOPs) по этим портам.

### Суперскалярные процессоры

**Суперскалярность** означает, что процессор может одновременно выполнять несколько инструкций, используя для этого несколько портов и блоков исполнения. В суперскалярных процессорах несколько операций могут выполняться параллельно, если они независимы друг от друга. Это достигается за счёт того, что разные микрооперации могут быть распределены по нескольким портам.

Например:

- Порт 0 может выполнить операцию сложения (ADD), используя ALU.
    
- Порт 1 может вычислить адрес для операции загрузки (LOAD), используя AGU.
    
- Порт 2 может выполнить умножение (MUL), если есть отдельный блок для работы с вещественными числами.
    

Если все эти операции независимы, процессор может выполнять их одновременно, что ускоряет выполнение программы в целом.

**Конвейер** (pipeline) ≠ **суперскалярность** (superscalar).  
Это **разные уровни параллелизма** — и они **могут существовать независимо**.

Конвейер — это **разделение выполнения одной инструкции** на несколько стадий (Fetch, Decode, Execute, Memory, Write-back), чтобы разные стадии могли выполняться **одновременно**, но **над разными инструкциями**.
**Суть:** Конвейер позволяет достичь **параллелизма на уровне инструкций**, но только **последовательно** (т.е. инструкция 1 на этапе D, инструкция 2 на этапе F).

Суперскалярный процессор идёт **на шаг дальше**:  
он имеет **несколько конвейеров или портов**, и может запускать **несколько инструкций в одном такте**, если они независимы.

**Суть:** Суперскалярность позволяет достичь **параллелизма на уровне микроопераций (μOPs)**.

·        Конвейер — делит **выполнение одной инструкции** на этапы.

·        Суперскалярность — позволяет **запускать несколько инструкций одновременно** (на разных ALU, AGU и т.д.).

![[Pasted image 20251209020552.png]]
### Пример в сравнении

#### 🔸 Конвейерный (не суперскалярный) процессор:

```
ADD → выполняется (в конвейере) 
SUB → ждёт своей очереди
```
#### 🔸 Суперскалярный процессор:
```
ADD → идёт в ALU через порт 0 
SUB → одновременно идёт в другой ALU через порт 1
```
 Оба выполняются **в один и тот же такт**, если не зависят друг от друга.

**Суть:** Суперскалярность позволяет достичь **параллелизма на уровне микроопераций ($\mu$OPs)**.
Конвейер позволяет достичь **параллелизма на уровне инструкций**, но только **последовательно**

![[Pasted image 20251209012927.png]]

### 1. Суперскалярность (Superscalar)

Как мы обсуждали ранее, суперскалярность — это архитектурная особенность, которая расширяет возможности конвейера.

- **Суть:** Процессор имеет **несколько исполнительных блоков (портов)** (ALU, AGU и т.д.) и может **обрабатывать несколько инструкций** (или их $\mu$OPs) **параллельно** в один и тот же момент времени.
    
- **Параллелизм:** Процессор стремится достичь **Instruction Level Parallelism (ILP)** — параллелизма на уровне инструкций. Он ищет независимые $\mu$OPs и отправляет их одновременно в разные порты.
    

---

### 2. Выполнение Вне Порядка (Out-of-Order Execution, OOO)

Это мощный метод, который позволяет процессору **скрывать задержки (latency)**, возникающие из-за зависимостей или медленного доступа к памяти.

- **Проблема:** В традиционном конвейере, если инструкция 1 (I1) ждет данные из памяти (что занимает много времени) или результат вычислений от предыдущей инструкции, весь конвейер **останавливается** (происходит **"затык"** или **"stalling"**).
    
- **Решение OOO:** Если **одна инструкция (I1) ожидает данные**, процессор не останавливается. Вместо этого он:
    
    - Смотрит вперед в поток команд.
        
    - Находит **следующую инструкцию (I2), которая НЕ зависит** от результата I1.
        
    - Начинает **выполнение I2**.
        
- **Итог:** Инструкции могут быть выполнены **не в том порядке, в котором они были написаны** в программе (или в котором они поступили на декодирование), но результат всегда будет **логически правильным**, как если бы они выполнялись последовательно (это называется **Retirement in Order**).
    

### **3. Диспетчер (Scheduler/Dispatcher)**

Для управления этим сложным процессом суперскалярного выполнения вне порядка необходим специальный компонент.

- **Название:** **Scheduler** (Диспетчер или Планировщик).
    
- **Функция:** Это "мозг" стадии Execute. Его задача — **анализировать зависимости** между всеми ожидающими $\mu$OPs.
    
- **Распределение:** Он распределяет **готовые к исполнению** $\mu$OPs по **доступным исполнительным блокам (портам)**, чтобы максимально использовать все ресурсы CPU и **минимизировать простои**.
    

### **📝 Краткая пометка: Связь с темой**

**Суперскалярность** обеспечивает **ширину** (много портов), а **Out-of-Order Execution**, управляемое **Scheduler**'ом, обеспечивает **глубину** (умное использование портов) для достижения максимального параллелизма и скорости. Без этих двух технологий современные процессоры не могли бы достичь своей производительности.

![[Pasted image 20251209021334.png]]
![[Pasted image 20251209021346.png]]

**Регистры** — это крошечные ячейки памяти внутри процессора.  
Они находятся **ближе всего к вычислительным блокам (ALU, AGU)** и работают **намного быстрее**, чем оперативная память (RAM).

Обычно у процессора **ограниченное количество регистров**, например:
·        у архитектуры x86 — 16 общих регистров (RAX, RBX, RCX, RDX и т.д.),
·        у ARM — 32.

## Почему возникают конфликты между регистрами

Когда процессор выполняет несколько инструкций **одновременно** (благодаря конвейеру и суперскалярности),  
может возникнуть **проблема: разные инструкции используют один и тот же регистр**.

 **Пример:**

```
1. ADD R1, R2, R3   // R1 = R2 + R3 
2. MUL R1, R4, R5   // R1 = R4 * R5
```

 Здесь обе инструкции записывают результат в **один и тот же регистр R1**.

Если процессор попытается выполнить их параллельно:

- вторая инструкция может **перезаписать R1**,
- а первая ещё не успела завершиться,
- результат первой потеряется.
Такой конфликт называется **WAW (Write After Write)** — "запись после записи".

Чтобы **избежать этих конфликтов**, процессор не использует напрямую “логические” регистры, указанные в коде (R1, R2, R3 и т.п.).  
Вместо этого он внутри себя создаёт **физические регистры** — временные копии.

 Идея:  
Каждое использование регистра (например, `R1`) при исполнении инструкции получает **уникальное физическое имя**, например `P1`, `P2`, `P3` и т.д.  
Так, даже если несколько инструкций пишут в “один и тот же логический регистр” (например, R1), на самом деле они используют **разные физические регистры**.

 **Пример:**
```
1. ADD R1, R2, R3   → использует физический регистр P1 
2. MUL R1, R4, R5   → использует физический регистр P2
```

То есть:

- Обе инструкции работают **с “логическим” R1**, но внутри процессора это **разные копии** — `P1` и `P2`.
- Конфликта нет, и процессор может выполнять их **параллельно**.
- После завершения второй инструкции, её результат (`P2`) просто “переименуется” обратно в логический `R1`.
### Роль регистрового файла

Чтобы всё это работало, процессор хранит **таблицу сопоставлений** между логическими и физическими регистрами —  
это и есть **регистровый файл (register file)** или **таблица переименований**.

![[Pasted image 20251209022259.png]]

![[Pasted image 20251208235414.png]]

### 1. Проблема: Ветвления и Конвейер

- **Что такое ветвление?** В программе это операторы `if/else`, циклы `for/while`, вызовы функций. Они заставляют поток инструкций **разветвляться**.
    
- **Конфликт с конвейером:** Чтобы конвейер работал эффективно, ему нужно знать, какую следующую инструкцию (**Fetch**) нужно загрузить. Но если текущая инструкция в конвейере — это `if (условие)`, процессор не знает, куда идти дальше (в ветку `if` или `else`), пока не выполнит условие на стадии **Execute** (Выполнение).
    
- **Задержка:** Пока результат условия не известен, процессор должен ждать или "гадать". Если он ждет, конвейер **простаивает**.
    

### 2. Решение: Предсказание Ветвлений

- **Суть:** Вместо того чтобы ждать, процессор использует специализированный аппаратный блок (**Branch Predictor**) для **"угадывания"** результата ветвления.
    
- **Как это работает:**
    
    - Процессор **пытается "угадать"**, какой путь программы (например, ветка `if`) будет выполнен.
    - Он **начинает загружать и выполнять** соответствующие инструкции **заранее** (спекулятивно).

### 1. **Процессор делает предположение**

Он анализирует предыдущие выполнения программы.  
Если, например, условие `x > 0` чаще всего **выполняется**, то он “запомнит”,  
что обычно переход совершается **в ветку if**, и в следующий раз предскажет тот же путь.
### 2. **Начинает загрузку и исполнение**

Пока реальный результат условия ещё не готов,  
процессор **заранее подгружает инструкции** из предполагаемой ветки  
и **запускает их в конвейер**.
### 3. **Проверка**

Когда вычисление условия готово,  
процессор **сравнивает предсказание с реальностью**:

- ✅ Если **предсказание было верным**, всё хорошо — работа продолжается без задержки.
    
- ❌ Если **ошибся**, процессор **откатывает** все выполненные заранее микрооперации (как будто их не было)  и **начинает заново** выполнять правильную ветку.

·        Предсказание ветвлений — одна из **ключевых частей современного суперскалярного процессора**.  Без него конвейер простаивал бы при каждом `if`, `for`, `while`.

·        Реализуется через специальные блоки: **Branch Predictor** и **Branch Target Buffer (BTB)**,  
которые хранят статистику и адреса прошлых ветвлений.

·        Современные процессоры угадывают направление **с точностью >95%** (!)

## Подробная Блок-Схема Архитектуры Процессора.
(тяжело и ненадобно)

Эта схема — визуализация **конвейера** и **выполнения вне порядка (OOO Execution)**. Главная цель всех этих блоков — как можно быстрее выполнить программу, используя **параллелизм**.

**Общая логика:**

1.     **Загрузка:** Инструкции (x86) быстро загружаются из кэша (L1-I) и прогнозируются (Branch Prediction).

2.     **Преобразование:** Сложные инструкции разбиваются на простые **микрооперации (μOPs)**.

3.     **Диспетчеризация OOO:** μOPs переименовываются (RAT) для устранения зависимостей и отправляются в центральный буфер (ROB), который управляет их **выполнением вне порядка**.

4.     **Исполнение:** μOPs распределяются по множеству **исполнительных портов** (Суперскалярность) и выполняются специализированными блоками (ALU, AGU).

5.     **Фиксация:** Результаты, выполненные вне порядка, **фиксируются по порядку** (Retirement), чтобы гарантировать правильность программы.

## Описание Основных Блоков

Процессор можно разделить на три функциональные группы:

![[Pasted image 20251209023403.png]]
![[Pasted image 20251209023420.png]]
![[Pasted image 20251209023431.png]]
![[Pasted image 20251209023442.png]]
![[Pasted image 20251209023453.png]]
![[Pasted image 20251209023458.png]]

![[Pasted image 20251208235425.png]]
![[Pasted image 20251208235429.png]]
# _1) Вход: I-cache, TLB и предвыборка инструкций_

_В_ _левом_ _верхнем_ _углу_ _— блок_ _«Quadruple associative instruction cache 32 KB…»Это L1-инструкционный кэш (32 КБ, 4-ассоциативный). Он хранит машинный код инструкций, чтобы выборка (fetch) была быстрой и местная. Рядом — TLB (128-entry TLB-4K и 7 TLB-2/4M per thread) — таблица трансляции виртуальных адресов в физические; нужна для быстрой адресации инструкций (и данных)._

_Надо понимать: доступ к памяти програмного кода сначала пытается прочитать из L1 I-cache; при промахе идёт L2/L3/память (uncore). Чтобы снизить задержки промахов, есть **prefetch buffer (16 bytes)** — аппаратная предзагрузка следующих частей кода. Также на схеме стоит **predecode & instruction length decoder** — аппарат, который заранее помечает границы x86-инструкций и их длину (x86 переменной длины), чтобы последующие декодеры могли работать быстрее._

# _2) Предсказатель переходов (Branch prediction)_

_Параллельно с выборкой находится блок **Branch prediction (global/bimodal, loop, indirect jump)**._

·        _Предсказатель решает, какой путь по коду дальше взять при условных и безусловных переходах._

·        _Используются разные механизмы: глобальный (учёт истории), бимодальный (простая таблица), специализированный для циклов (loop predictor) и отдельный для косвенных прыжков.  
Почему это важно: без хорошего предсказателя конвейер часто простаивает из-за неправильного ветвления, и проценты промахов напрямую влияют на производительность. На схеме стрелка идёт назад в front-end — это потому что при промахе предсказателя фронтенд должен сбросить неактуальные микрооперации._

# _3) Front-end: декодирование и очередь инструкций_

_Далее_ _— **Instruction queue / predecoder / complex & simple decoders**. В x86 большинство инструкций сначала попадают в декодер, который превращает сложные CISC-инструкции в внутренние RISC-подобные микрооперации (uops)._

_В схеме видно: есть **complex decoder** (для редких/сложных инструкций) и несколько **simple decoders** (быстрые параллельные пути). Также указана **alignment / macro-op fusion** — механизм объединения двух последовательных простых инструкций (например CMP+Jcc) в одну макро-операцию на ранних стадиях, чтобы экономить ресурсы при исполнении._

_После декодирования формируется **Decoded instruction queue (28 u-op entries)** — очередь уже декодированных микроопераций, из которой они будут подаваться дальше._

# _4) Микроопы, rename и ROB_

_Перед исполнением у x86-ядра есть фаза раннэйма (register renaming), хотя на схеме прямо видно: **2 x register allocation table (RAT)** и **Reorder buffer (128-entry) fused** + **2x Retirement register file**._

·        **_Register renaming (RAT)_** _убирает ложные зависимости между инструкциями (WAW, WAR) путём отображения архитектурных регистров в физические._

·        **_Reorder Buffer (ROB)_** _хранит информацию о порядке завершения инструкций — важен для реализации спекулятивного и out-of-order выполнения: инструкции выполняются вне порядка, но устаревшие результаты коммитятся (retire) строго в программном порядке. В случае неправильного предсказания ветвления ROB используется для отката._

_На схеме также «micro-instruction sequencer» — часть фронтенда, выдающая сложные микрокоды или последовательности микрокоманд для специфичных инструкций._

# _5) Диспетчер и резервные станции (Reservation station)_

_Из ROB/RAT микроопы передаются в **Reservation station (128-entry fused)** — буфер ожидания для операций, готовых исполниться, но ожидающих доступных функциональных блоков или данных операндов._

_Reservation station — это фактически окно out-of-order исполнения: инструкции могут ждать своих операндов, и как только операнды готовы, инструкция назначается на свободный execution port._

# _6) Исполнительные порты и функциональные блоки_

_Внизу внутри серой области — набор портов (Port 0..Port 7) и за ними — функциональные блоки:_

·        **_AGU (Address Generation Unit)_** _— формирует адреса для нагрузок/записей._

·        **_Integer ALU / MUX / branch_** _— целочисленные арифметические блоки и блоки для ветвлений._

·        **_SSE / FP units (ADD, MUL, MOVE)_** _— векторные и плавающей точки вычисления (SSE инструкции)._

·        **_Store data_** _и_ _пути_ _к_ _**Memory order buffer (MOB)**._

_Ключевая идея: разные операции могут идти на разные порты параллельно (например, одновременно целочисленное сложение и SSE-умножение), если аппаратные блоки и порты свободны. Количество портов/блоков и их специализация определяют IPC (инструкций за такт)._

# _7) Memory subsystem: MOB, L1-D, L2, L3, Uncore_

_Под_ _исполнительными_ _блоками_ _— **Memory order buffer (MOB)** и_ _дальше_ _— **Octuple associative data cache 32 KB, 64-entry TLB-4K, ...** — это_ _L1-D (данный_ _кэш_ _32 КБ__, 8-ассоциативный__), с_ _собственным_ _TLB. MOB координирует загрузки/записи, обеспечивает порядок видимости памяти (memory ordering), хранит буферы записей (store buffer) и обрабатывает зависимые загрузки._

_Если данные нет в L1, запрос идёт в L2 (на схеме справа: 256 KB, 8-way, private L2 per core) — большая, но более медленная локальная кэш-вложенность. Затем есть общий L3 (Common L3-cache 8 MB в разделе Uncore) — разделяемый между ядрами. Uncore содержит также контроллер DDR3 памяти и Quick Path Interconnect (QPI) — шина для связи с другими процессорами/чипсетами._

_Важно: на схеме прямая зелёная линия идёт от ROB/execute к L2/L3/uncore — это путь данных/кэша и размещение промахов._

# _8) Спекуляция, откат и retire_

·        _Инструкции выполняются спекулятивно (до подтверждения правильности пути ветвления)._

·        _Если предсказатель оказался неверен — БЛОК предсказания сообщает фронтенду и аппарат сбрасывает спекулятивные uops из ROB/Reservation station и восстанавливает архитектурные состояния._

·        _Когда инструкция успешно завершена и достигла головы ROB, её результат **retire** — коммитится в архитектурные регистры; на схеме это «2 x Retirement register file»._

# _9) Производительность: микрооп-фьюжн и макрооп-фьюжн_

_На схеме отмечено **micro-op fusion** и **macro-op fusion**. Это аппаратные оптимизации:_

·        **_Macro-op fusion_** _объединяет пару x86 инструкций (например CMP + Jcc) в одну макро-операцию на ранних стадиях, уменьшая число uops._

·        **_Micro-op fusion_** _объединяет микрооперации на уровне декодера/исполнения, что экономит ресурсы исполнительных портов и увеличивает IPC._

# _10) Конкретные числа и их смысл_

_Некоторые из указанных чисел:_

·        _L1 I/D — 32 KB: баланс между ёмкостью и доступностью на такт._

·        _ROB 128 entries: окно out-of-order исполнения — чем больше, тем шире спектр инструкций может быть вне порядка._

·        _Reservation station 128 entries: сколько uops может ожидать исполнения._

·        _L2 256 KB private: больше локальности и меньшие задержки, чем общий L3._

·        _L3 8 MB: общий между ядрами, хранит менее часто используемые данные._

# _11) Поток выполнения одной инструкции — краткая сводка_

_1._     _Команда загружается из L1 I-cache (prefetch + predecode помогают)._

_2._     _Декодируется в микропрограммы/микроопы (decoder)._

_3._     _uops попадают_ _в_ _Decoded instruction queue._

_4._     _Выполняется rename (RAT) и запись в ROB._

_5._     _Диспетчер кладёт uop в Reservation station, ждёт операндов/ресурсов._

_6._     _Когда операнды готовы и порт свободен — uop идёт в execution unit._

_7._     _Если нужна память — через AGU -> L1-D -> L2 -> L3 -> память. MOB обеспечивает согласованность и упорядоченность._

_8._     _Результаты возвращаются и записываются в ROB; когда uop достигает головы ROB — результат коммитится (retire)._

# _12) Архитектурные компромиссы и зачем всё это_

·        **_Параллелизм_**_: out-of-order + множество портов дают высокий IPC, но усложняют дизайн (ROB, RAT, RS)._

·        **_Спекуляция_**_: повышает среднюю производительность, но требует механизма отката и больших буферов._

·        **_Кэш-иерархия_**_: маленькие быстрые L1 для пропускной способности, большие L2/L3 для локальности, Uncore + контроллер памяти для объёма._

·        **_TLB и предзагрузка_**_: уменьшают стоимость виртуальной адресации и промахов по кэшу._

# _13) Практические следствия для программиста_

·        _Последовательный доступ и локальность данных — критичны (помогает L1/L2 hit rate)._

·        _Меньше ветвлений/предсказуемые ветвления — меньше штрафов на mispredict._

·        _Векторизация (SSE/AVX) эффективна, но зависит от наличия соответствующих execution units._

·        _Параллельные порты означают: компилятор/код, который позволяет независимым инструкциям исполняться параллельно, даст выигрыш._

## Кольца защиты

**Кольца защиты** — это аппаратный механизм процессора, который **разделяет уровни привилегий** для кода и предотвращает, чтобы программы с меньшими правами (например, обычные приложения) могли напрямую вмешиваться в критические части системы (например, ядро ОС или драйверы).

![[Pasted image 20251208235445.png]]
### Структура Колец

В теории многие архитектуры (включая **Intel x86**) поддерживают до четырех колец, нумеруемых от **0 до 3**:

- **Ring 0 (Ядро/Kernel Mode):** **Наивысшие привилегии**. Код, работающий здесь, имеет полный и неограниченный доступ ко всему оборудованию (памяти, CPU, дискам, сетевым картам). Здесь работает **ядро операционной системы (ОС)**.
    
- **Ring 1 и Ring 2:** Изначально предназначались для драйверов устройств и служб ОС, но на практике **почти не используются** современными ОС.
    
- **Ring 3 (Пользователь/User Mode):** **Низшие привилегии**. Здесь работают **все прикладные программы** (браузеры, игры, офисные приложения). Код Ring 3 ограничен и не может напрямую получить доступ к оборудованию или памяти других программ.
    

###  Схема и Взаимодействие

Схема иллюстрирует эту иерархию:

- **В центре (Ring 0):** Находится **Kernel (Ядро ОС)**. Это самая защищенная и привилегированная область.
    
- **На периферии (Ring 3):** Находятся **Applications (Приложения)**. Это наименее привилегированная область.
    
- **Механизм Защиты:** Приложение (Ring 3) не может напрямую выполнить привилегированную операцию (например, записать данные на диск). Для этого оно должно запросить у ядра (Ring 0) выполнить эту операцию от своего имени, используя специальный механизм — **системный вызов (System Call)**.
    
    - Когда происходит системный вызов, процессор выполняет **переход** из Ring 3 в Ring 0, ядро проверяет запрос и, если он законен, выполняет действие, а затем возвращает управление обратно в Ring 3.
        

###  Почему используется только два кольца (Ring 0 и Ring 3)?

- Современные ОС (Linux, Windows, macOS) упростили модель для повышения производительности и удобства. Разделение на Ring 1 и Ring 2 оказалось слишком сложным и излишним.
    
- **Ring 0** (Ядро): Максимальный контроль.
- **Ring 3** (Пользователь): Все остальные программы.
![[Pasted image 20251208235450.png]]

### **Ring 0: Ядро / Kernel Mode (Высшие Привилегии)**

Код, работающий в Ring 0, — это **Ядро операционной системы (ОС)**. Оно является "царем" системы и имеет полный, неограниченный доступ ко всем ресурсам.

1. **Доступ к железу (Hardware Access):**
    
    - **Port Mapped I/O (Ввод-вывод, отображаемый на порты):** Ядро может напрямую общаться с устройствами, читая и записывая данные в их регистры (через специальные порты).
        
    - **Memory Mapped I/O (Ввод-вывод, отображаемый на память):** Ядро может напрямую обращаться к памяти, которая управляет устройствами.
        
2. **Служебные Регистры (Control Registers):**
    
    - Доступ к критически важным управляющим регистрам, таким как **GDTR** (Global Descriptor Table Register) и **IDTR** (Interrupt Descriptor Table Register), а также **контрольным регистрам** (CR0, CR3, CR4), которые управляют подсистемой памяти (например, включением страничной организации). Изменение этих регистров может полностью нарушить работу системы.
        
3. **Служебные Инструкции (Privileged Instructions):**
    
    - Возможность выполнять специальные, **привилегированные инструкции**, такие как:
        
        - `lgdt`, `lidt` (загрузка таблиц дескрипторов).
            
        - `cli`/`sti` (отключение/включение прерываний), что критически важно для управления многозадачностью.
            
    - Только код Ring 0 может выполнять эти инструкции.
![[Pasted image 20251209024140.png]]

### **Ring 3: Приложения / User Mode (Низшие Привилегии)**

Код, работающий в Ring 3, — это **прикладные программы** (браузеры, редакторы, игры). Они являются "гражданами" системы и работают в строго изолированном и ограниченном пространстве.

1. **Доступ к инструкциям/регистрам общего назначения:**
    
    - Код может свободно использовать основные инструкции (сложение, вычитание, циклы) и регистры общего назначения (RAX, RBX и т.д.) для своих вычислений.
        
2. **Самостоятельные действия:**
    
    - Приложение может самостоятельно **пользоваться доступной ему памятью** (которая выделена ему ядром) и выполнять в ней вычисления.
        
3. **Ограничения:**
    
    - **За всем остальным придется звать ядро:** Приложение **НЕ МОЖЕТ** напрямую получить доступ к оборудованию, памяти другой программы или выполнить привилегированные инструкции. Любая такая попытка приводит к **исключению (исключительной ситуации/ошибке)**, которое перехватывается ядром.
![[Pasted image 20251209024147.png]]
**Прерывание** — это сигнал (событие), который временно **останавливает** нормальный ход выполнения программы процессором и заставляет его перейти к выполнению специальной подпрограммы, называемой **обработчиком прерывания (Interrupt Handler)**.

**Прерывания** — это механизм, который **заставляет процессор перейти из Ring 3 в Ring 0** для обработки критически важного события (ввода/вывода или ошибки), что является основой **безопасности** и **многозадачности** современных операционных систем.

### Типы Прерываний

1. **Аппаратные Прерывания (Hardware Interrupts):**
    
    - **Источник:** Внешнее устройство (железо).
        
    - **Пример:** Вы нажали клавишу на клавиатуре; сетевая карта приняла пакет данных; жесткий диск закончил операцию чтения.
        
    - **Назначение:** Оповестить CPU, что устройство требует внимания.
        
2. **Программные Прерывания (Software Interrupts / Исключения):**
    
    - **Источник:** Выполнение самой программы или критическая ошибка.
        
    - **Пример:** Деление на ноль; попытка доступа к неверному адресу памяти (**Segmentation Fault**); попытка выполнения **привилегированной инструкции в Ring 3**.
        
    - **Назначение:** Обработка ошибок или выполнение **системных вызовов**.
        
### Важность для Ring 0

- **Контроль:** Ядро ОС **обязано** контролировать прерывания. Если бы приложение в Ring 3 могло использовать `cli` (отключить прерывания), оно могло бы заставить процессор игнорировать сигналы от часов или других устройств.
    
- **Результат:** Система бы зависла, не смогла бы переключаться между процессами, и перестала бы реагировать на внешние события. Именно поэтому управление прерываниями является **привилегией Ring 0**.

### Схема взаимодействия колец и прерываний

```
+----------------------------+
| Ring 0 (Kernel Mode)       |
| - Доступ к железу          |
| - Служебные регистры       |
| - Прерывания и syscalls    |
+----------------------------+
          ▲
          | (Прерывание или syscall)
          |
+----------------------------+
| Ring 3 (User Mode)         |
| - Обычные приложения       |
| - Доступ только к своей памяти |
+----------------------------+

```

## **CPL — Текущий уровень привилегий**

**CPL (Current Privilege Level)** — это **состояние привилегий**, которое хранится в **младших двух битах регистра `CS`** (Code Segment Register) в процессоре x86 (или аналогичные регистры на других архитектурах).

- В **Ring 0** CPL будет равен **0**, что означает **максимальные привилегии**.
    
- В **Ring 3** CPL будет равен **3**, что означает **пользовательский режим**.
    

### Как работает **CPL**:

1. При запуске программы, процессор устанавливает **CPL в 3** для пользовательских приложений.
    
2. Когда приложение вызывает **системный вызов** (например, для работы с файлом), процессор **переключает CPL на 0**, переходя в **kernel mode**.
    
3. После завершения системной операции, процессор **возвращается в CPL 3**, чтобы продолжить выполнение в **user mode**.
    

Таким образом, **CPL** помогает процессору контролировать текущий режим работы программы и решать, какие инструкции разрешены для выполнения.
![[Pasted image 20251208235500.png]]

Картинка иллюстрирует типичную **многоуровневую иерархию кэша** в современном **многоядерном процессоре (multi-core CPU)**.
Кэши — это небольшие ячейки **сверхбыстрой памяти** на самом кристалле процессора. Их цель — **скрыть высокую задержку (latency)** доступа к более медленной **оперативной памяти (RAM)**. Кэши работают по принципу **локальности данных**: если данные были использованы недавно, или если они находятся рядом с недавно использованными данными, велика вероятность, что они скоро понадобятся снова.

Кэш необходим из-за **огромной разницы в скорости** между процессором и оперативной памятью:

- **Скорость CPU:** Процессоры работают невероятно быстро (гигагерцы).
- **Скорость RAM:** Оперативная память гораздо медленнее.

Если бы процессор ждал данные из RAM каждый раз, когда они ему нужны, он бы **большую часть времени простаивал**, тратя **~250 циклов** на каждое обращение. Кэш позволяет избежать этого простоя, сокращая время ожидания до 3 или 10 циклов, если данные там найдены
![[Pasted image 20251209025040.png]]
![[Pasted image 20251208235505.png]]

### Как Кэш Используется? (Принцип Локальности)

CPU не "угадывает" случайно. Он использует физический принцип, называемый **Локальностью (Locality of Reference)**:

1. **Временная локальность (Temporal Locality):** Если данные были использованы **недавно**, скорее всего, они понадобятся **снова в ближайшее время**. (Например, вы только что посчитали переменную `X` — она останется в кэше).
    
2. **Пространственная локальность (Spatial Locality):** Если вы обратились к одному адресу памяти, скорее всего, вам понадобятся данные, которые находятся **рядом** с этим адресом.
    

Когда процессор загружает что-то из RAM, он не загружает 1 байт. Он загружает целый блок данных, называемый **Линия Кэша (Cache Line)** (обычно 64 байта). Таким образом, даже если вы просили только один байт, кэш загружает соседние 63 байта "на всякий случай", чтобы воспользоваться пространственной локальностью.

---

На схеме показано, как эти кэши организованы, чтобы обеспечить максимальную скорость, сохраняя при этом разумную стоимость:
![[Pasted image 20251209025315.png]]
![[Pasted image 20251209025512.png]]

### Шаг 1: Первый Запрос (Промах Кэша / Cache Miss)

1. **Процессор (Ядро):** Нужен первый элемент массива (`Array[0]`).
    
2. **Поиск (Miss):** Процессор проверяет: **L1d $\rightarrow$ L2 $\rightarrow$ L3**. Нигде нет. Это **Промах Кэша**.
    
3. **Загрузка из RAM:** Процессор обращается к **RAM** (долго!).
    
4. **Принцип Локальности:** Процессор не загружает только `Array[0]`. Он загружает весь блок данных (**Линия Кэша**, 64 байта), который включает `Array[0], Array[1], Array[2]...` и так далее.
    
5. **Запись:** Загруженная **линия кэша** последовательно записывается: **RAM $\rightarrow$ L3 $\rightarrow$ L2 $\rightarrow$ L1d**.
    

**Итог:** Первый доступ был медленным, но теперь следующие 64 байта массива находятся в самом быстром кэше L1d.

### Шаг 2: Последующие Запросы (Попадание в Кэш / Cache Hit)

1. **Процессор (Ядро):** Нужен следующий элемент массива (`Array[1]`, `Array[2]`).
    
2. **Поиск (Hit):** Процессор ищет данные. Они **немедленно находятся** в **L1d Cache** (поскольку были загружены вместе с `Array[0]`). Это **Попадание в Кэш**.
    
3. **Исполнение:** Данные из L1d за 3 цикла переходят в **Регистры ЦПУ** и мгновенно обрабатываются.
    

**Итог:** Процессор работает **в сотни раз быстрее**, потому что ему больше не нужно идти к RAM.

### Шаг 3: Обработка Другим Ядром (Роль L3)

Предположим, **Ядро 1** закончит обработку первой части массива и запишет результат, а **Ядро 2** должно начать работать с этими же данными.

1. **Запись:** Ядро 1 записывает результат. Этот результат выталкивается из L1d и L2 в **общий L3 Cache**.
    
2. **Ядро 2 (Поиск):** Ядро 2 запрашивает данные. Оно не находит их в своих L1d/L2 (потому что они **приватные**).
    
3. **Попадание в L3:** Ядро 2 ищет в **L3 Cache** (общий) и **находит** там свежие данные.
    
4. **Передача:** L3 передает данные в L2 и L1d **Ядра 2**.
    
5. **Итог:** Данные передаются между ядрами через L3, что **гораздо быстрее**, чем если бы Ядру 1 пришлось сначала записать их в RAM, а Ядру 2 — загрузить из RAM.


Когда мы говорим о **кэше процессора**, важно понимать, что кэш — это очень **быстрая память** между основным процессором и основной памятью (RAM). Чтобы повысить производительность, процессоры используют кэш для хранения данных, к которым обращаются чаще, чтобы избежать медленных операций чтения из RAM.

Линия кэша (или строка кэша) — это **минимальный блок данных**, который процессор может **одновременно загрузить** из RAM в кэш или **выгрузить** из кэша.

Обычно размер строк кэша составляет **32, 64 или 128 байт** (64 байта — самый распространённый размер в современных CPU).

Когда процессор загружает данные в кэш, он загружает **целую линию** данных. Это означает, что если процессор обращается к адресу памяти, например, к адресу 0x1000, то в кэш загрузится не только данные по этому адресу, но и несколько последующих адресов, так как данные обычно хранятся в памяти блоками.

![[Pasted image 20251208235513.png]]

### Почему используются Линии Кэша?

Использование больших блоков, а не отдельных байтов, является прямым применением принципа **пространственной локальности**:

- Если процессору нужен 1 байт по адресу X, то, **скорее всего**, в ближайшее время ему понадобятся и соседние байты по адресам X+1, X+2 и т.д.
    
- Гораздо **эффективнее** совершить одно "медленное" обращение к RAM и загрузить 64 байта данных сразу, чем совершать 64 быстрых обращения к кэшу за отдельными байтами.
    
- Это позволяет **скрыть задержку (latency)** RAM: процессор один раз "страдает" от долгого доступа к RAM, но получает в кэше большой объем данных для последующей сверхбыстрой обработки.
    

### Как Хранится Линия Кэша?

Когда линия кэша сохраняется в кэше, процессор должен не только хранить сами 64 байта данных, но и **дополнительную информацию** для управления:

- **Данные:** 64 байта данных, скопированных из RAM.
    
- **Местоположение в памяти (Адрес / Tag):** Процессор **сохраняет адрес** (или его часть, называемую **тегом**), чтобы знать, **какое именно место** в огромной Оперативной Памяти эти 64 байта представляют. Это позволяет процессору быстро проверить, является ли линия в кэше той, которую он ищет.
    
- **Флаги состояния (State Bits):** Дополнительные биты, которые указывают, является ли линия **валидной** (содержит ли она актуальные данные) и **грязной** (была ли она изменена ядром и отличается ли теперь от версии в RAM). Это критически важно для **когерентности кэша** в многоядерных системах.
    

---

Вся логика работы кэша сводится к тому, чтобы процессор **как можно чаще** находил нужные данные в самых быстрых уровнях памяти (L1 и L2), избегая медленного обращения к RAM. Если **нужные данные уже есть в кэше**, то процессор может сразу их использовать. Это и называется **попаданием в кэш (cache hit)**. Если же нужных данных **нет в кэше**, процессору придётся **обратиться к оперативной памяти (RAM)**, чтобы загрузить их. Это и есть **промах кэша (cache miss)**.

![[Pasted image 20251208235518.png]]

Хотя кэш — это аппаратный механизм, **поведение программы** сильно влияет на эффективность кэширования. Путем структурирования кода и данных таким образом, чтобы максимально использовать **пространственную** и **временную локальность**.
## Типы промахов кэша

Не все промахи одинаковы — их делят на три категории (по классификации “3C”):

1. 🧩 **Compulsory miss** (обязательный промах)  
    — данные **ещё никогда не загружались** в кэш.  
    Происходит **при первом доступе** к новому адресу.  
    ➤ Избежать нельзя, но можно минимизировать (например, заранее подготавливая данные).
    
2. 🔁 **Capacity miss** (из-за ограниченной ёмкости кэша)  
    — данные **вытеснены**, потому что кэш **слишком мал**, чтобы удержать всё, что используется программой.  
    ➤ Можно уменьшить, оптимизируя порядок доступа к данным (локальность).
    
3. ⚖️ **Conflict miss** (конфликтный промах)  
    — данные **вытеснены**, потому что **несколько адресов маппятся в одну и ту же строку кэша**, даже если в кэше ещё есть место.  
    ➤ Возникает при неудачном распределении адресов по кэш-линиям.  
    ➤ Можно уменьшить, изменяя структуру данных или порядок их в памяти.

Хотя кэш — это аппаратный механизм, **поведение программы** сильно влияет на эффективность кэширования.

Вот несколько практических способов **увеличить количество попаданий**:

1. **Использовать локальность данных**
    
    - 📍 _Пространственная локальность (spatial locality)_ — данные, расположенные рядом, скорее всего будут использоваться вместе.  
        → Например, при обходе массива по порядку (`for i in 0..N`) кэш работает эффективно, потому что загружается целая линия (64 байта), а не по одному элементу.
        
    - ⏳ _Временная локальность (temporal locality)_ — если данные недавно использовались, они, скорее всего, понадобятся снова.  
        → Например, повторное использование переменной в цикле.
        
2. **Избегать “разбросанных” обращений к памяти**
    
    - Доступ к случайным элементам большого массива вызывает много промахов, потому что процессор каждый раз должен загружать новую линию кэша.
        
3. **Стараться хранить часто используемые данные рядом**
    
    - Если структура данных “разбросана” по памяти (например, массив указателей на разные объекты), кэш неэффективен.
        
4. **Бережно работать с большими структурами данных**
    
    - Если данные не помещаются в кэш, старые линии будут вытесняться (capacity misses).
        
    - Иногда эффективнее работать “порциями” (batch processing).

**conflict miss** возникает, когда несколько блоков данных **не могут быть сохранены** в кэше одновременно, потому что кэш не имеет достаточной ассоциативности или слишком ограничен в размере.

```
int sum = 0;
int A[1000];

for (int i = 0; i < 1000; i++) sum += A[i];
```

Здесь кэш работает **идеально**:

- Массив `A` хранится подряд в памяти.
- При загрузке `A[0]`, кэш сразу тянет, например, `A[0]–A[15]` (если строка 64 байта и элемент 4 байта).
- Следующие итерации используют эти же данные → **много cache hits**.

Теперь поменяем доступ:

```
for (int i = 0; i < 1000; i += 64) sum += A[i];
```

Здесь:

- Каждая итерация обращается к новому удалённому адресу.
- Кэш почти бесполезен → **много cache misses**.

Когда мы говорим о **линии кэша** (или **блоке кэша**), мы имеем в виду минимальный фрагмент данных, который может быть загружен в кэш процессора или выгружен из него в основную память. Каждый блок данных, который кэш сохраняет, называется линией кэша. Размер линии кэша играет ключевую роль в производительности процессора, особенно в контексте локальности данных. Слишком маленькие линии могут привести к большому количеству пропусков кэша, а слишком большие — к загрязнению кэша ненужными данными.

![[Pasted image 20251208235525.png]]

Размер линии кэша оказывает существенное влияние на **эффективность кэширования** и на **производительность** программы. Рассмотрим несколько ключевых факторов:

·        **Пространственная локальность** — это свойство программы, когда она обращается к данным, расположенным близко друг к другу в памяти). Если программа часто обращается к соседним данным, то более крупные линии кэша (например, 64 байта) позволяют загружать больше соседних данных за один раз. Это улучшает эффективность кэширования, потому что соседние данные, вероятно, будут использованы вскоре после первого обращения.

  **Трудности с большим размером кэша**:

·        **Cache pollution**: При работе с большими блоками данных в кэше можно столкнуться с ситуацией, когда в кэш загружается много ненужных данных, которые будут выгружены, не успев быть использованы. Это называется «загрязнением кэша» (cache pollution).( Например, если программа обращается к данным в памяти в случайном порядке)

  **Балансировка между размером линии кэша и размером кэша**:

·        Важно найти баланс между размером линии кэша и **общим размером кэша**. Например, если размер линии слишком большой, а объём кэша ограничен, то это может привести к тому, что кэш будет часто заполняться, выгружая полезные данные. В идеале размер линии должен быть таким, чтобы эффективно использовать локальность данных, но не перегружать кэш слишком большим объёмом ненужной информации.

·        ·  **Маленькие строки кэша** помогают уменьшить загрязнение кэша при случайных обращениях, потому что процессор загружает меньше данных за один раз, и кэш быстрее "освобождается" для загрузки более нужных данных.

·        ·  **Большие строки кэша** эффективны, если данные программы имеют **высокую пространственную локальность** (например, данные расположены близко друг к другу), потому что процессор загружает больше данных за один раз, уменьшая количество промахов. Однако если данные используются случайным образом, то это может привести к загрязнению кэша.

![[Pasted image 20251208235531.png]]

Многоуровневые Кэши: Разница в Скорости

Разница в скорости доступа измеряется в **тактах ЦПУ (Clock Cycles)** — это время, которое требуется процессору, чтобы получить данные.

![[Pasted image 20251208235536.png]]

### Структура многоуровневых кэшей

Современные процессоры обычно имеют **3 уровня кэш-памяти**:

- **L1 (Level 1) кэш**: Это самый быстрый и маленький кэш, который находится непосредственно внутри процессора, ближе всего к ядрам. Он обычно разделяется на два кэша: один для данных (D-cache) и один для инструкций (I-cache).
    
- **L2 (Level 2) кэш**: Он больше и медленнее, чем L1, но все равно находится на чипе процессора. Он обслуживает запросы, которые не могут быть удовлетворены L1 кэшом.
    
- **L3 (Level 3) кэш**: Это еще больший и более медленный кэш, который часто общий для всех ядер процессора (если процессор многоядерный). L3 кэш является промежуточным между основным процессором и оперативной памятью.

![[Pasted image 20251208235541.png]]

Разделение кэша L1 на две части:

·  **Устранение конфликтов:** Разделение устраняет конфликты доступа, которые возникли бы, если бы инструкции и данные пытались одновременно обратиться к одному и тому же блоку кэша.

·  **Параллелизм:** Процессор может **одновременно** извлекать **новую инструкцию** (из i-cache) и **загружать операнды** для уже декодированной инструкции (из d-cache). Это критически важно для максимальной загрузки конвейера.

Требование к **d-cache** поддерживать **запись** усложняет его конструкцию, так как он должен гарантировать, что измененные данные в кэше будут в конечном итоге записаны в RAM (**политики обратной записи / write-back**).

Требование к **i-cache** обеспечивать только **чтение** позволяет сделать его **более простым и оптимизированным** для быстрого последовательного доступа, что идеально подходит для непрерывного заполнения конвейера.

---

![[Pasted image 20251209030525.png]]

![[Pasted image 20251209030715.png]]

**Политика размещения** — это набор правил, которые определяют, **где именно** в кэше должна храниться конкретная **линия кэша**, загруженная из Оперативной Памяти (RAM).

Различные политики являются компромиссом между **скоростью доступа и простотой** (Кэш с прямым отображением) и **гибкостью и высоким процентом попаданий** (Полностью ассоциативный кэш).

![[Pasted image 20251208235550.png]]

---

## 1. Кэш с Прямым Отображением (Direct Mapped Cache)

- **Принцип:** **Простейший** и самый быстрый. Каждый блок памяти (линия кэша) может быть сохранен **только в одном, строго заданном месте** в кэше.
    
- **Местоположение:** Определяется по формуле: `Индекс Кэша = Адрес Памяти MOD Количество Блоков в Кэше`.
    
- **Плюсы:**
    
    - **Простота и скорость:** Нет необходимости искать данные; мы сразу знаем, в какой блок нужно обратиться.
        
- **Минусы:**
    
    - **Коллизии:** Если две часто используемые линии памяти отображаются на один и тот же блок кэша, они **постоянно вытесняют** друг друга, вызывая частые промахи (даже если кэш в целом пуст). Это худшая гибкость.
        

---

## 2. Полностью Ассоциативный Кэш (Fully Associative Cache)

- **Принцип:** **Максимальная гибкость.** Любой блок памяти может быть сохранен **в любом свободном месте** внутри кэша.
    
- **Местоположение:** Не определяется индексом. Для поиска нужной линии процессор должен **одновременно** проверить **ВСЕ** блоки кэша на соответствие адресу (тегу).
    
- **Плюсы:**
    
    - **Минимальное количество промахов:** Отсутствие коллизий. Кэш заполняется оптимально.
        
- **Минусы:**
    
    - **Сложность и медлительность:** Требует очень сложной аппаратной логики (одновременное сравнение тегов во всех блоках кэша). Подходит только для **очень маленьких** кэшей (например, буферы TLB или небольшие кэши L1).
        

---

## 3. Наборный Ассоциативный Кэш (Set-Associative Cache)

- **Принцип:** **Компромисс.** Это наиболее распространенный подход (используется в L1, L2, L3). Он сочетает скорость прямого отображения с гибкостью ассоциативного кэша.
    
- **Местоположение:** Кэш разделен на **наборы (Sets)**. Блок памяти отображается на **конкретный набор** (как в прямом отображении), но внутри этого набора он может быть сохранен **в любом свободном месте**.
    
    - Например, **4-сторонний наборный ассоциативный кэш** (4-way Set-Associative) означает, что каждый набор содержит 4 линии кэша.
        
- **Плюсы:**
    
    - **Баланс:** Устраняет большинство коллизий, характерных для прямого отображения, при этом сохраняя высокую скорость доступа (нужно проверять только 4 или 8 линий, а не все 1024).
        

---

### 4. **Двусторонний ассоциативный кэш с смещением (Two-way set-associative cache with offset)**

Это частный случай наборного ассоциативного кэша, где каждый набор состоит только из **двух строк**. То есть каждый набор имеет два места, в которые могут быть записаны данные.

- **Как работает**:  
    В двустороннем ассоциативном кэше кэш делится на наборы, каждый из которых содержит два блока. Когда процессор обращается к памяти, он выбирает набор и проверяет два блока внутри него. Таким образом, у нас есть два возможных места для размещения данных.
    
- **Преимущества**:  
    Такой подход может уменьшить количество конфликтов по сравнению с кэшем с прямым отображением, при этом скорость работы сохраняется на относительно высоком уровне.
    
- **Недостатки**:  
    Увеличение сложности в управлении кэшом и замене данных по сравнению с кэшем с прямым отображением.
    

---

### 5. **Псевдоассоциативный кэш (Pseudo-associative cache)**

**Псевдоассоциативный кэш** — это гибрид между кэшем с прямым отображением и ассоциативным кэшем. В этой системе, если произошел промах, система пытается загрузить данные в соседний блок, который связан с тем блоком, куда данные должны были попасть, используя **связку**.

- **Как работает**:  
    Если происходит промах в кэше с прямым отображением, то система будет искать в **соседней строке** или в другом наборе, что позволяет избежать дополнительных промахов, которые могли бы возникнуть из-за конфликта.
    
- **Преимущества**:  
    Уменьшает количество конфликтных промахов при сравнительно простом механизме работы.
    
- **Недостатки**:  
    Все равно присутствует некоторая вероятность конфликтов, а также нужно добавлять дополнительную логику для определения, куда именно помещать данные.

![[Pasted image 20251209030925.png]]

![[Pasted image 20251208235557.png]]

### Как Происходит Отображение

Местоположение блока памяти в кэше определяется с помощью **оператора остатка от деления** (модуль, `mod`).

- **Формула:** Если кэш содержит $N$ линий (например, $N=4$), то блок памяти $i$ попадет в линию кэша с индексом:
    
    $$Индекс = i \mod N$$
    
- **Пример из Слайда:**
    
    - **Линия кэша 0 (Индекс 0):** Сюда отображаются блоки памяти **0, 4, 8, 12...** (потому что $0 \mod 4 = 0$, $4 \mod 4 = 0$ и т.д.).
    - **Линия кэша 1 (Индекс 1):** Сюда отображаются блоки памяти **1, 5, 9, 13...**
    - **Линия кэша 2 (Индекс 2):** Сюда отображаются блоки памяти **2, 6, 10, 14...**
    - **Линия кэша 3 (Индекс 3):** Сюда отображаются блоки памяти **3, 7, 11, 15...**
        

### Схема и Поиск Данных

Схема иллюстрирует этот жесткий механизм:

1. **Адрес памяти (Memory Address):** Процессор запрашивает данные по определенному адресу (например, **Блок 4**).
    
2. **Индекс (Index):** Адрес делится на части. Часть битов адреса используется для вычисления **Индекса** (в нашем примере $4 \mod 4 = 0$). Этот индекс указывает на **строго одну** линию кэша (Линия 0).
    
3. **Тег (Tag):** Оставшиеся биты адреса используются как **Тег**. Тег хранится рядом с данными в самой линии кэша.
    
4. **Сравнение:** Процессор обращается к Линии 0 и **сравнивает Тег** линии с Тегом искомого адреса.
    
    - **Попадание (Hit):** Если Теги совпадают, процессор нашел данные. Это **быстрый** доступ.
        
    - **Промах (Miss):** Если Теги не совпадают (например, в Линии 0 лежат данные от **Блока 8**, а ищем **Блок 4**), это **Промах**. Процессор должен загрузить нужные данные из RAM, **вытеснить** старую линию (Блок 8) и записать новую (Блок 4).
        

### Главный Недостаток (Коллизии)

Самый большой недостаток этого метода — **проблема коллизий**.

- Поскольку множество блоков памяти (0, 4, 8, 12...) вынуждены бороться за **одну и ту же Линию 0**, если программа часто переключается между Блоком 0 и Блоком 4, они будут **постоянно вытеснять** друг друга.
    
- Это приводит к **частым промахам (Misses)** и низкой производительности, даже если в остальных трех линиях кэша (1, 2, 3) полно свободного места.

![[Pasted image 20251208235602.png]]
![[Pasted image 20251208235606.png]]

Когда процессор работает с кэш-памятью, важно не только хранить сами данные, но и управлять дополнительной информацией о них, называемой **метаданными**. Эти метаданные помогают процессору эффективно работать с данными, а также проверять их актуальность и целостность. 
**Метаданные** — это **дополнительные биты информации**, которые процессор хранит рядом с каждой **Линией Кэша**. Они нужны для того, чтобы быстро отвечать на два главных вопроса: **"Это те данные, которые я ищу?"** и **"Эти данные актуальны?"**

![[Pasted image 20251208235611.png]]

Кэш-линия состоит из трех основных частей
#### А. Тег (Tag)

Хотя слайд явно не называет его "Тегом", это основная "последняя частичка информации" о местоположении. **Тег** — это оставшаяся часть адреса памяти, которая не была использована для вычисления **Индекса** линии кэша.

Когда процессор находит нужный **Индекс** кэша, он должен **сравнить Тег** в кэш-линии с Тегом искомого адреса. Если теги совпадают, это подтверждает, что линия кэша содержит данные **именно с этого адреса** RAM, а не какого-либо другого адреса, который отображается на тот же индекс (это критически важно в **Кэше с Прямым Отображением**).

#### Б. Бит Действительности (Valid Bit)

Один бит, указывающий на **актуальность** данных в линии.

**Начальное состояние:** В начале работы системы, когда кэш пуст, **все биты действительности установлены в 0**. **При загрузке:** Когда данные **впервые загружаются** из медленной RAM в определенный блок кэша, соответствующий **бит действительности устанавливается в 1**.

Если бит действительности равен **0**, процессор знает, что данные в этой линии — старый "мусор" или они никогда не были загружены. Он немедленно объявляет **Промах Кэша (Cache Miss)** и начинает загрузку из более медленной памяти.

![[Pasted image 20251209031307.png]]

![[Pasted image 20251208235619.png]]
![[Pasted image 20251208235623.png]]

Когда мы говорим о **физических адресах** в RAM, мы имеем в виду реальные адреса, которые указывают на ячейки памяти в аппаратной RAM (оперативной памяти). Это то, что фактически физически доступно процессору.

**Виртуальные адреса**, с другой стороны, — это абстракция, с которой работают программы. Операционная система предоставляет каждому процессу свой виртуальный адресный пространство, которое процесс воспринимает как свою "собственную" память. Этот виртуальный адрес может быть маппирован (отображен) на физический адрес в RAM, или, если данных нет в RAM, то на пространство на диске (например, на своп-файл или раздел подкачки).

![[Pasted image 20251208235634.png]]

Виртуальная память позволяет программам работать с адресами, которые не соответствуют физическому расположению данных в оперативной памяти. Вместо того чтобы напрямую обращаться к физической памяти, программы используют **виртуальные адреса**, которые ОС преобразует в физические адреса. Это создает иллюзию, что у каждого процесса есть свой собственный адресный пространство, независимо от того, сколько реально доступно физической памяти.

### **Адресное пространство процесса**

Каждый процесс в системе имеет свое собственное **виртуальное адресное пространство**, которое изолирует его память от памяти других процессов. Это важное свойство обеспечивает:

- **Защиту памяти**: каждый процесс не может напрямую обратиться к памяти другого процесса, что предотвращает непреднамеренный доступ или вмешательство.
- **Изоляцию процессов**: это помогает избежать проблем, когда один процесс случайно изменяет данные другого.
    

Каждому процессу присваивается виртуальное пространство, которое делится на несколько областей, таких как:

- **Текстовый сегмент**: здесь находятся инструкции программы (код).
- **Данные**: хранятся глобальные переменные.
- **Стек**: используется для хранения данных о вызовах функций и локальных переменных.
- **Куча**: область для динамически выделенной памяти (например, при использовании `malloc` в C).

![[Pasted image 20251209031448.png]]

**Page Fault (Ошибка страницы)** — это **аппаратное прерывание**, которое возникает, когда MMU (блок в CPU, который переводит адреса) не может найти в **Таблице Страниц** соответствующий **Физический Адрес (PA)** для запрошенного **Виртуального Адреса (VA)**. ОС использует Page Fault как **сигнал** о том, что память действительно нужна

### Механизм Overcommitment (Зарезервировано > Доступно)

**Overcommitment** (Чрезмерное резервирование) — это ключевая особенность виртуальной памяти, которая повышает эффективность использования RAM.

- **Принцип:** ОС позволяет процессам **зарезервировать больше виртуальной памяти**, чем фактически доступно **физической RAM**.
    
- **Почему это работает:** Большинство программ резервируют большой объем памяти "на всякий случай" (например, 2 ГБ), но в любой момент времени активно используют лишь малую часть этой памяти (например, 200 МБ).
    
- **Эффект:** ОС резервирует виртуальное пространство, но выделяет **физическую RAM** только тогда, когда процесс **действительно начинает записывать** данные в эту область (принцип **CoW** или **Copy-on-Write**). Это позволяет системе запускать больше программ, чем позволяет физический объем RAM, за счет предположения, что не все они будут активно использовать зарезервированную память одновременно.
---

**Вывод:** Виртуальная память — это основа **безопасности, стабильности** и **многозадачности**. Она позволяет ОС эффективно использовать ограниченный объем RAM и обеспечивает изоляцию между программами.

### Зачем использовать виртуальные адреса, если есть физическая память?

Основная причина использования виртуальной памяти — это **абстракция и безопасность**. Виртуальная память позволяет процессам работать, как если бы у них было много памяти (больше, чем есть в физической RAM), и при этом изолировать их друг от друга.

**Вот зачем это нужно:**

1.     **Изоляция процессов:** Каждый процесс работает в своем собственном виртуальном адресном пространстве. Это значит, что один процесс не может напрямую обращаться к памяти другого процесса. Например, если у процесса A есть ошибка, и он обращается к памяти, которая ему не принадлежит, ОС может прервать его выполнение, предотвратив повреждение данных других процессов.

2.     **Безопасность:** Виртуальные адреса позволяют контролировать, какие данные и код доступны процессу, а какие — нет. Это помогает предотвратить несанкционированный доступ или манипуляции с памятью.

3.     **Управление памятью:** Виртуальная память позволяет ОС использовать механизмы, как **перемещение данных на диск** (через свопинг), если RAM переполнена, тем самым давая больше "виртуальной" памяти, чем есть в физическом оперативном хранилище.

### Зачем виртуальные адреса, если есть физические?

·        **Абстракция:** Программы не знают, где именно находятся их данные в физической памяти, они работают с виртуальными адресами. Это позволяет операционной системе скрывать детали аппаратного обеспечения и управлять памятью более гибко.

·        **Оптимизация:** Виртуальная память позволяет использовать **виртуальные адреса** для более сложных стратегий управления памятью, например, **пейджинг** и **свопинг**, которые позволяют эффективно использовать ограниченные ресурсы RAM, выгружая редко используемые данные на диск.

·        **Защита:** Виртуальная память также позволяет защитить процесс от ошибок, когда он случайно пытается изменить память другого процесса или ядра системы.

### Почему без адресного пространства было бы небезопасно?

Без изоляции памяти через виртуальные адреса, **процессам было бы гораздо сложнее работать безопасно и эффективно**. Например, если два процесса использовали бы одну и ту же физическую память, один процесс мог бы случайно перезаписать данные другого. Виртуальная память обеспечивает защиту и изоляцию, что является критически важным для стабильной и безопасной работы системы.

![[Pasted image 20251209031952.png]]
**Права доступа осуществляются механизмом RWX (как в ФС Unix)**.

Эта строка означает, что Виртуальная Память (VM) не просто отображает адреса, но и контролирует, **что** процесс может делать с данным блоком памяти. Эти биты разрешений (**RWX**) хранятся прямо в **Таблице Страниц** рядом с каждым отображаемым адресом.
### Механизм RWX

**RWX** — это стандартный набор разрешений (прав) доступа, взятый из файловых систем (File Systems, ФС), но примененный к **страницам памяти** (блокам виртуальной памяти):

- **R (Read) / Чтение:** Позволяет процессу **читать** данные из этой страницы памяти.
    
- **W (Write) / Запись:** Позволяет процессу **изменять** (записывать новые данные) в эту страницу памяти.
    
- **X (Execute) / Исполнение:** Позволяет процессору **исполнять** содержимое этой страницы как **инструкции** (код программы).
Эти биты разрешений (**RWX**) хранятся прямо в **Таблице Страниц** рядом с каждым отображаемым адресом.

![[Pasted image 20251209032156.png]]

### 1. Трансляция Адресов (MMU и TLB)

- **Виртуальные Адреса (VA):** Адреса, которые видит и использует программа (процесс).
    
- **Трансляция:** Виртуальные адреса преобразуются в **Физические Адреса (PA)** в RAM с помощью **MMU (Memory Management Unit)**. MMU — это специальный **аппаратный блок** внутри процессора.
    
- **Кэширование (TLB):** Трансляция адреса через MMU и Таблицы Страниц — это медленная операция. Чтобы ускорить ее, результат трансляции **кэшируется в TLB (Translation Lookaside Buffer)**.
    
    - **TLB** — это очень маленький, сверхбыстрый **ассоциативный кэш**, который хранит наиболее часто используемые пары `VA -> PA`. Если адрес найден в TLB, трансляция занимает **1-2 цикла ЦПУ**, а не сотни, как при полном просмотре Таблицы Страниц.
        
---
### 2. Блоки Памяти (Страницы и Фреймы)

Виртуальная память использует блочную организацию для упрощения управления и повышения эффективности:

- **Виртуальная Память делится на страницы (Pages):** Это блоки **фиксированного размера** (обычно **4 КБ**). Вся логика ОС по управлению VM работает на уровне страниц.
- **Физическая Память делится на фреймы (Frames):** Это блоки **того же фиксированного размера** (4 КБ) в Физической RAM.
- **Отображение:** Страница **Виртуальной Памяти** отображается на Фрейм **Физической Памяти**.

---
### 3. Таблицы Страниц (Page Tables)

- **Назначение:** **Таблицы Страниц** — это главная "карта", которую ядро ОС создает и использует для трансляции адресов.
    
- **Что хранит:** Каждая запись в Таблице Страниц отображает **Виртуальную Страницу** на:
    
    1. **Физический Фрейм** в RAM (если страница активна).
    2. **Область на Диске (Swap)** (если страница была выгружена).
    3. **Отсутствие** (если страница зарезервирована, но еще не выделена RAM — **Overcommitment**).
        
Каждый **Процесс** (ваша запущенная программа) имеет свою **уникальную Таблицу Страниц** (или набор таблиц), которая определяет его **Виртуальное Адресное Пространство (VAS)** Хотя **Таблицы Страниц** каждого процесса уникальны, они могут указывать на **одну и ту же Физическую Память (Фрейм RAM)**.

---
**Page Fault** — это ключевое **аппаратное прерывание**, которое возникает, когда MMU не может выполнить запрос процесса, и требуется вмешательство ядра ОС (Ring 0).

- **Причины Page Fault:**
    
    1. **Нет прав доступа:** Программа пытается **записать** данные в страницу, которая помечена как **только для чтения (R)**. Это **нарушение защиты**, и ОС завершит процесс.
        
    2. **Страница не в RAM (Absent):** Программа обращается к странице, которая:
        
        - Либо **никогда не была выделена** (Page Fault, который приводит к **ленивому выделению** RAM).
            
        - Либо **была выгружена на Диск (Swap Out)**.
            
- **Подкачка с Диска (Swapping):** Если Page Fault вызван тем, что **страница памяти не в RAM** (а в Swap-файле на диске), ядро ОС выполняет **подкачку (Swap In)**: загружает страницу с диска в свободный фрейм RAM, обновляет Таблицу Страниц и позволяет процессору повторить инструкцию.

![[Pasted image 20251209032430.png]]

![[Pasted image 20251208235644.png]]
![[Pasted image 20251209032525.png]]
![[Pasted image 20251208235650.png]]
![[Pasted image 20251209032558.png]]
![[Pasted image 20251209032605.png]]

1. Page Directory (каталог страниц)

- Каждая запись указывает на **физический адрес таблицы страниц (Page Table)**. Адрес **Page Directory** хранится в специальном регистре **CR3** (Control Register 3).

2. Page Table (таблица страниц)

Каждая запись указывает на **физический адрес начала страницы (frame)** в RAM.

3. Физическая страница (Frame)

·  Когда процессор дошёл до Page Table, он знает, **где именно в физической памяти** хранится нужная страница.

·  К этому адресу прибавляется **смещение (12 бит)** из виртуального адреса.

·  Получаем **физический адрес нужного байта**.

![[Pasted image 20251208235700.png]]
![[Pasted image 20251208235705.png]]

![[Pasted image 20251209032630.png]]
![[Pasted image 20251209032636.png]]
![[Pasted image 20251209032647.png]]

![[Pasted image 20251208235710.png]]

·  **Прошлая схема (Page Directory Entry):** Описывает **первый уровень** трансляции адреса и содержит **физический адрес** следующей карты (Таблицы Страниц).

·  **Схема на фото (Page Table Entry):** Описывает **второй уровень** трансляции адреса и содержит **физический адрес** самого блока данных (Фрейма).

![[Pasted image 20251208235715.png]]
![[Pasted image 20251208235721.png]]

**Файловый маппинг** — это техника ядра операционной системы, которая позволяет **отобразить содержимое файла на диске непосредственно в виртуальное адресное пространство** процесса.

Вместо того чтобы работать с файлом, используя традиционные медленные операции ввода-вывода (например, `read()` или `write()`), программа может работать с ним **как с обычным массивом в памяти**.

![[Pasted image 20251208235725.png]]

### 1. Как это Работает

1.     **Выделение Виртуальной Области:** Ядро ОС выделяет область **виртуальной памяти**, которая не имеет собственного физического фрейма в RAM, но **связана с файлом на диске**.

2.     **Работа с Памятью:** Процесс получает начальный **виртуальный адрес** этой области и может читать или записывать данные, используя обычные инструкции процессора.

3.     **Синхронизация:** Когда процесс читает или записывает данные в эту область, ОС **автоматически синхронизирует** эти изменения с файлом на диске.

### 2. Принцип "Ленивой" Загрузки (Lazy Loading)

Отображение файла в память использует механизм **страничной виртуальной памяти** (который мы только что обсуждали) для эффективной загрузки:

·        **Page Fault (Ошибка Страницы):** Данные из файла **загружаются в физическую память (RAM) только при первом обращении** к ним, то есть подгрузка содержимого файла происходит **лениво** (по требованию).

·        **Механизм:** Если программа пытается прочитать часть отображенной области памяти, а соответствующей страницы еще нет в RAM, MMU вызывает **Page Fault**. Ядро ОС перехватывает его и понимает: "Эта страница — часть файла X". Ядро **загружает необходимый блок данных** (страницу) из файла X на диске в свободный фрейм RAM, обновляет Таблицу Страниц и разрешает выполнение инструкции.

### 3. Преимущества

1.     **Скорость:** Это значительно быстрее, чем традиционный ввод-вывод, поскольку исключаются лишние **копирования данных** между буферами ядра и пользовательского процесса.

2.     **Эффективность:** Память выделяется только для тех частей файла, к которым действительно обращаются.

3.     **Удобство:** Программист работает с файлом как с указателем на память, что упрощает код.

Файловый маппинг часто используется для быстрой загрузки исполняемых файлов (библиотек, программ) и для очень больших файлов, которые не помещаются целиком в RAM.

![[Pasted image 20251208235731.png]]

### 1. Файл на Диске (File on Disk)

- **File on Disk (Справа):** Это сам файл, который хранится на **медленном, постоянном носителе** (жесткий диск или SSD).
    
- **Стрелка File Mapping:** Операционная система устанавливает связь между этим файлом и **Объектом отображения файла (File Mapping Object)**.
### 2. Область Физической Памяти (Physical Memory)

- **File Mapping Object (в центре):** Это **метаданные ядра ОС**, которые описывают связь между файлом и Физической Памятью.
    
- **Physical Memory (Физическая Память):** Это **реальная RAM** вашей системы. **Важный момент:** В отличие от обычных данных, которые сначала копируются из файла в буфер, здесь **Физический Фрейм** в RAM становится **кешем** для этого файла. Части файла загружаются в эти фреймы RAM **только по требованию (лениво)**, когда процесс обращается к ним, используя механизм **Page Fault**.
### 3. Виртуальное Адресное Пространство (Virtual Memory)

- **Process 1 Virtual Memory** и **Process 2 Virtual Memory (Слева):** Это **изолированные адресные пространства** каждого процесса.
    
- **File View 1, File View 2:** Это **области виртуальной памяти**, которые каждый процесс использует для работы с файлом.
    
    - **Изоляция:** Процессы не видят и не знают, где находится файл физически или как его видят другие процессы. Они работают со **своими виртуальными адресами**.
        
    - **Гибкость:** Процесс 1 может отобразить часть файла в область **File View 1** (например, с 0x80000000), а другую часть — в **File View 2** (например, с 0xA0000000). Процесс 2 может отобразить ту же часть файла в **свою** область **File View 1** (например, с 0x50000000).

![[Pasted image 20251208235737.png]]
![[Pasted image 20251209032908.png]]

## Преимущества

✅ Быстрее, чем системные вызовы `read`/`write` — данные читаются постранично и кэшируются.  
✅ Не нужно вручную управлять буферами.  
✅ Упрощает работу с большими файлами (ОС сама решает, какие страницы держать в RAM).  
✅ Позволяет **разделять память между процессами** — если два процесса мапят один и тот же файл, они фактически работают с **одними и теми же страницами** (экономия памяти и IPC).

---

## 🚫 Недостатки и ограничения

⚠️ Если файл очень большой и ты обращаешься к случайным местам, может быть много page fault’ов — это замедлит работу.  
⚠️ Если изменяешь данные — нужно помнить про `msync()` или `munmap()` для записи на диск.  
⚠️ Ошибки при обращении к несуществующим страницам → `SIGSEGV` (segmentation fault).

![[Pasted image 20251208235743.png]]

###  1.`void *addr` (Адрес по желанию)

- **Назначение:** Указывает **желаемый Виртуальный Адрес** начала отображаемой области памяти.
    
- **Особенности:** Если передать `NULL`, ОС автоматически выберет подходящий свободный виртуальный адрес. В большинстве случаев рекомендуется использовать `NULL`.
    
### 2. `size_t length` (Сколько байт нужно отобразить)

- **Назначение:** Общая **длина в байтах** области виртуальной памяти, которую необходимо отобразить.
    
- **Особенности:** Длина обычно равна размеру файла или части файла, с которой нужно работать.
### 3. `int prot` (Права доступа)

- **Назначение:** Определяет **аппаратные права доступа** к этой области памяти в **Таблицах Страниц** (механизм **RWX**).
    
- **Возможные значения (флаги):**
    
    - `PROT_READ` (Чтение)
    - `PROT_WRITE` (Запись)
    - `PROT_EXEC` (Исполнение)
    - `PROT_NONE` (Нет доступа)
        

### 4. `int flags` (Флаги)

- **Назначение:** Определяет **тип отображения** (важнейший аргумент).
    
- **Основные значения:**
    
    - **`MAP_SHARED` (Общее):** Любые изменения, сделанные в этой области памяти, **будут видны другим процессам**, отображающим тот же файл, и будут **записаны обратно в файл** на диске. Используется для IPC и постоянного сохранения данных.
        
    - **`MAP_PRIVATE` (Приватное):** Изменения, сделанные процессом, **не записываются обратно в файл** и **не видны другим процессам**. Используется принцип **Copy-on-Write (CoW)**: при первой попытке записи создается приватная копия страницы, чтобы изменения не повлияли на исходный файл. Используется для загрузки исполняемых файлов (библиотек).
        

### 5. `int fd` (Дескриптор файла)

- **Назначение:** **Дескриптор открытого файла**, содержимое которого нужно отобразить.
    
- **Особенности:** Файл должен быть открыт с соответствующими правами (`O_RDONLY` для чтения, `O_RDWR` для чтения/записи), соответствующими аргументу `prot`.
    

### 6. `off_t offset` (Смещение)

- **Назначение:** Определяет **смещение (от начала файла)**, откуда должно начинаться отображение.
    
- **Особенности:** Смещение **должно быть кратно размеру страницы** (обычно 4 КБ), поскольку файловый маппинг работает постранично.
    

После успешного вызова `mmap()`, функция возвращает **начальный виртуальный адрес** новой отображенной области, с которой программа может работать, как с обычным указателем.

![[Pasted image 20251208235753.png]]

### 1. Начальное Состояние (Общее Использование)

- Изначально, копии данных **не создаются**, пока общие данные не изменяются. Это экономит время и физическую оперативную память (RAM).
    
- Процессы/объекты получают **доступ только для чтения** к общему блоку памяти.
    
### 2. Принцип Срабатывания (Модификация)

- Если процесс пытается **изменить** данные в общей странице, это вызывает **Page Fault (ошибку страницы)**.
    
- Ядро ОС перехватывает Page Fault и понимает, что это защищенная CoW-страница.
    
- Только в этот момент ядро создает **приватную копию** этой страницы для модифицирующего процесса, обновляет его **Таблицу Страниц**, чтобы она указывала на новую, приватную копию, и разрешает операцию записи.
    
- Исходная страница остаётся доступной для других процессов, которые её не модифицируют.
    
### 3. Применение в Linux (Процесс `fork`)

- В операционной системе Linux, при создании дочернего процесса (через системный вызов `fork`), копируется только **Таблица Страниц**.
    
- Данные процесса (**физические фреймы**) остаются общими между родительским и дочерним процессами до тех пор, пока один из них не попытается в них **записать**.
    
### 4. Гарантии и Преимущества

- **Атомарность и Изоляция:** Благодаря механизму CoW, гарантируется **атомарность изменений**, что означает, что другие процессы **видят неизменные данные** до тех пор, пока не будет завершена полная операция копирования.
    
- **Эффективность:** CoW позволяет быстро запускать новые процессы (`fork`), так как большая часть памяти не копируется, а просто разделяется, если данные не будут изменены.

![[Pasted image 20251208235758.png]]
![[Pasted image 20251209033353.png]]
**Shared Memory (Общая Память)** — это один из наиболее быстрых и эффективных механизмов **Межпроцессного Взаимодействия (IPC)**, который позволяет двум или более **независимым процессам** получать одновременный и прямой доступ к одной и той же области **Физической Памяти (RAM)**.

Вот как это работает и почему это важно:

---

## 🚀 Принцип Работы Общей Памяти

1.     **Выделение Области:** Один процесс (обычно сервер или инициатор) запрашивает у ядра ОС выделение специального, **именованного блока физической памяти**.

2.     **Отображение:** Ядро ОС затем отображает этот физический блок в **виртуальное адресное пространство** каждого процесса, который запрашивает доступ к общей памяти.

3.     **Прямой Доступ:** После отображения процессы могут читать и записывать данные в этот блок, используя **обычные инструкции процессора (Load/Store)**, как если бы это был массив внутри их собственного процесса.

### Ключевое Отличие от Других IPC

В отличие от других механизмов IPC (например, сокетов или пайпов), которые требуют **копирования данных** из пространства одного процесса в пространство ядра, а затем в пространство другого процесса, **Shared Memory** устраняет этот этап копирования.

·        Процессы получают **прямой, небуферизованный доступ** к общим данным, что делает этот метод самым быстрым для обмена большими объемами информации.

## ⚠️ Важный Аспект: Синхронизация

Поскольку процессы работают с общей памятью **одновременно**, они могут попытаться изменить одни и те же данные в одно и то же время, что может привести к **состоянию гонки (race condition)**.

·        Для безопасной работы с общей памятью процессы **обязательно** должны использовать дополнительные механизмы **синхронизации** (например, мьютексы, семафоры). Эти механизмы гарантируют, что только один процесс в определенный момент времени может изменять общую область.

**Отображение памяти (Memory Mapping)** — это общий механизм операционной системы, который связывает область **Виртуального Адресного Пространства (Virtual Address Space)** процесса с какой-либо **физической сущностью**.

По сути, это техника, позволяющая процессу обращаться к данным, расположенным в разных местах (будь то файл на диске, область RAM или даже регистры аппаратного устройства), как к **единому массиву** в собственной памяти.

## 🧠 Как Устроено Отображение Памяти

1.     **Виртуальная Связь:** Ядро выделяет диапазон **Виртуальных Адресов** внутри процесса, но изначально не связывает их с физической RAM.

2.     **Аппаратный Перевод:** Когда процесс обращается к этим виртуальным адресам, аппаратный блок **MMU (Memory Management Unit)** использует **Таблицы Страниц** для их трансляции.

3.     **Гибкость:** В зависимости от типа отображения, запись в Таблице Страниц может указывать на:

o   **Физический Фрейм (RAM)**.
o   **Файл на диске** (в случае файлового маппинга).
o   **Область подкачки (Swap)**

![[Pasted image 20251229223216.png]]

### Список литературы:

* Первая секция этого видео объясняет работу процессора в аналогии с тем, как принимают заказы в Starbucks:
https://www.youtube.com/watch?v=t95irwtd_t4&t=405s&pp=0gcJCfwJAYcqIYzv

* Таблица с задержками, показывающая, почему попадания в кэш полезны для производительности: https://gist.github.com/jboner/2841832

* Видик поясняющий за виртуальную память: https://www.youtube.com/watch?v=fGP6VHxqkIM

* Видео, объясняющая, что такое page fault и как он используется для манипуляций виртуальной памяти: https://www.youtube.com/watch?v=eoFcFqPiPjk

* Видик, показывающий многоуровневую структуру страниц виртуальной памяти: https://www.youtube.com/watch?v=zNPVmTGt7Ds

* Видик поясняющий за кэши в процессоре: https://www.youtube.com/watch?v=zF4VMombo7U
* https://chatgpt.com/share/6952d68d-fe28-8001-b9a8-07534eb80947
* https://gemini.google.com/share/c0c28a682ae5
* https://gist.github.com/vladeemerr/218f742f4b0c2b39d8c1cebe4da8dbe7