![[Pasted image 20251228223119.png]]

![[Pasted image 20251228223141.png]]

- **Программа** — это статический объект (просто файл `.exe` на диске, набор байт). Она «спит».
    
- **Процесс** — это «ожившая» программа. Как только вы дважды кликнули по иконке, ОС создает процесс. Это программа в движении.

---
**Контекст процесса** — это **всё состояние процессора и ОС**, необходимое для продолжения выполнения процесса **с того же места**.

Контекст сохраняется, когда:

- таймер прерывания (квант времени закончился);
- процесс делает `syscall`;
- процесс блокируется (I/O);
- происходит переключение на другой процесс.
    
 Это называется **контекстное переключение** (_context switch_).

### Что именно сохраняется

- **Указатель инструкции (Instruction Pointer):** Адрес в памяти, где лежит следующая команда, которую должен выполнить процессор. Если процесс прервется (например, вы переключились на другое окно), ОС запомнит этот адрес, чтобы потом продолжить точно с того же места.
    
- **Регистры:** Временные ячейки памяти прямо внутри процессора. Процесс хранит там промежуточные результаты вычислений (например, переменную `x` в цикле).

- Указатель стека + состояние стека -  адрес вершины стека; стек вызовов функций.

- Системные структуры ОС: ОС хранит информацию о процессе в специальных структурах, например:  PCB (Process Control Block); task_struct (Linux). Там лежит: PID; состояние (running / ready / blocked); приоритет; указатели на таблицы памяти; список открытых файлов.

>Бывает ситуация, когда программа уже закрылась, но процесс все еще висит в списке (так называемый **Zombie Process** в Linux). Он уже не выполняет код, но ОС хранит его структуру в таблице процессов, пока родительский процесс не заберет отчет о его завершении.

![[Pasted image 20251228223158.png]]

### 1. Одна программа — несколько процессов

Это классика современной архитектуры. Программа на диске одна, а в памяти «живет» несколько её копий.

- **Зачем это нужно:** Для отказоустойчивости и распределения задач.
    
- **Пример с браузером:** Как я уже упоминал, Chrome создает отдельный процесс на каждую вкладку. Если "тяжелый" сайт зависнет в одной вкладке, остальные будут работать, так как их процессы изолированы друг от друга.
    
- **Пример с сервером:** Веб-сервер (например, Apache) может запускать отдельный процесс для каждого нового пользователя, чтобы они не мешали друг другу.
    

### 2. В рамках одного процесса — несколько «программ» (потоки)

- **Суть:** Процесс — это «контейнер» (ресурсы, память, открытые файлы). А потоки — это «рабочие руки», которые бегают внутри этого контейнера.
    
- **В чем фишка:** Все потоки одного процесса **делят общую память**. Если один поток изменил значение глобальной переменной, второй это сразу увидит. Это быстрее, чем пересылать данные между процессами, но опаснее (могут возникнуть конфликты доступа).

Все потоки **делят между собой**:

- адресное пространство;
- код;
- глобальные переменные;
- файлы и сокеты.
    
Что у каждого потока своё

- указатель инструкции;
- регистры;
- стек.
### 3. Системные вызовы (Syscalls): Код, которого нет в программе

Это очень важный момент для понимания того, как работает ОС. Ваша программа не умеет сама писать файлы на диск или отправлять пакеты в сеть. Она «не имеет прав» трогать железо напрямую.

- **Как это работает:** Когда программе нужно что-то от железа, она делает **системный вызов**. В этот момент выполнение кода вашей программы приостанавливается, и управление передается **ядру ОС**.
    
- **Парадокс:** Процесс вроде бы ваш, но в данный конкретный момент внутри него выполняется код ядра Linux или Windows.
    
- **Пример:** Функция `printf()` в Си или `print()` в Python в конечном итоге вызывает системный вызов `write()`, который является частью операционной системы.


---
### 1. Браузер (на примере Chrome/Firefox)

Современные браузеры — это гибриды. Они используют и процессы, и потоки:

- **Процессы:** Каждая вкладка — отдельный процесс. Если сайт с кривым кодом зависнет, ты просто закроешь одну вкладку, а не весь браузер.
    
- **Потоки (внутри одной вкладки):**
    
    - **Main Thread (Главный поток):** Отрисовывает интерфейс (UI). Если он занят, кнопка «нажать» не сработает.
        
    - **Network Thread:** Загружает картинки и скрипты из сети в фоновом режиме, пока ты читаешь текст.
        
    - **Render Thread:** Берет HTML-код и превращает его в пиксели на экране.
        

### 2. Компьютерные игры

В играх потоки жизненно необходимы, чтобы картинка не «фризила» (не дергалась):

- **Main/Render Thread:** Занимается только графикой. Его задача — выдавать 60+ кадров в секунду.
    
- **Physics Thread:** Рассчитывает, как падают ящики или летят пули. Если бы это делал тот же поток, что рисует графику, игра бы тормозила при каждом взрыве.
    
- **Audio Thread:** Проигрывает музыку и звуки шагов.
    
- **AI Thread:** Думает за противников (куда побежать боту).
    

### 3. Мессенджеры (Telegram, WhatsApp)

- **Поток интерфейса:** Рисует твои сообщения и анимации.
    
- **Фоновый поток:** Постоянно «слушает» сервер. Когда тебе приходит сообщение, этот поток ловит его и говорит главному: «Эй, обнови экран, там пришло новое сообщение!»
![[Pasted image 20251228223217.png]]
Каждый процесс видит память **в виде виртуального адресного пространства**.

👉 Процессу кажется, что
- память начинается с адреса `0x0`,
- память «бесконечная» и **только его**.
На самом деле
- это иллюзия,
- за ней стоит **MMU + ОС**.

---
### 1. Text Segment (Segment кода)

- **Что там:** Скомпилированный машинный код программы — те самые нули и единицы, которые понимает процессор.
    
- **Особенность:** Обычно этот сегмент доступен только для чтения (**Read-Only**). Это защита: чтобы программа случайно (или из-за вируса) не изменила свой собственный код во время работы.
### 2. Data Segment и BSS

Эти два парня хранят «глобальные» вещи, которые должны жить всё время, пока работает программа:

- **Data Segment:** Здесь лежат глобальные переменные, которым вы **сразу дали значение**. Например: `int x = 10;`.
    
- **BSS segment:** Здесь лежат глобальные переменные, которые **не инициализированы** (просто объявлены). ОС сама заполнит их нулями при запуске. Например: `int y;`.
### 3. Heap (Куча) — растет ВВЕРХ ⬆️

- **Что там:** Память, которую вы запрашиваете сами во время работы программы (через `malloc` в Си или `new` в C++/Java/C#).
    
- **Особенность:** Она очень большая, но медленная. Вы сами решаете, когда взять память и когда её вернуть (`free`). Если забыть вернуть — случится **утечка памяти**.
### 4. Memory Mapping Region

- **Что там:** Сюда «проектируются» файлы с диска (например, когда программа загружает большую базу данных) или **динамические библиотеки** (`.dll` в Windows или `.so` в Linux). Это позволяет обращаться к файлу на диске так же просто, как к массиву в памяти.
### 5. Stack (Стек) — растет ВНИЗ ⬇️

- **Что там:** Локальные переменные внутри функций и адреса возврата.
    
- **Как работает:** Как стопка тарелок. Вызвали функцию — положили «тарелку» (фрейм) наверх. Функция закончилась — тарелку сняли.
    
- **Особенность:** Стек очень быстрый, но маленький. Если сделать бесконечную рекурсию, произойдет **Stack Overflow** (переполнение стека).
### 6. Kernel Space (Пространство ядра)

- Это «запретная зона». Она занимает верхнюю часть адресов (на схеме помечено как 1GB из 4GB общего пространства).
    
- Обычная программа не может туда заглянуть. Там живут «мозги» самой операционной системы.

>_«Почему на схеме адреса начинаются не с 0, а с какого-то смещения (например, 0x08048000)?»_ **Ответ:** Нулевой адрес (NULL) специально оставляют пустым и недоступным. Это сделано для того, чтобы если в программе есть ошибка (обращение по пустому указателю), она тут же «упала», а не начала читать системные данные.

То есть-
### 1. Ловушка для ошибок (Null Pointer Dereference)

Самая частая ошибка в программировании (особенно на C/C++) — это когда ты создаешь указатель, но забываешь дать ему адрес. По умолчанию такой указатель часто равен **0** (тот самый `NULL`).

Если бы по адресу `0` лежал код твоей программы, то при попытке прочитать данные по «пустому» адресу программа бы просто прочитала кусочек своего кода и пошла работать дальше, выдавая случайный бред. Это **«тихая смерть»**: программа работает неправильно, и ты не понимаешь почему.

**Как это работает сейчас:**

1. Программа пытается обратиться к адресу `0`.
    
2. Процессор лезет в таблицу страниц и видит: «На этот участок памяти (0x00000000) наложен запрет, там ничего нет».
    
3. ОС мгновенно прибивает программу с ошибкой **Segmentation Fault** или **Access Violation**.
    
4. **Итог:** Ты сразу видишь, в какой строчке кода накосячил. Это «громкая смерть», которая спасает от долгих поисков бага.
    

### 2. Защита от «дурака» и вирусов

Если бы в самом начале (адрес 0) лежали важные системные данные, любая ошибка новичка могла бы их перезаписать. Оставляя «дырку» в начале адресов, ОС гарантирует, что случайный доступ по низким адресам не приведет к порче данных.

![[Pasted image 20251229213826.png]]


![[Pasted image 20251228223238.png]]

### 1. Состояния процесса (Кружочки на схеме)

- **Ready (Готов):** Процесс полностью готов к работе. У него есть память, есть данные, он просто стоит в очереди и ждет, когда ОС скажет: «Твое время пришло, заходи на процессор».
    
- **Run (Исполнение):** Процесс прямо сейчас находится «внутри» процессора. Его команды выполняются, регистры меняются.
    
- **Wait (Ожидание / Блокировка):** Самое интересное состояние. Процесс не может работать, даже если процессор свободен. Почему? Потому что он ждет **события** (например, пока пользователь нажмет кнопку, или пока с диска прочитается файл).
    

---

### 2. Операции и переходы (Стрелочки)

Теперь свяжем это с твоим списком операций:

- **Создание / Выход:** * Стрелка сверху вниз в **Ready** — ОС выделила ресурсы, и новый процесс встал в очередь.
    
    - Стрелка из **Run** вниз в **Выход** — программа закончила работу (или ты её закрыл) и вернула ресурсы системе.
        
- **Исполняется / Прерван:**
    
    - **Из Ready в Run:** Операция «Избран на исполнение». Планировщик ОС выбрал счастливчика.
        
    - **Из Run в Ready:** «Произошло прерывание». Это **Квантование времени**. ОС говорит: «Ты поработал свои 10 миллисекунд, дай другим шанс». Процесс не виноват, его просто попросили подождать в очереди.
        
- **Заблокирован / Разблокирован:**
    
    - **Из Run в Wait:** Процесс сам говорит: «Я засыпаю («Спит»), пока не придут данные из сети». Он добровольно покидает процессор.
        
    - **Из Wait в Ready:** «Произошло событие ОС». Данные пришли! Но процесс не прыгает сразу в Run, он сначала встает в очередь **Ready**, чтобы честно дождаться своей смены.

>**Истинный параллелизм:** Когда у тебя 2 ядра и 2 процесса. Они реально бегут бок о бок.
   **Конкурентность (Concurrency):** Когда у тебя 1 ядро и 2 процесса. Они делают вид, что бегут вместе, постоянно перебивая друг друга.

---
Планирование процессов (Process Scheduling) 
Задачи: 
- Многопрограммирование (multiprogramming) — всегда держать процессор занятым каким-то процессом, чтобы не простаивал. 
- Разделение времени (time sharing) — переключать процессор между процессами так быстро, чтобы казалось, что они работают одновременно и пользователи могли взаимодействовать с каждым. 
👉 Для этого нужен диспетчер процессов (scheduler), который выбирает, какой процесс получит процессор. Каждое ядро может выполнять только один процесс одновременно. . 

Баланс I/O-bound и CPU-bound процессов 
- I/O-bound процесс — больше ждёт ввода/вывода, чем считает. Пример: браузер. 
- CPU-bound процесс — много вычисляет, редко делает I/O. Пример: видеокодек. 

Планировщик должен балансировать оба типа: не дать «засорить» процессор только вычислительными задачами и не дать простаивать CPU, пока все ждут I/O.

![[Pasted image 20251228223311.png]]
### 1. Создание и выход процесса

Это границы жизни процесса.

- **Создание:** ОС выделяет память под сегменты (Text, Data, Stack, Heap), создает уникальный идентификатор **PID** и заводит структуру **PCB** (Process Control Block), куда будет записывать всё о процессе.
    
    - _Пример:_ В Linux это системный вызов `fork()` (копирование родителя) или `exec()` (замена кода на новый).
        
- **Выход (Exit):** Процесс добровольно (`exit`) или принудительно (ошибка/kill) завершается. ОС освобождает его память, закрывает открытые им файлы, но оставляет «запись-отчет», пока родитель её не заберет (помнишь про зомби?).
    

### 2. Исполняется и прерван

Это динамика работы на процессоре.

- **Процесс исполняется (Run):** Его инструкции загружены в конвейер процессора, счетчик команд ($IP$) постоянно растет.
    
- **Процесс прерван (Interrupt/Preempt):** Это происходит не по воле процесса. У ОС есть «таймер», который говорит: «Эй, ты занял процессор слишком надолго».
    
    - **Механизм:** Процессор получает сигнал прерывания, сохраняет текущие регистры в память и передает управление планировщику ОС. Процесс переходит обратно в состояние **Ready**.
        

### 3. Заблокирован и разблокирован

Это работа с внешним миром (диск, сеть, клавиатура).

- **Заблокирован (Block/Sleep):** Процесс сам просит ОС: «Мне нужны данные из файла, я подожду». Поскольку чтение с диска в миллионы раз медленнее работы процессора, ОС убирает процесс с процессора, чтобы тот не простаивал зря.
    
- **Разблокирован (Wake up):** Как только контроллер диска сообщает: «Данные готовы!», ОС переводит процесс из **Wait** в **Ready**. Теперь он снова может претендовать на очередь к процессору.
    

---

### 💡 Интересная фишка: «Переключение контекста» (Context Switch)

Когда процесс прерывается или блокируется, происходит самая «дорогая» операция в ОС — Context Switch.

ОС должна:

1. Сохранить значения всех регистров текущего процесса в его PCB.
    
2. Загрузить значения регистров другого процесса из его PCB.
    
    Это занимает время, поэтому слишком частое переключение (если процессов тысячи) может заставить компьютер тормозить, даже если процессор мощный.
### смешные факты

**Зомби (Zombie Process)** — это процесс, который уже завершил свое выполнение, но все еще занимает место в таблице процессов ОС.

---

### 1. Как рождается «Зомби»?

В системе существует строгая иерархия: у каждого процесса есть «родитель» (тот, кто его запустил).

1. Когда процесс заканчивает работу, он умирает. Его память, открытые файлы и ресурсы освобождаются.
    
2. Но в таблице процессов ОС остается запись о нем. В этой записи хранится **код завершения** (например, «я закончил работу успешно» или «я вылетел с ошибкой»).
    
3. Процесс превращается в «Зомби» и ждет, когда родитель «считает» этот код завершения.
    

### 2. Почему они «неупокоенные»?

Процесс называется зомби, потому что:

- Он **мертв**: он не занимает место в оперативной памяти (его _Stack_ и _Heap_ удалены) и не потребляет процессорное время.
    
- Он **существует**: его идентификатор (PID) и запись в системной таблице всё еще заняты.
    

### 3. Как их «убить»?

В этом и заключается главная фишка: **зомби нельзя убить командой `kill`**, потому что они уже мертвы! Единственный способ убрать зомби — это заставить родителя выполнить системный вызов `wait()`. Это действие официально подтверждает, что родитель получил отчет, после чего ОС окончательно стирает запись о процессе.

**Если родитель «плохой»** (написан с ошибкой и не вызывает `wait`), зомби будет висеть вечно. **Что делать?** Нужно убить самого родителя. Тогда зомби станут «сиротами», их усыновит самый главный процесс в системе (в Linux это `init` или `systemd`), который очень ответственный и тут же вызовет `wait()`, очистив систему

>Многие думают, что раз зомби не едят память, то они безобидны. Но это не так. Количество записей в таблице процессов ОС **ограничено**. Если программа-родитель будет бесконечно плодить зомби, в какой-то момент в системе закончатся свободные PID (идентификаторы), и ты не сможешь запустить даже простейший калькулятор — ОС скажет: «Мест нет!».

![[Pasted image 20251229220643.png]]

### Что такое PCB (Блок управления процессом)?

Это структура данных в памяти **ядра** (Kernel Space), в которой ОС хранит абсолютно всё, что нужно для возобновления работы процесса после его остановки.

### 1. Регистровый контекст

- **Указатель инструкции (IP / Program Counter):** Адрес следующей команды. Когда процесс снова получит процессор, он начнет именно с этой строчки кода.
    
- **Содержание регистров:** Все промежуточные вычисления (сумма в регистре AX, индекс в CX и т.д.) сохраняются здесь, чтобы не начинать расчеты заново.
    

### 2. Системный контекст (Учетная книга ОС)

Тут лежат данные, которые нужны самой ОС, чтобы «рулить» процессом:

- **Состояние процесса:** Тот самый статус из предыдущего слайда (Ready, Run или Wait).
    
- **Данные для планирования:** Приоритет процесса. Чем выше приоритет, тем чаще планировщик будет выбирать этот процесс из очереди.
    
- **Учетная информация:** Кто запустил (User ID), какой номер у процесса (PID), сколько процессорного времени он уже «съел».
    
- **Управление памятью:** Ссылки на таблицы страниц, чтобы ОС знала, где в физической оперативной памяти лежат Stack, Heap и Code этого конкретного процесса.
    
- **Объекты ОС:** Список открытых файлов, запущенных таймеров или открытых сетевых сокетов.

 >**Контекст процесса** — это сумма системных данных (PCB) и самого исполняемого кода с данными в памяти.

>_«Где физически хранится PCB — в памяти самого процесса или в памяти ядра?»_ **Ответ:** **В памяти ядра (Kernel Space).** Это критически важно для безопасности. Если бы PCB лежал в памяти процесса, программа могла бы сама себе «накрутить» приоритет или подсмотреть, какие файлы открыты у соседа. Доступ к PCB имеет только операционная система.

![[Pasted image 20251229222243.png]]

### 1. Создание PCB и состояние "New"

Первым делом ОС заводит «анкету» (тот самый **PCB**, который мы разобрали раньше). В поле «Состояние» записывается значение **New**. На этом этапе процесса как такового еще нет, есть только намерение его создать.

### 2. Присвоение PID (Process ID)

ОС выдает процессу уникальный номер — **PID**.

PID нужен для:

- сигналов;
- планирования;
- управления ресурсами.

🧠 Важная деталь:

> PID уникален только в данный момент времени, после завершения может быть переиспользован.

- **Фишка:** В системе не может быть двух процессов с одинаковым PID одновременно. Это как номер паспорта. По этому номеру ОС будет понимать, кому выделять ресурсы, а кого «убивать» по команде пользователя.

### 3. Выделение ресурсов ОС

Здесь ядро выделяет физическую память под будущие сегменты (стек, куча) и инициализирует структуры для управления файлами и устройствами.

### Какие ресурсы выделяются

- виртуальное адресное пространство;
- таблицы страниц;
- системные структуры;
- минимальный набор файловых дескрипторов.

📌 Пока:

- процесс **ещё не выполняется**;
- но инфраструктура под него уже есть.
### 4. Заполнение адресного пространства и точка входа

### Что реально происходит

- исполняемый файл загружается в память:
    - `.text`
    - `.data`
    - `.bss`
- настраивается виртуальная память;
- **указатель инструкции (PC)** ставится: на точку входа программы.

Это критический момент:

- В память копируется машинный код программы с диска.
    
- В PCB записывается **указатель инструкции (IP)**. Он указывает не на начало файла, а на первую выполняемую инструкцию — функцию **main()**.
    

### 5. Окончание заполнения PCB

Записываются все остальные данные: имя пользователя, приоритет, лимиты по памяти и так далее.

В PCB окончательно записываются:

- регистры (начальные значения);
- данные планирования;
- данные управления памятью;
- учетная информация;
- ссылки на ресурсы ОС.

📌 Теперь PCB полностью описывает процесс.
### 6. Состояние "Ready"

Процесс официально считается созданным. Его состояние меняется на **Ready**. Теперь он попадает в очередь планировщика и просто ждет, когда процессор освободится, чтобы начать работать.
### на вопрос от феди

Когда вызывается `fork()`, новый процесс (ребенок) получает:

- **Точную копию сегментов данных, стека и кучи:** Если у папы-процесса была переменная `x = 10`, у ребенка тоже будет `x = 10`. Но они живут в разных адресных пространствах — если ребенок изменит свою `x`, у папы она останется прежней.
    
- **Тот же программный код (Text segment):** Код не копируется физически (зачем дублировать одно и то же в памяти?), а просто используется обоими процессами совместно в режиме "только чтение".
    
- **Тот же Program Counter (Указатель инструкции):** Это самое забавное. Ребенок «просыпается» не в начале программы, а **в той же самой строчке кода**, где был вызван `fork()`.
### Почему сделали именно так? (3 главные причины)

#### 1. Удобство настройки перед запуском

Представь, что ты хочешь запустить программу, но заранее перенаправить её вывод в текстовый файл, а не на экран.

- С `fork()` это просто: ты клонируешься -> в ребенке закрываешь стандартный вывод и открываешь файл -> вызываешь замену кода (`exec`).
    
- Если бы была одна команда «создать процесс сразу с новой программой», она бы обросла сотнями настроек (аргументы, файлы, права, переменные окружения). `fork` позволяет настроить всё в «песочнице» ребенка перед стартом.
    

#### 2. Многопоточность «на минималках» (до появления потоков)

Раньше потоков не было. Если серверу нужно было обработать 10 клиентов одновременно, он просто делал 10 `fork()`. Каждый клон уже знал всё о состоянии сервера и мог сразу начать работать с клиентом, не тратя время на инициализацию.

#### 3. Экономия памяти (Copy-on-Write / Копирование при записи)

На самом деле, ОС — хитрая. Она не копирует всю память сразу физически (это было бы ужасно долго). Она говорит: «Окей, вы оба смотрите на одни и те же физические страницы памяти».

- Если оба процесса только читают данные — всё супер, память не тратится.
    
- Как только один из них (например, ребенок) решит **записать** что-то в переменную, ОС в этот момент создаст реальную копию только этой маленькой страницы памяти. Это называется **CoW (Copy-on-Write)**. Именно благодаря этому `fork()` работает мгновенно.
    
то есть
#### 2.1. Код (`.text`)

- **не копируется физически**;
- один и тот же код отображается в оба процесса;
- сегмент кода — `read-only`.
#### 2.2. Данные, heap и stack

### Логически:

- **копируются полностью**;
- значения переменных одинаковые;
- стек вызовов одинаковый.
### Физически:

❌ **не копируются сразу**.

Используется механизм **Copy-On-Write (COW)**.

### Что такое Copy-On-Write (COW)

### Как это работает:

1️⃣ После `fork()`

- родитель и потомок **делят одни и те же физические страницы**
- страницы помечены как «только для чтения»

2️⃣ Если один процесс **пытается изменить страницу**

- MMU ловит запись
- ОС **создаёт копию страницы**
- изменение идёт уже в копию

📌 До первого изменения — **память общая**.

---

### 🎓 Экзаменационная фишка (главный вопрос по fork):

**Преподаватель:** _«Как программа понимает, кто она — папа или ребенок, если код и место остановки у них одинаковые?»_ **Ответ:** По результату, который возвращает функция `fork()`.

- В **родительском** процессе `fork()` возвращает **PID ребенка** (число больше 0).
    
- В **дочернем** процессе `fork()` возвращает **0**. Программист просто пишет `if (pid == 0) { /* я ребенок */ } else { /* я папа */ }`.

![[Pasted image 20251230170654.png]]

![[Pasted image 20251229222306.png]]

# Сравнительная таблица

|Критерий|Unix|Windows|
|---|---|---|
|Создание|fork + exec|CreateProcess|
|Копирование|логическое|нет|
|Код|общий|загружается|
|Иерархия|есть|нет|
|Родитель|важен|формален|
|Гибкость|высокая|ниже|
|Простота|ниже|выше|

### 1. Unix-путь: Метод копирования (Fork)

В Unix-системах (и Linux) нельзя просто «создать» процесс из пустоты. Его можно только «отпочковать» от уже существующего.

- **Дубликат (Fork):** Как мы уже обсуждали, создается полная копия родителя. Новое адресное пространство получает свои копии стека и кучи, но **код (text segment) остается общим** (он «шарится» между процессами в режиме Read-Only, чтобы не занимать лишнюю память).
    
- **Иерархия:** В Unix каждый процесс знает своего «папу» (PPID — Parent PID). Все процессы в системе образуют огромное дерево, корнем которого является самый первый процесс — `init` (или `systemd`).
    
    - _Зачем:_ Это удобно для управления. Если ты закрываешь терминал, он может автоматически «прибить» все запущенные в нем программы, потому что знает, что они его «дети».
        

### 2. Windows-путь: Прямое создание (CreateProcess)

В Windows всё иначе. Там нет обязательного клонирования.

- **Прямое создание:** Функция `CreateProcess` сразу берет указанный `.exe` файл, выделяет под него чистую память и загружает код туда с нуля.
    
- **Независимость:** В Windows нет такой жесткой «семейной» привязанности, как в Unix. Процесс-родитель, конечно, может следить за ребенком, но по умолчанию они считаются более независимыми единицами.
    
    - _Плюс:_ Это чуть более прямолинейно, если тебе нужно просто запустить новую программу. Не нужно делать лишний шаг «клонирование -> замена кода».
![[Pasted image 20251229222327.png]]

### 1. Инициализация системы (Boot)

Когда ты нажимаешь кнопку включения компьютера, ОС загружается и сама создает первые процессы.

- **В Unix/Linux:** Это процесс `init` (или `systemd`), который имеет PID 1. Из него потом «вырастают» все остальные процессы: графическая оболочка, службы сети и т.д..
    
- **Зачем:** Чтобы компьютер был готов к работе еще до того, как ты ввел пароль.
    

### 2. Работающий процесс осуществляет системный вызов

Это тот самый случай с **fork()** (в Unix) или **CreateProcess()** (в Windows), который мы обсуждали.

- **Пример:** Ты открыл браузер. Когда ты кликаешь по ссылке, основной процесс браузера порождает новый процесс для отрисовки новой вкладки.
    
- **Суть:** Программы сами «плодятся», чтобы делегировать задачи.
    

### 3. Создание процесса по запросу пользователя

Это самый привычный нам способ.

- **Пример:** Двойной клик по иконке игры на рабочем столе или ввод команды `python script.py` в консоли.
    
- **Механизм:** Пользователь дает команду оболочке (Shell или Explorer), а оболочка уже просит ядро ОС запустить процесс.
    

### 4. Инициализация пакетного задания (Batch Job)

Это привет из мира серверов и суперкомпьютеров.

- **Как это работает:** Ты не сидишь и не ждешь у монитора. Ты создаешь список задач («пакет») и отдаешь его системе. ОС сама решит, когда есть свободные ресурсы (например, ночью), и создаст процессы для выполнения этих задач.
    
- **Пример:** Рендеринг сложной 3D-анимации на ферме или банковские расчеты в конце месяца.
    
- **Архивация данных (Backup):** В крупных компаниях сервера каждую ночь в 03:00 запускают процесс копирования всех данных. Никто не нажимает кнопку «ОК» — система сама создает процесс по расписанию.
    
- **Научные расчеты:** Ученый отправляет на суперкомпьютер задачу: «Просчитать движение 1000 астероидов». Система ставит это в очередь и запустит процесс только тогда, когда освободятся мощности.
    
- **Компиляция огромного проекта:** Когда программисты собирают Windows или игру уровня GTA, это может занимать часы. Они запускают «скрипт сборки» (пакет), который по очереди запускает сотни процессов компиляции.
---

### 💡 Интересная фишка: Демоны и Службы

Процессы, которые создаются при **инициализации системы** и работают в фоне, не имея видимого окна, называются **Демонами** (в Linux, например, `sshd` для удаленного доступа) или **Службами** (в Windows, например, служба печати). Они — «невидимые рабочие», которые поддерживают жизнь системы.

### 🎓 Экзаменационная фишка:

**Преподаватель:** _«Может ли пользовательский процесс создаться без участия другого процесса?»_ **Ответ:** **Нет.** Все процессы (кроме самого первого, созданного ядром при загрузке) создаются каким-то другим процессом-родителем. Даже когда ты кликаешь на иконку, процесс «Рабочий стол» или «Проводник» делает системный вызов для создания нового процесса.

![[Pasted image 20251229222346.png]]

### 1. Изменение состояния на “Exit”

Как только программа выполняет последнюю инструкцию или получает сигнал на закрытие, планировщик переводит её в состояние **Exit** (иногда называется _Terminated_). С этого момента процесс больше не претендует на процессорное время.

### 2. Освобождение ресурсов

Это самый важный этап для здоровья всей системы. ОС забирает обратно всё, что «выдавала в аренду»:

- **Оперативная память:** Очищаются сегменты Stack и Heap.
    
- **Файлы:** Если программа забыла закрыть какой-то текстовый файл или базу данных, ОС закрывает их принудительно.
    
- **Устройства:** Если процесс занимал видеокарту или сетевой порт, они помечаются как свободные.
    

### 3. Очистка элементов PCB (частичная)

Из Блока Управления Процессом удаляется всё, что было нужно для работы: регистровый контекст (состояние счетчика команд, значения в регистрах), таблицы страниц памяти. Процессу больше не нужно «помнить», где он остановился, потому что он больше не вернется в строй.

### 4. Сохранение информации о причине завершения

Это тот самый момент, который превращает процесс в **Зомби**, о которых мы говорили раньше.

- В структуре PCB остается **код возврата** (Exit Code).
    
- **Зачем:** Чтобы родительский процесс мог узнать, как дела у «ребенка». Например, код `0` обычно означает «все прошло успешно», а любой другой код (например, `1` или `127`) — ошибку.

Иногда процесс не хочет умирать сам (завис). В Unix-системах есть команда `kill -9`. Цифра `9` означает сигнал **SIGKILL**. В этом случае ОС не просит процесс закрыться, а просто мгновенно обрывает его выполнение и переходит к этапу освобождения ресурсов. Это как если бы официант не ждал, пока гость доест, а просто выставил бы его за дверь вместе со стулом.

### 🎓 Экзаменационная фишка:

**Преподаватель:** _«Когда PCB окончательно удаляется из памяти ядра?»_ **Ответ:** Только после того, как родительский процесс выполнит системный вызов `wait()` и прочитает ту самую «причину завершения». До этого момента запись в системной таблице (скелет процесса) будет существовать.

![[Pasted image 20251229222401.png]]

### 1. Выбор процесса планировщиком (Scheduler)

Процессор не может сам решить, что ему делать. В ядре ОС живет специальный алгоритм — **Планировщик**.

- Он смотрит на очередь процессов в состоянии **Ready**.
    
- Выбирает одного «счастливчика» на основе приоритетов или того, кто дольше всех ждал.
    

### 2. Перевод в состояние “Run”

Как только выбор сделан, ОС меняет флаг в PCB этого процесса с **Ready** на **Run**. Теперь система официально считает этот процесс исполняющимся.

### 3. Восстановление регистров из PCB

Это самый технически сложный момент. Процессор — это «чистый лист». Чтобы процесс продолжил работу так, будто его и не прерывали, ОС берет данные из его **PCB** и физически записывает их в ячейки процессора (регистры):

- Загружает значения промежуточных вычислений в регистры общего назначения.
    
- Загружает данные о сегментах памяти.
    

### 4. Прыжок к инструкции (IP)

Финальный аккорд: ОС записывает в регистр **Instruction Pointer (IP)** тот адрес, который был сохранен в PCB при прошлом прерывании.

- Процессор видит адрес в IP, летит туда и выполняет следующую команду.
    
- **Процесс «ожил»!** Он даже не подозревает, что его прерывали — для него время просто замерло и снова пошло.
    

---

>_А что происходит с регистрами того процесса, который только что ушел с процессора?»_ **Ответ:** Происходит обратный процесс — **Save State**. Перед тем как загрузить новые регистры, ОС обязана скопировать текущее состояние «уходящего» процесса в его PCB. Если этого не сделать, все его вычисления просто испарятся, и он никогда не сможет продолжить работу.

![[Pasted image 20251229222420.png]]

### Разбор алгоритма прерывания

#### 1. Сохранение регистров в PCB

Действие: Самое первое, что происходит при возникновении прерывания, — это сохранение критически важной части состояния прерванного процесса (например, счётчика команд и указателя стека) в его Блоке Управления Процессом (PCB). 

Цель: Это минимальное сохранение обычно выполняется аппаратно (или на очень низком уровне, в ассемблерной вставке) и является абсолютно необходимым минимумом, чтобы не потерять место, на котором остановился процесс. 

#### 2. Прыжок по таблице прерываний (IVT)

Действие: Используя номер (вектор) прерывания, процессор находит в специальной структуре данных ядра (таблице прерываний) адрес, по которому находится функция-обработчик этого конкретного прерывания. 

Цель: Передача управления из пользовательского кода в ядро ОС для начала обработки события.

#### 3. Сохранение динамических частей контекста

Действие: После того как управление передано коду ядра, выполняется полное сохранение оставшейся части контекста процесса (всех общих регистров, состояния FPU/SIMD и других регистров состояния). 

Цель: Гарантировать, что весь контекст прерванного процесса полностью сохранен. Это необходимо, чтобы после завершения работы ядра процесс мог быть безошибочно возобновлён с того самого места, где он был прерван
#### 4. Переключение на стек ядра (Kernel Stack)

Действие: Ядро перестаёт использовать стек прерванного процесса (пользовательский стек). Вместо этого оно переключается на собственный, выделенный и защищённый стек ядра. 

Цель: Код ядра не может доверять пользовательскому стеку (он может быть повреждён или содержать ошибки). Переключение на стек ядра обеспечивает безопасную и надежную среду для выполнения критически важных операций ОС. 
    

#### 5. Обработка прерывания

Действие: Выполняется основная работа обработчика прерывания (interrupt handler). 

Цель: Выполнить задачу, вызвавшую прерывание: 
- Если это таймерное прерывание, ядро запускает планировщик. 
- Если это I/O прерывание, ядро перемещает данные с устройства. 
- Если это системный вызов, ядро выполняет запрошенную операцию (например, открытие файла).

#### 6. Переключение на стек процесса

Действие: После завершения обработки прерывания, если ОС решает возобновить другой процесс (или, в некоторых случаях, тот же самый), ядро готовит среду для возврата в пользовательский режим, переключаясь на стек того процесса, который должен быть запущен следующим. 

Цель: Подготовка к восстановлению контекста и передаче управления обратно в пользовательский режим.

#### 7. Изменение состояния в “Ready”

Действие: Состояние прерванного процесса в его PCB меняется с "Running" (Выполняется) на "Ready" (Готов). 

Цель: Поместить процесс обратно в Очередь Готовых, чтобы он мог ожидать следующего выделения процессорного времени. Этот шаг обычно выполняется после того, как прерывание, вызванное, например, истечением кванта времени (time slice), принудительно вытесняет процесс с ЦП, чтобы дать возможность поработать другим процессам. 

>**Преподаватель:** _«Что такое 'таблица прерываний' и кто её заполняет?»_ **Ответ:** Это массив указателей в памяти, который связывает номер прерывания с кодом его обработки. Её заполняет **ядро ОС** при загрузке системы. Это «телефонная книга» для процессора, чтобы он знал, кому «звонить», когда что-то случилось.

![[Pasted image 20251229222436.png]]

1. Сохранение системного контекста PCB 
     Действие: Ядро ОС сохраняет текущее состояние выполняющегося процесса (значения регистров ЦП, счётчик команд, информацию об управлении памятью и т. д.) в его Блоке Управления Процессом (PCB). 
     
     Цель: Полностью зафиксировать состояние процесса в момент его прерывания. Это необходимо для того, чтобы, когда блокировка закончится, процесс мог быть бесшовно возобновлён с того самого места, где он остановился. 
2. Обработка системного вызова 
       Действие: Ядро выполняет логику, связанную с запрошенным процессом системным вызовом. 
       Причина блокировки: В данном случае предполагается, что системный вызов связан с операцией, которая не может быть завершена немедленно. 
       Самые частые примеры:  
       - Ввод/вывод (I/O): Процесс вызывает read() или write() и должен ждать, пока медленное устройство (диск, сеть) завершит операцию. 
       - Синхронизация: Процесс вызывает wait() и должен ждать завершения дочернего процесса или освобождения критического ресурса (мьютекса, семафора). 
       
3. Перевод состояния процесса в “Wait” (Ожидание) 
     Действие: После того как ядро определило, что процесс должен ждать, оно меняет поле состояния в его PCB с "Running" (Выполняется) на "Waiting" (Ожидание). 
     
     Цель: 
     -  Удаление из очереди готовых: Процесс убирается из очереди процессов, готовых к выполнению, так как он всё равно не может работать. 
     -  Перемещение в очередь ожидания: Процесс помещается в соответствующую очередь ожидания (например, очередь ожидания диска или очередь ожидания завершения дочернего процесса). 
     
После выполнения этих шагов ядро вызывает планировщик ЦП, чтобы выбрать из Очереди Готовых следующий процесс для выполнения. Таким образом, ЦП не простаивает, пока заблокированный процесс ждёт завершения I/O.

![[Pasted image 20251229222452.png]]

**Механизм Разблокировки Процесса (Wakeup Mechanism)** 🔄

Когда процесс находится в состоянии "Waiting", он помещён в специальную очередь ожидания, привязанную к конкретному ресурсу (например, к диску или сетевому порту). Следующие шаги описывают, как ядро узнаёт о завершении ожидания и переводит процесс обратно в активный цикл:

1. **Ядро узнает про событие из очереди внутри OS**
    

- **Действие:** Это инициирующий шаг. Ядро получает аппаратное прерывание (например, от контроллера диска, сигнализирующее, что чтение завершено) или внутренний сигнал ОС.
    
- **Цель:** Уведомить ядро о том, что некий ресурс или операция, которая ранее блокировала один или несколько процессов, теперь свободна или завершена.
    

2. **Проверяет процессы, ожидающие событие**
    

- **Действие:** Ядро обращается к очереди ожидания, которая связана с только что произошедшим событием.
    
- **Цель:** Идентифицировать все процессы, которые были помещены в эту очередь и теперь могут продолжить работу.
    

3. **Если событие для процессов произошло, то событие обрабатывается (input/output)**
    

- **Действие:** Ядро выполняет финальные действия, связанные с событием. Если это I/O, ядро, например, копирует запрошенные данные из буфера устройства в адресное пространство процесса.
    
- **Цель:** Завершить операцию, которую процесс ранее запросил через системный вызов.
    

4. **и состояние процесса становится “Ready”**
    

- **Действие:** Состояние процесса в его PCB меняется с "Waiting" (Ожидание) на "Ready" (Готов).
    
- **Цель:** Процесс разблокирован. Теперь он перемещается из очереди ожидания обратно в Очередь Готовых (Ready Queue), где он будет ждать, пока планировщик ЦП выделит ему процессорное время.
    

5. **иначе процессы все еще ждут...**
    

- **Действие:** Если по какой-то причине событие не разблокировало процесс (например, это было ложное прерывание или событие относилось к другому ресурсу), процесс остаётся в очереди ожидания.
    
- **Цель:** Поддерживать блокировку, пока условие не будет истинным.
    

Этот механизм обеспечивает, что Центральный Процессор (ЦП) не тратит время на ожидание медленных устройств — пока один процесс заблокирован, ядро занимается обработкой других процессов из очереди "Ready", а разблокированный процесс возвращается в эту очередь только тогда, когда он действительно готов к выполнению.

![[Pasted image 20251230185900.png]]

Кооперация (Cooperation) — это совместное выполнение задач несколькими процессами или потоками, при котором они активно обмениваются данными или координируют свои действия.

### 1. Повышение скорости решения задач (Параллелизм)

В современных многоядерных системах одну большую задачу можно разделить на части и выполнять их одновременно на разных ядрах процессора.

- **Threads (Потоки) vs. Processes (Процессы):** * **Потоки** — это «легковесная» кооперация. Они живут внутри одного процесса и по умолчанию имеют доступ к общим данным. Это очень быстро.
    
    - **Процессы** — это «тяжеловесная» кооперация. Чтобы заставить два процесса работать над одной задачей, ОС должна помочь им обменяться результатами.
        
- **Пример:** Рендеринг видео. Одно ядро считает первые 10 минут, второе — вторые 10 минут. Затем результаты склеиваются. Без кооперации они бы никогда не узнали друг о друге.
    

### 2. Совместное использование данных (Shared Data)

Иногда нескольким независимым программам нужен доступ к одному ресурсу.

- **Пример:** База данных. У вас может быть процесс, который записывает данные о продажах, и процесс, который строит графики для директора. Оба процесса кооперируются, обращаясь к одному и тому же файлу или сегменту памяти, чтобы информация всегда была актуальной.
    

### 3. Модульная конструкция (Микросервисы)

Это современный стандарт разработки крупных систем. Вместо одной гигантской программы создается много маленьких «модулей-процессов».

- **Микросервисы:** Ты абсолютно верно подметил. Например, в интернет-магазине один процесс отвечает за корзину, другой за оплату, третий за поиск.
    
- **Зачем это нужно:** Если «повиснет» поиск, ты всё равно сможешь оплатить уже добавленный товар. Процессы кооперируются, пересылая друг другу запросы («Пользователь нажал 'купить', вот его ID»), но остаются независимыми.
    
### Примеры

🔹 Микросервисы:

- сервис авторизации,
- сервис билетов,
- сервис оплаты.
    
🔹 Браузер:

- процесс UI,
- процесс рендеринга,
- процесс GPU.
### 4. Удобство работы (Shell Piping)

Это классика командной строки (Linux/Unix). Оператор `|` (pipe) позволяет соединять процессы в цепочку.

- **Пример:** `cat logs.txt | grep "Error"`.
    
    - Первый процесс (`cat`) читает файл.
        
    - Второй процесс (`grep`) фильтрует только ошибки.
        
- Они кооперируются в реальном времени: данные вытекают из одного и тут же втекают в другой.
    

---

### Механизмы обмена информацией (Как они это делают?)
#### А. Общая память (Shared Memory) — «Одна тетрадь на двоих»

ОС выделяет кусок RAM, который виден обоим процессам.

- **Плюс:** Невероятно быстро. Процесс А записал — процесс Б тут же прочитал.
    
- **Минус:** Сложно. Нужно следить, чтобы они не начали писать в одну ячейку одновременно (это называется «состояние гонки»).
    

#### Б. Передача сообщений (Message Passing) — «Электронная почта»

Процессы обмениваются пакетами данных через ядро ОС.

- **Плюс:** Очень надежно и безопасно. ОС сама следит за очередью сообщений. Это единственный способ кооперироваться, если процессы запущены на разных компьютерах в сети.
    
- **Минус:** Медленнее, так как ядру приходится копировать данные из памяти одного процесса в свою, а потом в память другого.

![[Pasted image 20251230185918.png]]

Канал (Pipe) действует как проводник, позволяя двум процессам обмениваться информацией. Каналы были одним из первых механизмов межпроцессного взаимодействия (IPC) в ранних системах UNIX. Обычно они предоставляют один из самых простых способов взаимодействия процессов друг с другом, хотя у них также есть некоторые ограничения.

### 1. Инициализация (Establishment)

Перед тем как данные потекут от процесса А к процессу Б, система должна решить: нужен ли «ритуал» открытия канала?

- **Динамическая инициализация:** Процессы должны сначала «договориться». Один создает канал (например, `shared memory segment`), второй к нему подключается. Если канала нет — передача невозможна.
    
- **Статическая (без инициализации):** Каналы существуют всегда (например, системная шина или предопределенные порты). Процесс просто кидает данные в сторону другого процесса, не проверяя, готов ли тракт.
    

### 2. Способы адресации (Addressing)

Как именно отправитель находит получателя в дебрях оперативной памяти?

- **Прямая адресация (Direct):** В коде прописано имя или ID процесса.
    
    - **Симметричная:** Процесс А говорит: «Шлю Процессу Б», а Процесс Б говорит: «Жду от Процесса А». Оба должны знать друг друга по именам.
        
    - **Асимметричная:** Процесс А говорит: «Шлю Процессу Б», а Процесс Б говорит: «Жду сообщение от **любого**». Это удобно для серверов, которые обслуживают сотни разных клиентов.
        
- **Косвенная адресация (Indirect):** Процессы общаются через «посредника» — **почтовый ящик (Mailbox)** или **порт**.
    
    - Процесс А кладет данные в объект `X`.
        
    - Процесс Б забирает данные из объекта `X`.
        
    - **Зачем это нужно?** Это позволяет менять процессы на ходу. Мы можем заменить Процесс Б на другой, и Процесс А даже не заметит разницы, так как он всё так же кладет письма в ящик `X`.
        

### 3. Пределы средств связи (Capacity and Direction)

Здесь мы описываем «физику» канала.

- **Количество участников:** Канал может быть **unicast** (строго между двумя), **multicast** (группа процессов) или **broadcast** (всем процессам в системе).
    
- **Направленность:**
    
    - **Simplex (Симплекс):** Односторонняя связь. Только от А к Б. (Например, системный логгер, который только записывает данные от программ).
        
    - **Half-duplex (Полудуплекс):** Данные ходят в обе стороны, но не одновременно. Если Процесс А пишет, Процесс Б обязан ждать и только слушать.
        
    - **Full-duplex (Полный дуплекс):** Два независимых подканала. А пишет Б, и в ту же миллисекунду Б пишет А. Это самый эффективный, но сложный в реализации способ.
        

### 4. Особенности каналов (Buffering and Models)

#### ○ Буферизация (Capacity)

Где находятся данные в момент, когда они уже вышли из А, но еще не дошли до Б?

- **Нулевая ёмкость (No buffering):** Буфера нет. Это «жесткая синхронизация». Если Процесс А хочет передать данные, он **замирает (блокируется)** до тех пор, пока Процесс Б не вызовет команду «Принять». Они встречаются в одной точке времени. Это называется **Рандеву**.
    
- **Конечная ёмкость (Bounded Buffer):** Есть очередь, например, на 10 сообщений.
    
    - Если очередь пуста — получатель ждет.
        
    - Если очередь полна — отправитель ждет.
        
    - Если в очереди есть место — отправитель кидает данные и сразу идет работать дальше (асинхронность).
        
- **Бесконечная ёмкость (Unbounded):** Теоретическая модель, где отправитель вообще никогда не ждет. В реальности ограничена всей доступной оперативной памятью.
    

#### ○ Модели передачи (Format)

- **Потоковая (Byte Stream):** Данные — это просто бесконечная лента байтов. Система не знает, где начало или конец сообщения. Процесс Б просто отрезает столько байтов, сколько ему нужно. (Так работает протокол TCP или конвейеры в консоли `|`).
    
- **Модель сообщений (Message-based):** Данные передаются четкими блоками с заголовками. Ядро ОС гарантирует: если ты отправил сообщение «ПРИВЕТ» и сообщение «ПОКА», получатель увидит их как два отдельных объекта, а не как одну строку «ПРИВЕТПОКА».
    

---

### Подведем итог (Зачем это знать?)

Когда разработчик или системный архитектор выбирает способ связи, он смотрит на эти пункты как на чек-лист:

1. Если важна **скорость** — берем **Shared Memory** с **прямой адресацией** и **дуплексом**.
    
2. Если важна **надежность** — берем **Message Passing** с **косвенной адресацией** и **конечным буфером**.



![[Pasted image 20251230185936.png]]

>Что такое pipe? «Это защищенный ядром буфер, работающий по принципу FIFO, который предоставляет косвенную потоковую связь между двумя процессами. В Unix он односторонний, а в Windows — двусторонний».

---
### 1. Виды каналов: Неименованные (Pipe) и Именованные (FIFO)

- **Неименованные (pipe)**:Это временный канал, который существует только в оперативной памяти. Доступ к нему осуществляется через файловые дескрипторы. Он может использоваться только между родственными процессами (например, родитель-потомок), так как дескрипторы передаются по наследству через fork(). Как только программы закрываются, канал исчезает.
    
- **Именованные (fifo):** Этот канал имеет имя в файловой системе (в UNIX известен как FIFO). Он позволяет общаться любым процессам на одной машине, не связанным отношениями родитель- потомок, поскольку его можно открыть по имени, как обычный файл. Он сохраняется в файловой системе до явного удаления.
    

### 2. Потоковая модель передачи (Byte Stream)

Канал работает как **шланг с водой**.

- В нем нет структуры. Процесс-отправитель может записать 100 байт за один раз, а процесс-получатель может прочитать их по 1 байту за 100 заходов.
    
- Система не разделяет данные на «сообщения». Это просто бесконечный поток байтов. Разбираться, где заканчивается одна команда и начинается другая, должны сами программисты.
    

### 3. Буфер ядра и отсутствие Random Access

Внутри ядра ОС выделяется небольшой кусок памяти под этот канал (обычно несколько килобайт).

- **Никакого Random Access:** Ты не можешь сказать: «Прочитай мне 5-й байт из середины канала». Это очередь типа **FIFO** (First In, First Out) — «первым пришел, первым ушел». Ты можешь только забирать данные из начала и дописывать в конец.
    
- **Защита:** Процессы не имеют прямого доступа к этому буферу, они общаются с ним только через системные вызовы `read` и `write`.
    

### 4. Ограничение: Только два процесса

Стандартный канал — это линия связи «точка-точка». Один пишет в один конец («вход»), другой читает из другого («выход»). Нельзя сделать так, чтобы в один pipe писали сразу пять разных программ и еще десять читали — данные просто перемешаются в кашу.

### 5. Направленность (Unix vs Windows)

Это важное различие архитектур:

- **В Unix (Linux, macOS):** Каналы **Simplex** (односторонние). Если нужно, чтобы процессы говорили друг с другом в обе стороны, приходится создавать **два** канала: один для передачи «туда», другой — «обратно».
    
- **В Windows:** Каналы изначально проектировались как **Full-duplex**. По одному и тому же каналу данные могут течь одновременно в обе стороны.
    

### 6. Косвенная адресация

Ты не обращаешься к процессу напрямую по его ID.

- В случае неименованных каналов ты используешь **дескриптор** (числовой номер, который дала тебе ОС при создании канала).
    
- В случае именованных — ты открываешь его **по имени**, как обычный файл (например, `/tmp/my_pipe`).


Общие Вопросы Реализации Каналов 
1. Направленность (Bidirectional vs. Unidirectional): Позволяет ли канал двусторонний обмен данными или только односторонний? 
2. Режим Двусторонней Связи (Half-Duplex vs. Full-Duplex): Если связь двусторонняя, может ли она происходить одновременно (Full-duplex), или только в одном направлении за раз (Half-duplex)? 
3. Требуемые Отношения (Relationship Requirement): Должны ли процессы иметь определённые отношения (например, родитель- потомок) для использования канала? 
4. Сетевая Связь (Network Communication): Может ли канал использоваться для связи между процессами на разных машинах, или он ограничен процессами, находящимися на одной машине?

---
### Pipe

![[Pasted image 20251230194209.png]]

Характеристики 
- Направленность: Односторонняя (Unidirectional). Если нужна двусторонняя связь, необходимо создать два отдельных канала. 

- Отношения: Требуется отношение родитель-потомок. Канал существует только до тех пор, пока процессы, использующие его, не завершатся. 

- Сетевое взаимодействие: Не поддерживается. Работает только внутри одной машины. 

- Механизм: Работает по принципу производитель-потребитель. Производитель пишет в один конец (write end), потребитель читает из другого (read end).

Реализация

1. Создание: Используется системный вызов pipe(int fd[]). Создаётся два файловых дескриптора: fd[0] для чтения (read end) и fd[1] для записи (write end). 
2. Обмен: После создания родительский процесс вызывает fork(). Дочерний процесс наследует оба дескриптора (fd[0] и fd[1]). 
3. Использование: Процессы используют обычные системные вызовы read() и write(). 
4. Важный Шаг: Оба процесса должны закрыть неиспользуемые концы канала, чтобы читающий процесс мог корректно обнаружить конец файла (read() вернёт 0), когда пишущий процесс закроет свой конец.

>Наследование: В отличие от UNIX, где дескрипторы наследуются автоматически, в Windows программист должен явно разрешить наследование дескрипторов канала через структуру SECURITY_ATTRIBUTES и STARTUPINFO при создании дочернего процесса.

### Именованные каналы

Характеристики 
- Отношения: Не требуется отношение родитель-потомок. Любой процесс на машине может получить доступ к каналу, зная его имя. 
- Сетевое взаимодействие: Поддерживается (особенно в Windows). 
- Долговечность: Канал продолжает существовать даже после завершения процессов, которые его использовали, пока его явно не удалят. 
- Топология: Может быть много процессов-писателей. 

Реализация в UNIX-системах (FIFO) 
- Название: В UNIX именованные каналы называются FIFO (First-In, First-Out). 
- Создание: Создаются с помощью системного вызова mkfifo(). После создания FIFO появляется в файловой системе как обычный файл. 
- Связь: Используются обычные вызовы open(), read(), write() и close(). 
- Режим связи: Поддерживают двустороннюю связь, но только в режиме полудуплекса (half-duplex) (данные могут идти в одном направлении за раз

## Список литературы

https://gemini.google.com/share/a0f4182d6bdf

https://chatgpt.com/share/69540261-9858-8001-b0b0-4c13b75fe1bb

